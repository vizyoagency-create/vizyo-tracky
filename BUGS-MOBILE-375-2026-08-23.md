# Bugs mobile 375 px — tableau de bord

> Balayage du 2026-08-23 : **61 routes** + **6 modales** à 375×812, en local (données réelles de dev),
> session super-admin, mesure automatique du débordement + examen visuel de chaque capture.
> **Résultat global : 0 débordement horizontal sur les 61 routes, modales principales saines.**
> Les défauts relevés sont ciblés et listés ci-dessous.
>
> **Mode d'emploi** : même logique que `DECISIONS-A-TRANCHER-2026-08-23.md` — chaque bug se corrige
> dans un chat séparé avec le modèle de prompt en bas de page, puis coche sa ligne **Statut**.

**Gravité** : 🔴 gênant à l'usage · 🟠 visible mais utilisable · 🟡 finition.

---

## Bugs à corriger

### M1 · 🔴 `/vehicles` (vue liste) — plaques cassées en trois lignes
La colonne véhicule est trop étroite : « AH-001-TS » devient « AH- / 001- / TS », et les en-têtes
« VÉHICULE / STATUT » se chevauchent. La plaque doit rester sur UNE ligne (white-space, min-width,
ou colonnes repensées à 375 — les vues grille/cartes, elles, sont saines).
— `apps/web/src/app/features/vehicles/vehicles-list.component.ts` (vue « liste »)
**Statut :**

### M2 · 🔴 Fiche véhicule — tuile POSITION en purée
« Voir sur la carte » rend mot à mot à la verticale, les coordonnées s'écrasent contre le bord.
Revoir la composition de la tuile à 375 (libellé au-dessus, action en dessous).
— `apps/web/src/app/features/vehicles/vehicle-detail.component.ts` (carte POSITION)
**Statut :**

### M3 · 🟠 `/users/overview` — fil d'Ariane et kicker superposés
« ← Utilisateurs » et « PILOTAGE DES ACCÈS » se chevauchent sur la même ligne.
— `apps/web/src/app/features/users/permissions-overview.component.ts` (en-tête)
**Statut :**

### M4 · 🟡 `/users` — onglet « Conducteurs » orphelin
La barre d'onglets wrappe : « Conducteurs » passe seul sous « Utilisateurs / Rôles & permissions »,
détaché visuellement de la barre. Autoriser le défilement horizontal de la barre ou compacter.
— `apps/web/src/app/features/users/` (barre d'onglets)
**Statut :**

### M5 · 🟡 `/admin/trackers` — la loupe chevauche le placeholder
« 🔍Rechercher IMEI… » : l'icône ne réserve pas son espace (padding-left du champ).
— écran Trackers, champ de recherche
**Statut :**

### M6 · 🟡 `/admin/ai-usage` — badge inséré au milieu de la phrase
« …pour activer ou couper son IA (l'IA est [désactivée par défaut] ). » : le badge flotte à droite
et la parenthèse fermante reste orpheline. Reformuler ou sortir le badge de la phrase.
— `apps/web/src/app/features/observability/admin-ai-usage.component.ts`
**Statut :**

### M7 · 🟡 `/assistance` — troncature en plein mot
Les aperçus de demandes coupent sans ellipse : « …ou demander un ra ». Ajouter « … » (ellipsis CSS
ou slice + …).
— `apps/web/src/app/features/assistance/` (cartes « Vos demandes »)
**Statut :**

### M8 · 🟡 Centre d'alertes — « a investiguer » sans accent
Bandeau « 330 erreurs en 24h — a investiguer » → « à investiguer ». (Le garde ne liste pas « a »,
ambigu avec le verbe avoir — corriger la chaîne suffit.)
— `apps/web/src/app/features/observability/` (bandeau du centre d'alertes)
**Statut :**

### M9 · 🟠 URL inconnue → `/login` même connecté
`{ path: '**', redirectTo: 'login' }` : un utilisateur CONNECTÉ qui suit un lien mort atterrit sur
l'écran de connexion — sensation de déconnexion. Proposition : rediriger vers `/dashboard` quand une
session existe (le garde d'auth fera le reste).
— `apps/web/src/app/app.routes.ts:654`
**Statut :**

### M10 · 🟡 `/account` — 4e onglet à vérifier au doigt
La barre « Profil / Invitations / Notifications / … » est coupée au bord droit. Vérifier qu'elle
DÉFILE au toucher (sinon le 4e onglet est inaccessible à 375).
— `apps/web/src/app/features/account/account.component.ts`
**Statut :**

---

## Vérifié sain à 375 px — pour mémoire

- **0 débordement horizontal sur les 61 routes** (mesuré `scrollWidth ≤ innerWidth`).
- Écrans denses OK : dashboard, alertes, rapports, scores, missions, agenda (grille mois),
  installations, paramètres, horaires flotte, qualité GPS, notifications, centre d'alertes,
  `/driver` (jamais vérifié jusqu'ici) et tous les écrans admin re-balayés hors 429.
- **Modales saines** : création véhicule (2 étapes), export PDF, réservation agenda (feuille basse),
  nouvel événement, signalement d'incident, tiroir de navigation « Plus ».
- Redirections voulues confirmées : `/groups→/vehicles`, `/geofences→/alerts`, `/drivers→/users`,
  `/depot→/dashboard` (garde de rôle).

## À vérifier en production (le local ne peut pas conclure)

- **`/map`** : les tuiles OSM étaient bloquées par le bac à sable de l'outillage — la carte à 375
  n'a pas pu être jugée. Un coup d'œil en prod suffit.
- **`/integrations`** (400 local) et **`/admin/sms`** (503 local) : états d'erreur PROPRES en local ;
  confirmer que les états « sains » se chargent en prod.
- **Limiteur de débit (429)** : mordu en local pendant le balayage ET vu en prod dans la console du
  propriétaire (`GET /api/vehicles/snapshot → 429`, quelques onglets ouverts seulement). À trancher :
  l'app fait beaucoup d'appels par page — relever la limite, ou réduire les appels. → à ajouter aux
  décisions (`DECISIONS-A-TRANCHER-2026-08-23.md`).
- **Écrans à état riche non couverts en local** : `/places` (0 lieu en local — vérifié en prod le
  23/08 ✔), mode veilleur et écrans `depot` (rôles dédiés — dans la recette manuelle),
  `trip-note-modal` et confirmations moteur (exigent un contexte réel).

---

## Modèle de prompt pour corriger UN lot de bugs dans un chat séparé

```
Dans le dépôt vizyo-tracky, worktree D:\www\vizyo-agency\vizyo-tracky\wt-allowlist
(ou un worktree isolé) — git fetch + rebase sur origin/main d'abord.

Corrige les bugs <M1, M2, …> du fichier BUGS-MOBILE-375-2026-08-23.md (contexte et
fichiers dans chaque entrée). Captures de référence : balayage du 2026-08-23.

Règles du chantier : NE DÉPLOIE PAS. Aucun backtick dans un commentaire de
template:/styles:. Les cinq gardes bloquants (npm run verif:accents / litteraux /
variables / contraste / confirmations) doivent passer. CHAQUE correction se vérifie
DANS LE NAVIGATEUR à 375×812 avant/après (session locale : jeton JWT local, voir
apps/web/e2e/helpers/session-locale.ts, protocole TEST_PLAN T04) — pas de conclusion
sur lecture de code. À la fin : git fetch + rebase, committe et pousse, et remplis la
ligne Statut de chaque bug traité dans BUGS-MOBILE-375-2026-08-23.md.
```
