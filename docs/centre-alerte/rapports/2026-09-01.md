# Audit du centre d'alerte — 2026-09-01

> Collecte : **2026-09-01 de 07:39 à 07:56 UTC** (heure serveur = UTC).
> `tracky-api` démarré le **26/08 05:55:08**, `restarts=0` · image `tracky-api:latest` du **26/08 05:53:41**
> `tracky-web` démarré le **26/08 05:57:53**, `restarts=0` · image `tracky-web:latest` du **26/08 05:57:17**
> **Aucun déploiement, aucun redémarrage depuis 6 jours.** Tout ce qui suit s'exerce sur le code du 26/08.
>
> ⚠️ **Premier passage depuis le 26/08 — six jours sans audit.** Les séries de ce rapport
> sautent donc du 26/08 au 01/09 ; les journées 27→31/08 n'ont pas été instruites au fil de l'eau
> et ne sont reconstituées ici que par ce que la base garde.

**Verdict :** le centre d'alerte est presque vide — **4 défauts actifs, 0 sur 24 h** — et il a raison,
mais il ne raconte pas la journée qui compte : le **31/08, six véhicules ont perdu leur alimentation
en deux heures, leur batterie de secours s'est vidée jusqu'à 0 %, et leurs six boîtiers sont muets
depuis 18 heures** — la flotte est passée de **38 à 32 émetteurs** et n'est pas remontée.

---

## Chiffres

| | |
|---|---|
| Lignes `error_logs` **actives** | **26**, dont **4 défauts** et **22 `DEGRADATION`** — *le chiffre du rapport est **4**, cf. TRK-037* |
| Défauts actifs sur 24 h | **0** *(les 3 lignes des 24 h sont toutes des `DEGRADATION` Overpass)* |
| Lignes archivées | **32** *(classées par un humain, toujours en base)* |
| Ligne la plus récente | **01/09 01:45:16** — une `DEGRADATION` |
| Constats de disparition | **0** — et `temoin_arme` rend bien ses **4 lignes**, toutes `actif = O` |
| Écart `ins − del − live` (`error_logs`) | **13 250** — **8ᵉ point identique**, `n_tup_del` figé à **3 785** |
| Écart `ins − del − live` (`alerts`) | **0** *(51 910 − 41 824 − 10 086)* |
| Trackers FAILING | **0** |
| Trackers hors ligne > 1 h | 🔴 **12** (dont **0** jamais mis en service) — **contre 5 le 26/08** |
| Commandes en attente > 10 min | **1** *(FV-941-LZ, 23 min)* |
| Commandes `fix_continuous` / jour (7 j) | **73,3** *(total famille **513** : 404 ACK · 108 FAILED · 1 SENT)* — **0 réponse de boîtier** |
| Boîtiers sous le minimum matériel (`cadence_resume`) | **0** sur **44** — cible la plus basse **20 s** *(4ᵉ point à 0)* |
| Débit | **≈ 90 000 trames / 24 h** pour **32 émetteurs** *(2 813/émetteur)* — contre **125 414 / 38** *(3 300)* le 26/08 |
| Positions stockées / 24 h | **23 182** *(J−1 : 15 523)* |

---

## 🔴 À traiter

### 1. 🔵 Six boîtiers perdus le 31/08 — coupure d'alimentation confirmée, batterie à 0 %, muets depuis 18 h

**Ce n'est pas un défaut logiciel : c'est le fait le plus important de la semaine, et la chaîne
d'alerte l'a parfaitement vu.** Il est en tête parce qu'il est **toujours en cours**.

Le déroulé, mesuré ligne à ligne (UTC) :

| Heure | Ce qui se passe |
|---|---|
| **07:29 → 09:00** | six véhicules perdent leur **alimentation externe**, à 15-30 min d'écart |
| **08:15 → 09:30** | six alertes **`POWER_CUT` CRITICAL**, « coupure **CONFIRMÉE au réexamen** : batterie passée de 100 % à 86-89 % » |
| 08:26 → 09:41 | les six sont **escaladées** automatiquement, ~10 min après leur création |
| **11:19 → 13:21** | huit alertes **`LOW_BATTERY`** sur les mêmes véhicules |
| 11:39 → 11:40 | un exploitant **acquitte** les six `POWER_CUT` |
| **11:53 → 13:50** | les six boîtiers **s'éteignent**, dans l'ordre exact où ils avaient été coupés |

**La preuve matérielle est dans la dernière trame de chacun** — exemple FR-629-AD à 13:50:47 :

```
imei:864035054489431,tracker,260831134948,0%,F,134948.000,A,4336.94308,N,00123.85353,E
                                          ^^^ batterie 0 %      ^^^^^^^^^ position figée
```

`lastBatteryPercent = 0` pour **les six**, position **identique** sur toute la fin, `lastKnownIgnition = false`.
**Ils étaient garés, ils ont été débranchés, leur batterie interne a tenu 3 à 4 heures.**

**Ce que ça a coûté et ce que ça coûte encore :**

- la flotte émettrice est passée de **38 à 30** boîtiers en 8 h le 31/08, et stagne à **32** depuis ;
- **12 des 15** immobilisations non confirmées des 3 derniers jours visent ces six véhicules — un
  `CUT` et un `RESTORE` chacun : **la coupe programmée continue de commander six boîtiers morts** ;
- les `SENT_UNCONFIRMED` d'`engine_control_commands` remontent à **9 le 31/08 et 6 le 01/09**,
  contre **2/jour** au 26/08. *Le compteur de TRK-018 monte pour une raison entièrement physique.*

> 🔑 **Six coupures d'alimentation en deux heures, échelonnées de 15 à 30 minutes, sur des véhicules
> à l'arrêt au même endroit, ne sont pas six pannes indépendantes.** C'est un geste, ou un incident
> unique, sur un parc de stationnement. **Action TERRAIN, urgente** : identifier ce qui a débranché
> ces six véhicules le 31/08 au matin, et les remettre en service. Rien de logiciel ne les
> rallumera.

**✅ Et au passage, [TRK-040](../REFERENCE-ERREURS.md#trk-040) vient d'être exercé six fois et il a
eu raison six fois.** La « confirmation par la pente » — ne crier qu'après avoir vérifié que la
batterie *se vide* — a produit six `POWER_CUT` dont les six se sont soldées par une batterie à 0 %.
**Zéro faux positif sur six cas.** C'est le premier exercice à cette échelle depuis son
déploiement, et il est concluant.

---

### 2. 🆕 [TRK-052](../REFERENCE-ERREURS.md#trk-052) — **acquitter une alerte RÉ-ARME l'alarme** : le geste de triage lève la déduplication de 6 h

**Signature** — `alerts | <TYPE> | seconde alerte du même type sur le même véhicule à <DURÉE> < 6 h,
créée immédiatement après l'acquittement de la première`

**La mesure, et la corrélation est exacte : 2 cas sur 2.**

| Véhicule | 1ʳᵉ alerte `LOW_BATTERY` | Acquittée | 2ᵉ alerte | Écart |
|---|---|---|---|---|
| **FS-808-CE** | 11:50:37 | **11:50:56** *(19 s plus tard)* | **12:05:38** | 15 min |
| **FZ-731-YF** | 11:55:34 | **12:05:59** | **12:10:36** | 15 min |
| FS-253-HR | 11:19:32 | 11:40:00 | — | — |
| FZ-862-VY | 12:28:21 | 12:39:23 | — | — |
| FW-298-WV | 13:10:24 | 19:15:38 | — | — |
| FR-629-AD | 13:21:21 | 19:15:38 | — | — |

**Les deux seuls véhicules à recevoir un doublon sont les deux dont la première alerte a été
acquittée AVANT que la condition physique cesse.** Les quatre autres, acquittés plus tard ou une
seule fois, n'en ont eu qu'une — alors que leur batterie se vidait tout autant.

**Cause racine** — `apps/api/src/alerts/alerts.service.ts:229-240`. Le prédicat de déduplication
porte **deux** conditions, et la seconde est le défaut :

```ts
const dejaOuverte = await this.prisma.alert.findFirst({
  where: {
    vehicleId: tracker.vehicle.id,
    type: mapping.type,
    acknowledgedAt: null,                                   // ← ICI
    createdAt: { gte: new Date(Date.now() - DEDUP_ALARME_MS) },
  },
});
```

Le commentaire l'assume : *« On rouvre seulement si la précédente a été acquittée — l'exploitant a
alors signifié qu'il avait traité l'épisode. »* **L'hypothèse est fausse en exploitation.** Acquitter
n'est pas « j'ai traité », c'est « j'ai vu » — mesuré ici à **19 secondes** après la création, sur
une condition physique qui allait durer encore deux heures. `createPowerCutConfirmedAlert`
(ligne 494) porte le **même** prédicat.

**Pourquoi c'est plus grave que deux doublons.** C'est exactement le mécanisme qui ramènerait le
déluge de [TRK-022](../REFERENCE-ERREURS.md#trk-022) : le 12/08, FG-669-DQ a produit **1 317**
alertes `OVERSPEED` en une journée. La garde de 6 h l'a ramené à **1-2 par jour** — mais elle ne
protège que tant que **personne n'acquitte pendant l'épisode**. Un exploitant qui nettoie son écran
pendant que le véhicule roule encore à 135 km/h rouvre le robinet à la trame suivante. *La
protection dépend d'un geste humain qu'aucune fiche n'a jamais nommé comme condition.*

**Correctifs proposés**, du moins cher au plus juste :

1. **Retirer `acknowledgedAt: null` du prédicat** et s'en tenir à la fenêtre de 6 h. Acquitter
   redevient « j'ai vu ». Contrepartie assumée : un épisode réellement nouveau dans les 6 h serait
   fondu dans le précédent — c'est déjà ce que la fenêtre accepte aujourd'hui pour toute alerte non
   acquittée.
2. **Poser un plancher indépendant de l'acquittement** : aucune réouverture avant N minutes
   (proposition : 15 min), acquittée ou non. Corrige le cas mesuré ici sans toucher au principe.
3. **Le plus juste — fermer sur l'ÉTAT, pas sur l'écran** : la réouverture n'est légitime que si la
   condition physique a cessé (batterie remontée, vitesse repassée sous le seuil pendant N minutes).
   C'est la notion d'**épisode** que [TRK-022](../REFERENCE-ERREURS.md#trk-022) réclamait déjà, et
   qui n'a jamais été écrite.

**Vérification — double condition, à relever séparément :** après correctif, le nombre d'alertes
par **épisode** doit tomber à 1 **pendant que** le nombre d'**épisodes distincts** reste identique.
Si les deux baissent, on a supprimé du signal et pas des doublons.

---

### 3. 🔴 [TRK-021](../REFERENCE-ERREURS.md#trk-021) — **17 jours sans le moindre essai**, la règle des 7 jours est dépassée de dix

Le correctif « l'enveloppe suit le canal » (PR #110) est en production depuis le **23/08**. Il n'a
**jamais** été exercé. Mesure de ce jour, sur 30 jours de `tracker_commands` :

| `createdAt` | `templateId` | **`channel`** | `status` | `lastError` |
|---|---|---|---|---|
| 2026-08-15 13:26 | `shock_on` | **TCP** | FAILED | ACK timeout after 15000 ms |
| 2026-08-15 13:26 | `sensitivity` | **TCP** | FAILED | ACK timeout after 15000 ms |
| 2026-08-14 21:02 | `shock_on` | **TCP** | FAILED | ACK timeout after 15000 ms |
| … | *(6 lignes de plus, toutes TCP, toutes FAILED)* | | | |

**Toutes ces lignes sont ANTÉRIEURES au correctif** — la dernière tentative SMS-only date du
**15/08 13:26**, soit **17 jours**. Les deux seules commandes hors `fix_continuous` depuis sont
deux `raw` du 24/08 (les canaris de TRK-045).

> 🔑 **Un correctif que rien n'a exercé rend exactement le même zéro qu'un correctif qui marche.**
> Ce zéro-là dure depuis 17 jours et il n'est pas une réussite : c'est un **oubli d'essayer**.
> Et il bloque autre chose — le volet 3 de [TRK-022](../REFERENCE-ERREURS.md#trk-022) (provisionner
> le seuil de survitesse sur les 42 véhicules muets) **exige** que TRK-021 soit prouvé, faute de
> quoi les 42 commandes partiront en TCP et expireront comme les huit ci-dessus.

**Le geste, et il tient en une minute :** émettre un `shock_on` depuis
`/admin/trackers/:id` sur un boîtier vivant, puis lire **une** colonne :

```sql
SELECT "createdAt", "templateId", channel, status FROM tracker_commands
WHERE "templateId" = 'shock_on' ORDER BY "createdAt" DESC LIMIT 1;
```

**Preuve attendue : `channel = 'SMS'`.** Si c'est encore `TCP`, le correctif est faux et la fiche
repasse en 🔴 le jour même.

---

### 4. ⚠️ Quatre boîtiers émettent 14 à 50 fois plus vite que la trame qu'ils viennent de recevoir

**Le fait mesuré, et rien de plus** (détail relevé à 07:45 UTC — ces valeurs bougent en permanence,
cf. §4 bis) :

| Véhicule | Cible `desiredFixIntervalS` | Cadence mesurée | Dernière trame `,C,` réellement reçue |
|---|---|---|---|
| **GR-294-VW** | 99 s | **2 s** | `**,imei:…,C,99s;` le 31/08 **23:53:09** |
| **GS-878-NX** | 99 s | **5 s** | `**,imei:…,C,99s;` le 31/08 **09:11:14** |
| **FG-669-DQ** | 99 s | **7 s** | `**,imei:…,C,99s;` le 31/08 **22:39:08** |
| EP-047-TY | 99 s | 18 s | `**,imei:…,C,99s;` le 31/08 **12:27:59** |

**Le modèle « deux chiffres, unité jetée » de [TRK-045](../REFERENCE-ERREURS.md#trk-045) prédit
99 s pour `,C,99s;`** — et il avait été validé au canari le 24/08 (six écarts de 99 s pile). Ici,
trois boîtiers sur quatre rendent 2 à 7 s. **Le modèle ne couvre pas ce cas.**

Second fait, indépendant : **un cycle quotidien identique**. Chacun de ces boîtiers reçoit un
`,C,20s;` puis un `,C,99s;` quelques minutes ou quelques heures plus tard, **aux mêmes heures les
30 et 31/08**. Et le motif d'échec dominant de la semaine dit l'inverse du tableau ci-dessus :

| Motif observé (`commandes_motifs_7j`) | n |
|---|---|
| « cadence réelle **99 s** pour **20 s** demandé » *(toutes durées confondues)* | **~29** |
| « cadence réelle **2-3 s** pour 20 s ou 99 s demandés » | **8** |
| « cadence réelle 198 s pour 99 s demandés » | 2 |

> ⚠️ **Ce n'est pas une tendance, c'est un premier point sur une définition neuve.** Deux lectures
> restent ouvertes — le boîtier applique la commande **précédente** (un cran de retard), ou
> l'auto-alignement oscille et mesure entre deux régimes — et rien dans les données collectées ne
> permet de trancher. **Ce qui est certain : la cible et la mesure se contredisent dans les deux
> sens sur les mêmes boîtiers, tous les jours.**

**Ce que ça ne coûte PAS, et il faut le dire :** le débit global est de **≈ 90 000 trames / 24 h**,
soit **2 813 par émetteur** — en dessous des 3 300/émetteur du 26/08. **Il n'y a pas de déluge.**
Les quatre boîtiers concernés émettent 3 420 à 4 557 trames/24 h, dans la fourchette du reste du
parc. *Le défaut est un défaut de pilotage, pas encore un défaut de facture.*

**Test daté proposé** — un troisième canari, comme le 24/08 : émettre `,C,99s;` en commande brute
sur **GR-294-VW** (celui à 2 s) et relever les douze écarts suivants dans `wire_logs`. Si les écarts
valent 99 s, le boîtier obéit et c'est l'auto-alignement qui le dérègle ensuite ; s'ils valent 2 s,
le modèle de TRK-045 est incomplet et la fiche doit être rouverte.

---

## 🟢 Prouvé par le comportement — trois tests datés répondent OUI

### ✅ [TRK-037](../REFERENCE-ERREURS.md#trk-037) — le test daté du 26/08 est **répondu**

La consigne était : *« la PROCHAINE ligne Overpass doit porter `level = 'DEGRADATION'` »*.

| `source` | `level` | n | actives | dernière |
|---|---|---|---|---|
| `trip-analysis` | **`DEGRADATION`** | **22** | 22 | **01/09 01:45:16** |
| `trip-analysis` | `ERROR` | 16 | **0** | **26/08 04:47:19** *(avant la bascule de 05:55)* |

**Aucune ligne `trip-analysis` en `ERROR` depuis le déploiement, 22 en `DEGRADATION` depuis.**
La bascule est nette, sans exception, sur six jours. Et le correctif **ne cache rien** : les
22 lignes sont écrites, actives, consultables — elles ne comptent simplement plus comme des défauts.
*C'est la différence entre nommer et masquer, et elle est vérifiée.*

### ✅ [TRK-022](../REFERENCE-ERREURS.md#trk-022) volet 1 — **exercé, et le facteur est de 1 000**

La déduplication de 6 h était en ligne depuis le 19/08 sans avoir jamais été mise à l'épreuve.
Elle l'a été. Série `OVERSPEED` par jour, sur FG-669-DQ (le seul véhicule du parc qui en produise) :

| 12/08 | 13/08 | 19/08 | 20 | 21 | 22 | 24 | 26 | 28 | 29 | 31 | 01/09 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| **1 317** | 5 | 2 | 1 | 1 | 1 | 1 | 1 | 1 | 2 | 2 | 1 |

Et les intervalles entre deux alertes consécutives sont **toujours ≥ 6 h 10** (29/08 : 07:42 puis
13:52 ; 31/08 : 06:13 puis 13:27) — la fenêtre est respectée au détail près.

⚠️ **Deux moitiés de la fiche restent entières :**
- **le volet 2 est inchangé** — 1 657 alertes `OVERSPEED` sur 30 jours, **1 seul véhicule sur 44**.
  42 boîtiers restent incapables d'alerter, et rien à l'écran ne le dit ;
- **l'enrichissement n'a pas été fait** — les messages disent toujours « Vitesse détectée :
  135 km/h », sans durée ni maximum d'épisode, ce que la fiche exigeait.

### ✅ [TRK-047](../REFERENCE-ERREURS.md#trk-047) — le correctif divise par 5, sans supprimer le signal

| Jour | 24/08 | 25/08 | **correctif 25/08 09:22** | 26/08 | 27 | 28 | 29/08 | 30 | 31 | 01/09 |
|---|---|---|---|---|---|---|---|---|---|---|
| lignes `recompute:no-positions` | 1 | 2 *(08:22 et 09:10, **avant**)* | ▼ | 1 | 0 | 0 | 1 | 0 | 0 | 0 |

**Avant : ~1,5 ligne/jour. Après : 2 lignes en 6,9 jours, soit 0,29/jour.** Et les deux lignes
restantes ne sont **pas** des faux positifs de stationnement : elles portent sur **FZ-862-VY** et
**GLA•KC•31**, deux véhicules qui n'avaient **aucune position** dans la fenêtre — GLA•KC•31 est
justement celui dont le GPS était perdu depuis le 28/08 21:40. Le correctif est **fail-open vers le
signal** quand le voisinage est vide : ces deux lignes sont son comportement voulu, pas sa faille.

---

## 🟠 Connu, correctif en attente

| Fiche | État ce jour |
|---|---|
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) | 🔴 **inchangée** — `acquittees_boitier` = **0 sur 513** `fix_continuous` en 7 j. Les 404 `ACKNOWLEDGED` n'en referment pas une ligne |
| [TRK-016](../REFERENCE-ERREURS.md#trk-016) | 🔴 9ᵉ point. Trajets sans recalage : 88,8 · 85,2 · 89,5 · 87,6 · 88,7 · 96,1 · **80,3 %** (31/08). ⚠️ Le 01/09 rend **57,1 %** — mais sur **77 trajets d'une journée en cours** : *un point partiel n'est pas un point*. À relever demain sur la journée pleine |
| [TRK-018](../REFERENCE-ERREURS.md#trk-018) | 🟠 **333** immobilisations `SENT_UNCONFIRMED` (316 le 26/08). **12 des 15 des 3 derniers jours visent les six boîtiers morts** — proposition ajoutée à la fiche : distinguer à l'écran « boîtier hors ligne à l'envoi » de « nul ne sait » |
| [TRK-035](../REFERENCE-ERREURS.md#trk-035) | 🟠 **10ᵉ journée pleine sans disparition** ; témoin **4/4** ; écart **13 250** inchangé (8ᵉ point) ; `errorDeleted = 0` du 28/08 au 01/09 — **16ᵉ jour**. La séparation des rôles reste entière |
| [TRK-050](../REFERENCE-ERREURS.md#trk-050) | 🟢 **toujours AUCUNE OCCASION** — `restarts = 0`, aucun redémarrage d'API depuis le 26/08 05:55. **6 jours** que le test attend |
| [TRK-051](../REFERENCE-ERREURS.md#trk-051) | 🟢 déployé, **non exercé à l'écran** — le badge ambre « CIBLE ATTEINTE (mesurée) » n'a été vérifié qu'en littéral dans le bundle, jamais ouvert |
| [TRK-017](../REFERENCE-ERREURS.md#trk-017) | ⚠️ **réapparition de faible amplitude** — voir ci-dessous |

### ⚠️ TRK-017 — la signature réapparaît, à 1/25ᵉ de son amplitude, et se répare seule

Une ligne `sms-allowlist` le **30/08 23:25:00** : « **1** numéro(s) manquant(s) rétabli(s) »,
`episodeRepairs: 1`, `removalsBlocked: 0`. La fiche est 🟢 **CORRIGÉ** : au sens du §6, une
signature corrigée qui revient est une **régression** et doit être instruite.

**Elle l'a été, et le verdict est rassurant sans être une fermeture.** Les huit dernières
réconciliations horaires (00:25 → 07:25 le 01/09) rendent toutes :

```
{"added": 0, "removed": 0, "skipped": 0, "unchanged": 46, "episodeRepairs": 0, "removalsBlocked": 0}
```

**32 heures sans un seul rétablissement**, sur **46** numéros — contre **25 numéros effacés en
boucle plusieurs fois par jour** au plus fort de la fiche. Un rétablissement isolé d'**un** numéro
est le comportement normal du cron quand la liste poussée gagne une entrée.

🗓️ **Test daté :** si une **deuxième** ligne `sms-allowlist` apparaît dans les 7 jours, ou si
`removalsBlocked` devient non nul, **rouvrir TRK-017 en 🔴** — deux épisodes ne sont plus un
rattrapage. Sinon, la fiche reste 🟢 et l'épisode du 30/08 est classé comme rattrapage d'ajout.

---

## ⚪ Bruit / 🔵 Terrain

- **[TRK-037](../REFERENCE-ERREURS.md#trk-037)** — 22 lignes `DEGRADATION` Overpass, dernière le
  01/09 01:45. Rythme régulier d'une toutes les ~6 h. Dépendance tierce gratuite, contrepartie
  assumée. **Ne compte pas comme défaut.**
- **[TRK-001](../REFERENCE-ERREURS.md#trk-001)** — 1 ligne `gps-integrity` active, **28/08 23:45**,
  GLA•KC•31 : « boîtier vivant mais sans position GPS depuis 2 h ». `deadZoneOccurrences: 1` — donc
  **pas** encore une zone qualifiée bénigne. Le détecteur a parlé, l'alerte `GPS_LOST` a été créée :
  la chaîne a fonctionné. Action **terrain** : contrôler l'antenne de GLA•KC•31.
- **[TRK-047](../REFERENCE-ERREURS.md#trk-047)** — 2 lignes `TRIP_AUTOMATION` actives (26/08 et
  29/08), toutes deux sur des véhicules sans aucune position. Comportement **voulu** (fail-open).
- **`ACCIDENT` sur HD-779-MA le 31/08 17:30** — « roulait à 113 km/h et son boîtier n'émet plus
  depuis 2,2 h ». Instruit : le boîtier a bien eu **un trou de 4 h** (66 trames à 15 h, **zéro** de
  16 h à 19 h), puis est revenu à 20 h avec une **rafale de 686 trames** — un vidage de tampon.
  Il est aujourd'hui le **3ᵉ émetteur du parc** (4 390 trames/24 h) et n'est pas hors ligne. Alerte
  escaladée à 17:41, acquittée à 19:15. *Le profil (silence long puis rafale bufferisée) ressemble
  davantage à une perte de couverture qu'à un choc — mais l'alerte a fait son travail et la
  distinction demande une vérification humaine.*
- **6 autres boîtiers hors ligne > 1 h**, sans lien avec l'événement du 31/08 : trois sans véhicule
  (05/06, 01/07, 29/08 — stock), KSR•370 (muet depuis le 14/08, coupure d'alimentation documentée,
  action terrain), HD-292-SH (19/08), DZ-034-CA (21/08).

---

## Angles morts examinés

| Section | Mesure | Verdict |
|---|---|---|
| `temoin_arme` | **4** déclencheurs, tous `actif = O` | ✅ les zéros ci-dessous sont des **mesures**, pas des silences |
| `disparitions` | **0** constat | ✅ **10ᵉ journée pleine** sans effacement |
| `comptes_disparition` | `error_logs` écart **13 250**, `n_tup_del` **3 785** — inchangés | ✅ aucun `TRUNCATE`, aucun `DELETE` |
| `retention_declaree` | `errorDeleted = 0` du 28/08 au 01/09 | ✅ **16ᵉ jour** — l'application ne supprime rien et en tient le compte |
| `trackers_failing` | **0** | ✅ |
| `trackers_offline` | 🔴 **12** — dont **6 tombés le 31/08 entre 11:53 et 13:50** | 🔴 **le sujet du jour** (§1) |
| `commandes_en_attente` | **1** > 10 min | ✅ |
| `commandes_sante_7j` | **513** `fix_continuous` — 404 ACK / 108 FAILED / 1 SENT, **0 réponse boîtier** | 🟠 total **stable** (514 le 26/08) — le ré-étiquetage de TRK-051 tient |
| `commandes_motifs_7j` | motif dominant : « cadence réelle **99 s** pour **20 s** demandé » | ⚠️ voir §4 |
| `cadence_resume` | **0** sous le minimum matériel, cible la plus basse **20 s**, **44** boîtiers | ✅ **4ᵉ point à 0** |
| `cadence_derive` | **0 ligne** | ✅ |
| `cadence_reelle` | **5** rapides réels · **4** à ≤ 6 s · **2** vestiges périmés | ⚠️ voir §4 — TRK-048 sépare toujours correctement le vestige de la mesure |
| `gps_sans_fix` | **0 ligne** | ⚠️ **et ce zéro ne vaut rien ici** : la section exige une trame de moins de 30 min, or les véhicules concernés sont **entièrement hors ligne**. *Un capteur qui ne voit que les vivants rend zéro sur un mort.* |
| Alertes 7 j | **19** : 6 `POWER_CUT` · 8 `LOW_BATTERY` · 4 `OVERSPEED` · 1 `ACCIDENT` | 🔵 toutes du 31/08 sauf 3 — journée unique |
| Alertes 30 j | 1 657 `OVERSPEED` **(1 véhicule)** · 31 `POWER_CUT` (7) · 30 `GPS_LOST` (11) · 12 `LOW_BATTERY` (9) | 🟠 TRK-022 volet 2 : **1 véhicule sur 44** produit 99,8 % des alertes de survitesse |
| `OFF_SCHEDULE_MOVEMENT` | **0 depuis toujours** | ⚠️ **toujours aucune occasion** — 7ᵉ jour |
| Débit | **≈ 90 000** trames/24 h, **32** émetteurs (2 813/émetteur) · **23 182** positions | 🔴 **−6 émetteurs** ; le débit par émetteur baisse de 15 % — mesurer les deux, jamais le total seul |
| Recalage OSRM | **80,3 %** sans polyligne le 31/08 | 🔴 TRK-016, 9ᵉ point, plateau |

### 🔑 Ce que ce passage apprend sur la lecture des compteurs

> **Un compteur qui filtre sur « vivant » rend zéro sur un mort.** `gps_sans_fix` exige
> `lastSeenAt > now() − 30 min`. Les six véhicules du 31/08 n'ont plus de position **et** plus de
> trame : ils sortent de la section, et elle rend **0 ligne** — le même zéro qu'une flotte en
> parfaite santé. La panne la plus grave de la semaine est **invisible** dans la section conçue
> pour voir les pannes GPS, et visible seulement dans `trackers_offline`.
>
> *C'est la variante « angle mort » du piège du 26/08 : là, un compteur tombait parce que la
> catégorie voisine montait ; ici, un compteur reste à zéro parce que son critère d'entrée exclut
> précisément le cas grave.* **Vérifier ce qu'un filtre EXCLUT avant de lire son zéro.**

---

## Fiches ajoutées ou mises à jour

| Fiche | Mouvement |
|---|---|
| 🆕 [TRK-052](../REFERENCE-ERREURS.md#trk-052) | **créée** — l'acquittement ré-arme l'alarme ; gravité 2 ; corrélation 2/2 mesurée sur `LOW_BATTERY` du 31/08 |
| [TRK-037](../REFERENCE-ERREURS.md#trk-037) | ✅ **test daté RÉPONDU** — 22 `DEGRADATION` / **0 `ERROR`** depuis le 26/08 05:55 |
| [TRK-022](../REFERENCE-ERREURS.md#trk-022) | ✅ **volet 1 EXERCÉ ET PROUVÉ** — 1 317 → 1-2/j, intervalles ≥ 6 h 10. Volets 2 et 3 **entiers** |
| [TRK-040](../REFERENCE-ERREURS.md#trk-040) | ✅ **exercé six fois, 0 faux positif** — les 6 `POWER_CUT` confirmées par la pente se soldent par 6 batteries à 0 % |
| [TRK-047](../REFERENCE-ERREURS.md#trk-047) | ✅ **mesuré** — 1,5/j → 0,29/j ; les 2 lignes restantes sont le fail-open voulu |
| [TRK-021](../REFERENCE-ERREURS.md#trk-021) | 🔴 **17 jours sans essai** — remontée en tête ; canal mesuré **TCP** sur les 8 dernières tentatives (toutes antérieures au correctif) |
| [TRK-017](../REFERENCE-ERREURS.md#trk-017) | ⚠️ **réapparition à 1 numéro** le 30/08, puis 32 h à zéro sur 46 — test daté à 7 jours posé |
| [TRK-018](../REFERENCE-ERREURS.md#trk-018) | 🟠 **333** non confirmées ; proposition ajoutée : distinguer « boîtier hors ligne à l'envoi » |
| [TRK-045](../REFERENCE-ERREURS.md#trk-045) | ⚠️ **le modèle ne couvre pas 4 boîtiers** — `,C,99s;` reçu, 2 à 7 s mesurés. Canari proposé |
| [TRK-016](../REFERENCE-ERREURS.md#trk-016) | 🔴 9ᵉ point, **80,3 %** — plateau ; point partiel du 01/09 explicitement écarté |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) | 🔴 inchangée — 0 réponse de boîtier sur 513 |
| [TRK-035](../REFERENCE-ERREURS.md#trk-035) | 🟠 10ᵉ journée sans disparition ; 16ᵉ jour à `errorDeleted = 0` |
| [TRK-050](../REFERENCE-ERREURS.md#trk-050) | 🟢 **6 jours sans occasion** — aucun redémarrage d'API |
| [TRK-001](../REFERENCE-ERREURS.md#trk-001) | 🔵 1 ligne active (GLA•KC•31, 28/08) — action terrain |

---

## La seule chose à faire en premier

**Retrouver ce qui a débranché six véhicules le 31/08 au matin, et les remettre en service.**
Ils sont muets depuis 18 heures, la flotte a perdu 16 % de ses émetteurs, la coupe programmée
continue de leur envoyer des ordres que personne ne peut confirmer — et **aucune ligne du centre
d'alerte ne le dira demain**, parce que les six alertes qui l'ont annoncé ont été acquittées le
31/08 à 11 h 40.

*Vient ensuite, et ça coûte une minute : **provoquer TRK-021**. Dix-sept jours qu'un correctif en
production attend qu'on l'essaie.*

---

## 🧾 Table des tests en attente — recalculée à ce passage

| Fiche | Ce qu'il faut provoquer | Preuve attendue | En attente depuis |
|---|---|---|---|
| [TRK-021](../REFERENCE-ERREURS.md#trk-021) | une commande d'un template **SMS-only** (`shock_on`) | `channel = 'SMS'` | 🔴 **17 j** (15/08) |
| [TRK-022](../REFERENCE-ERREURS.md#trk-022) *(volet 2)* | qu'un **2ᵉ véhicule** produise une alerte de survitesse | ≥ 2 plaques distinctes en 30 j | 🔴 **19 j** (13/08) |
| [TRK-046](../REFERENCE-ERREURS.md#trk-046) *(filet)* | un véhicule qui **ressort en roulant** d'un parking validé, en plage de coupure | `OFF_SCHEDULE_MOVEMENT` CRITICAL | 🟠 **7 j** (26/08) |
| [TRK-050](../REFERENCE-ERREURS.md#trk-050) | un **redémarrage d'API** avec une session ouverte | la session **survit** | 🟠 **6 j** (26/08) |
| [TRK-051](../REFERENCE-ERREURS.md#trk-051) | **ouvrir** le mode fix d'un boîtier | badge **AMBRE** « CIBLE ATTEINTE (mesurée) » | 🟠 **6 j** (26/08) |
| [TRK-045](../REFERENCE-ERREURS.md#trk-045) | canari `,C,99s;` sur **GR-294-VW** *(mesuré à 2 s)* | douze écarts de **99 s** | 🔵 **posé ce jour** |
| [TRK-017](../REFERENCE-ERREURS.md#trk-017) | *(rien à provoquer)* — surveiller | **0** seconde ligne `sms-allowlist` sous 7 j | 🔵 **posé ce jour** |
| [TRK-052](../REFERENCE-ERREURS.md#trk-052) | *(après correctif)* un épisode long **acquitté en cours** | **1 seule** alerte, épisodes distincts inchangés | 🔵 **posé ce jour** |

⚠️ **Deux tests franchissent les 7 jours et l'un les dépasse de dix.** Selon la règle posée le
26/08, ils doivent être **provoqués ou requalifiés** — pas laissés attendre en silence, où ils
ressemblent à des réussites.
