# Audit du centre d'alerte — 2026-08-20

> Collecte : **2026-08-20 01:11 → 01:30 UTC** (tous les horodatages du rapport sont en UTC).

**Verdict :** Une journée à **au moins trois déploiements** a répondu à quatre tests datés et ouvert
cinq défauts : la prédiction d'hier sur [TRK-031](../REFERENCE-ERREURS.md#trk-031) s'est réalisée en
douze heures — **neuf épisodes refermés à la même seconde**, dont un de **35,18 jours réfuté par
5 027 positions**, et la médiane de la zone annonce désormais **16,03 j pour une absence réelle de
6,94 j** ; le déluge de `POWER_CUT` a cessé **parce que les trames ont cessé** (dernière `ac alarm`
le 19/08 à 02:26:23), pas parce qu'il a été corrigé ; **41 709 alertes et au moins 89 lignes
d'erreur ont été effacées hors de toute trace** ; et la garde qui remplace ces alertes éteint
aujourd'hui l'alarme d'alimentation de **33 boîtiers sur 42, toutes les nuits**.

---

## Chiffres

| | |
|---|---|
| Lignes `error_logs` | **14** (dont **14** sur 24 h, **14** sur 7 j — la table ne contient plus rien d'antérieur) |
| CRITICAL | **4** (toutes le 19/08, la sonde de tâche silencieuse) |
| Plus ancienne · plus récente | **19/08 09:35:00** · 20/08 00:32:01 |
| Trackers FAILING | **1** sur **42** (HD-779-MA) |
| Trackers hors ligne > 1 h | **4** (dont **0** jamais mis en service) |
| Commandes en attente > 10 min | **0** |
| Commandes échouées / jour (7 j) | **71,4** (500 `FAILED`, dont **494** `fix_continuous`) |
| Cibles de cadence sous le minimum matériel (20 s) | **0** sur 42 |
| Boîtiers à cadence dérivée | **0 ligne** |
| **Alertes en base** | **10 055** — contre **51 735** hier *(voir [TRK-035](../REFERENCE-ERREURS.md#trk-035))* |
| Alertes créées sur 24 h | **29** — 24 `POWER_CUT` (toutes **antérieures** au correctif de 01:56), 2 `OVERSPEED`, 1 `LOW_BATTERY`, 1 `GPS_LOST`, 1 `UNKNOWN` |
| Notifications sur 24 h | **145** — 112 supprimées, 22 groupées, **11 envoyées** |
| Positions insérées 24 h | **27 086** |
| Trajets créés 24 h | **1 311** — ⚠️ **non comparable** aux 207 d'hier : gonflé par un recalcul de masse |

**Cinq signatures nouvelles** : [TRK-032](../REFERENCE-ERREURS.md#trk-032),
[TRK-033](../REFERENCE-ERREURS.md#trk-033), [TRK-034](../REFERENCE-ERREURS.md#trk-034),
[TRK-035](../REFERENCE-ERREURS.md#trk-035), [TRK-036](../REFERENCE-ERREURS.md#trk-036).

---

## 🟢 Ce qui est en ligne — au moins trois déploiements le 19/08

| | Constat |
|---|---|
| Images `tracky-api` · `tracky-web` | **19/08 17:10:15** · **17:21:39** |
| Conteneurs recréés | **19/08 17:12:20** · **17:21:41**, `restarts=0` |
| Branche servie (`/opt/vizyo-tracky`) | **`main`** @ **`f71ddc3`** |
| Migrations appliquées le 19/08 | **4**, en **3 vagues** : `01:56:50`, `12:30:53` (×2), `17:12:23` |
| Commits du 19/08 | **une trentaine** — supervision, assistance IA, coûts IA, limites de vitesse OSM, alimentation |

**[TRK-019](../REFERENCE-ERREURS.md#trk-019) tient pour la 4ᵉ image consécutive** : servie depuis
`main`. ⚠️ **Le nombre réel de déploiements est ≥ 3 et non mesurable** : l'image porte le tag
`latest` et est réécrite à chaque fois, donc `docker images` n'en montre qu'une. Seules les
**migrations** laissent une trace horodatée, et elles ne marquent que les déploiements qui touchent
le schéma. *Trois vagues de migration prouvent trois déploiements ; elles ne prouvent pas qu'il n'y
en a pas eu six.*

**Fenêtre de journal réellement observée** *(§2 — jamais la fenêtre demandée)* : **5 955 lignes**, de
la bannière de démarrage du **19/08 17:12** à un `GET /api/health` du **20/08 01:28:32**, soit la
seconde de la collecte. La fenêtre couvre la vie entière du conteneur, sans perte de rotation — mais
elle ne fait que **8 h 16**. ⚠️ **Aucun compteur « sur 24 h » ne peut venir du journal aujourd'hui.**
Les deux compteurs qui en sortent sont écrits avec leur fenêtre : **261** rejets `stale_devicetime`
et **21** `ACK timeout`, **sur 8 h 16**.

---

## 🔴 À traiter

### TRK-032 🆕 — l'alarme d'alimentation de 33 boîtiers sur 42 est éteinte toutes les nuits

**Signature** — *(absence d'alerte)*
`alerts | AUCUNE LIGNE | POWER_CUT supprimé car "moteur coupé par nous" — sans borne de durée`

Le correctif du 19/08 (`10fe7a8` + migration `20260819030000_alimentation_sans_coupure`) fait taire
l'alarme `POWER_CUT` quand la coupure est **commandée par l'application**. L'intention est juste :
couper le moteur coupe l'alimentation, s'en alarmer est absurde. **C'est la façon de le savoir qui
est fausse.**

```js
const derniereCommande = await this.prisma.engineControlCommand.findFirst({
  where: { trackerId: tracker.id, status: { in: ['ACKNOWLEDGED', 'SENT'] } },
  orderBy: { createdAt: 'desc' },
  select: { action: true },
});
… analyserAlimentation(frame, { moteurCoupeParNous: derniereCommande?.action === 'CUT' })
```

**Cause racine.** La requête lit **la dernière commande moteur, sans aucune borne de temps** et sans
vérifier que la coupe est **encore en vigueur**. Tant qu'aucun `RESTORE` n'est venu se placer après
elle, un `CUT` vieux d'une heure, d'un jour ou d'un mois continue d'affirmer « c'est nous ».

**Ce que ça donne en production, mesuré à 01:1x ce matin :**

| Dernière commande du boîtier | Boîtiers | Effet sur l'alarme d'alimentation |
|---|---|---|
| **`CUT` / `ACKNOWLEDGED`** | **30** | 🔇 éteinte |
| **`CUT` / `SENT`** | **3** | 🔇 éteinte |
| `RESTORE` / `ACKNOWLEDGED` | 5 | ✅ active |
| `RESTORE` / `SENT` | 1 | ✅ active |

> 🔑 **33 boîtiers sur 42 — 79 % du parc — ont leur alarme d'alimentation éteinte au moment de la
> collecte.** Et ce n'est pas un accident : c'est **l'automatisation horaire** qui les a coupés, à
> 18:00 et 20:00 la veille, et qui les rétablira à 06:00. *La flotte entière est silenciée chaque
> nuit, exactement pendant les heures où un véhicule est garé sans surveillance — c'est-à-dire la
> seule fenêtre où une coupure d'alimentation réelle (vol, batterie débranchée, sabotage) est
> plausible.*

**Deux aggravants, tous deux déjà documentés ailleurs dans ce référentiel :**

1. **`SENT` suffit.** [TRK-014](../REFERENCE-ERREURS.md#trk-014) prouve qu'**aucun boîtier n'a
   jamais acquitté quoi que ce soit** : `ackedAt` = **0 sur 4 733**. Une commande `SENT` n'est pas
   une commande exécutée — c'est une commande **émise**, et
   [TRK-012](../REFERENCE-ERREURS.md#trk-012) montre qu'elle part au mauvais format. L'application
   éteint donc une alarme au motif d'une coupe qu'elle n'a pas la preuve d'avoir faite.
2. **Un `RESTORE` qui reste `SENT` ne débloque rien — et ça arrive.** Un boîtier porte aujourd'hui
   un `RESTORE / SENT` du **27/07, soit 559 heures**. Si la même chose arrivait à un `CUT` resté en
   tête, ce véhicule serait silencié **définitivement**, sans que rien ne le signale.

**Correctif proposé** — trois gestes, du plus urgent au plus propre :

1. **Borner dans le temps.** N'accepter le motif « coupure commandée » que si le `CUT` est récent —
   quelques minutes suffisent : la chute d'alimentation suit la commande de très près. Au-delà,
   l'alarme redevient légitime, parce qu'un véhicule coupé depuis six heures qui perd son
   alimentation ne la perd plus à cause de la coupe.
2. **Lire l'état, pas l'historique.** La bonne question n'est pas « quelle est la dernière commande
   enregistrée ? » mais « le moteur est-il coupé **maintenant** ? ». Si un état de coupe est tenu
   quelque part, c'est lui qu'il faut lire ; sinon, l'introduire vaut mieux que de déduire un état
   d'une pile de commandes non acquittées.
3. **Exclure `SENT`.** Tant que [TRK-014](../REFERENCE-ERREURS.md#trk-014) tient, `SENT` ne prouve
   rien. Restreindre à `ACKNOWLEDGED` réduit le silence aux seuls cas prouvés. *(Ce geste seul ne
   suffit pas : 30 des 33 boîtiers silenciés sont en `ACKNOWLEDGED`.)*

**Vérification (porte sur la cause, pas sur l'affichage).** Rejouer la requête du tableau ci-dessus
**en pleine nuit**, entre 20:00 et 06:00 : le nombre de boîtiers dont la dernière commande est un
`CUT` doit être **indépendant** du nombre de boîtiers dont l'alarme est éteinte. Aujourd'hui les
deux sont égaux à 33. Et **ne pas** valider en constatant qu'il n'y a plus de fausses `POWER_CUT` :
il n'y en a plus parce que les trames ont cessé (ci-dessous), pas grâce à cette garde.

**⛔ Ce qu'il ne faut PAS faire.** Ne pas supprimer la garde : elle traite un vrai problème
(202 fausses alertes en une nuit le 19/08). Le défaut est sa **portée**, pas son principe.

**Gravité 1.** C'est le seul défaut du jour qui **retire une alarme de sécurité** à la quasi-totalité
du parc, et il le fait pendant la fenêtre où elle sert.

---

### TRK-036 🆕 — l'accusé de réception du boîtier arrive, est stocké par Tracky, et n'est jamais rattaché

**Signature** — *(défaut de chaînage, pas une ligne d'erreur)*
`sms_logs | AUCUNE LIGNE | SMS entrant "…engine Succeed" enregistré avec imei NULL, jamais rapproché de la commande`

[TRK-018](../REFERENCE-ERREURS.md#trk-018) dit depuis le 11/08 que le repli SMS n'a **aucune preuve
de remise**. Le 19/08 a produit la preuve — **et elle était déjà dans la base de Tracky.**

La chaîne complète, rapprochée sur le **numéro de SIM** :

| Heure (UTC) | Où | Fait |
|---|---|---|
| 04:39:13.553 | Tracky | Commande `RESTORE` créée pour **GS-014-NY** |
| 04:39:13.668 | Passerelle | `resume123456` empilé vers **+345901030609501** |
| 04:39:14.040 | Tracky | Commande passée en **`SENT`** |
| **08:28:58.320** | Passerelle | 🔔 **SMS entrant de +345901030609501 : « Resume engine Succeed »** |
| **08:28:58.558** | **Tracky** | 🔔 **Le même SMS est écrit dans `sms_logs`** — `imei` **NULL**, `context` **NULL**, `template` **NULL** |
| **maintenant (+21 h)** | Tracky | La commande est **toujours `SENT`** |

Et `trackers.simPhoneNumber` pour GS-014-NY vaut **exactement `+345901030609501`**. Le boîtier est
identifiable **par une seule jointure**, sur une colonne qui existe déjà.

> 🔑 **Le défaut n'est pas que la preuve manque — c'est qu'elle est arrivée, a été rangée, et que
> personne n'est allé la chercher.** Le repli SMS n'est pas aveugle : il est **sourd d'une oreille**.
> Tracky reçoit la phrase « Resume engine Succeed » et la classe comme un SMS entrant anonyme, à
> côté d'un « Binjour, je vois avec eux demain ! » et d'un démarchage commercial.

**Ce n'est pas un cas isolé mais un cas rare** — et c'est la nuance qui compte. `messages` (côté
passerelle) ne porte que **2** accusés de ce type : « Stop engine Succeed » le **13/07** et « Resume
engine Succeed » le **19/08**. Deux en cinq semaines, pour **1 602 commandes moteur `FAILED`** et
**280 `SENT`**. *Le rattachement manquant explique pourquoi on ne les voit pas ; il n'explique pas
pourquoi il n'y en a que deux.* Les deux questions sont distinctes et doivent le rester.

**Correctif proposé :**

1. **À la réception d'un SMS entrant, résoudre l'émetteur** : `trackers.simPhoneNumber = fromNumber`
   → renseigner `sms_logs.imei`. Geste isolé, sans effet de bord, utile même sans la suite.
2. **Reconnaître les accusés** : le corps suit un gabarit fixe (`Stop engine Succeed`,
   `Resume engine Succeed`). Les rapprocher de la dernière `engine_control_commands` `SENT` du même
   boîtier et écrire l'acquittement.
3. **Ne pas rapprocher à l'aveugle sur le temps seul** : 3 h 50 séparent ici la commande de sa
   réponse. Une fenêtre trop large rattacherait un accusé à la mauvaise commande ; le couple
   (boîtier, action) est le bon discriminant, pas la proximité horaire.

**Vérification.** Rejouer la requête du tableau : tout `sms_logs` de direction `IN` dont le
`fromNumber` correspond à un `simPhoneNumber` connu doit porter un `imei`. **Aujourd'hui : 0 sur 1.**
Et **pas** « la commande est passée en ACKNOWLEDGED » : ce serait vrai aussi si on acquittait à
l'aveugle.

**Gravité 2.** Rien n'est en panne. Mais le coupe-circuit est la fonction la plus sensible du
produit, et la seule preuve de bon fonctionnement qu'il ait jamais reçue a été jetée.

---

### TRK-035 🆕 — 41 709 alertes et au moins 89 lignes d'erreur effacées, sans aucune trace

**Signature** — *(défaut de plateforme, pas une ligne d'erreur)*
`plateforme | AUCUNE LIGNE | suppression de masse hors application — invisible de tous les journaux`

Le centre d'alerte a perdu son histoire entre deux passages de l'audit :

| | 19/08 (mesuré à 01:32) | **20/08** |
|---|---|---|
| Lignes `error_logs` | **50**, la plus ancienne du **28/07** | **14**, la plus ancienne du **19/08 09:35:00** |
| Lignes `alerts` | **51 735** | **10 055** |
| Dont `POWER_CUT` | **41 733** | **24** |

**Ce n'est ni la rétention, ni un chemin de l'application.** Les trois vérifications :

1. **La rétention n'a rien supprimé.** Le journal du cron du 19/08 03:00:20 porte son propre
   compte-rendu : `{"errorDeleted": 0, "sysActivityDeleted": 0, "wireDeleted": 163301}`. Seuls les
   journaux de trame ont été purgés — et `ERROR_LOGS_RETENTION_DAYS` vaut bien **90** dans
   l'environnement du conteneur, ce qui laissait le 28/07 très largement dans la fenêtre.
2. **Aucun code applicatif ne peut le faire.** Une seule `errorLog.deleteMany` existe dans toute
   l'image servie (`log-cleanup.service.js`), et **aucune** `alert.deleteMany` — nulle part. Le
   bouton « tout acquitter » (`POST /api/alerts/acknowledge-all`, appelé 3 fois le 19/08) fait un
   `updateMany` sur `acknowledgedAt` : **il ne supprime rien**, vérifié dans le service.
3. **La migration ne le fait pas non plus.** `20260819030000_alimentation_sans_coupure` a été lue
   intégralement : elle ajoute deux colonnes à `trackers`, un point.

**La seule trace de l'événement est indirecte** : `pg_stat_user_tables` compte **41 824**
suppressions cumulées sur `alerts`, et l'autovacuum de `error_logs` s'est déclenché le **19/08 à
09:35:15** — quinze secondes après l'horodatage de la plus ancienne ligne survivante. La suppression
a donc eu lieu **le 19/08 vers 09:35 UTC**, et elle vient **de l'extérieur de l'application**.

Le volume effacé de `error_logs` est minoré : aux **50** lignes mesurées la veille s'ajoutent les
**39** comptées par le courriel de synthèse du 19/08 04:10:00 (« 39 erreurs en 1 h »), toutes
disparues. **Au moins 89 lignes**, sans compter les tranches non mesurées.

> 🔑 **[TRK-010](../REFERENCE-ERREURS.md#trk-010) avait fermé la mauvaise porte.** La fiche du 03/08
> a identifié la rétention automatique comme le moyen par lequel une erreur non corrigée s'efface, et
> l'a bouché en portant la fenêtre à 90 jours. La porte réellement empruntée le 19/08 est une autre :
> **le SQL direct sur la base de production**, qui ne passe ni par la rétention, ni par
> l'application, ni par le journal des mutations. *Un garde-fou posé dans le code ne protège que ce
> qui passe par le code.*

**Ce que ça coûte.** Deux choses, et la seconde est la plus chère :

- Les **taux** de ce référentiel deviennent illisibles sans avertissement.
  [TRK-023](../REFERENCE-ERREURS.md#trk-023) affichait hier **42 240 / 51 735 = 81,6 %** d'alertes
  sans message ; il afficherait aujourd'hui **556 / 10 055 = 5,5 %**. **Cette chute de 76 points ne
  doit rien à un correctif** — elle vient de la disparition du dénominateur. Reporter ce chiffre
  comme un progrès serait la plus grosse erreur de lecture possible de tout le dossier.
- La consigne explicite du propriétaire — *« une erreur reste visible tant qu'elle n'est pas corrigée
  ET vérifiée »* — est contournable sans laisser de trace, y compris par mégarde.

**Correctif proposé.** Il n'y en a pas côté code, et c'est le fond du sujet : **aucun garde-fou
applicatif ne peut arrêter un `DELETE` en base.** Ce qui est possible :

1. **Rendre la disparition visible plutôt que l'empêcher.** Un relevé quotidien de `count(*)` et
   `min("createdAt")` sur `error_logs` et `alerts`, conservé ailleurs que dans ces tables, transforme
   un effacement muet en fait daté. Le journal des passages de ce dossier fait déjà exactement cela,
   et **c'est lui qui a détecté celui-ci** — mais un jour plus tard.
2. **Documenter la manœuvre quand elle est volontaire.** Si un nettoyage est décidé, une ligne dans
   `system_activity_logs` (catégorie `RETENTION`) coûte une insertion et rend l'événement lisible
   depuis l'écran, comme le fait déjà le cron.

**Vérification.** Au prochain passage, comparer `min("createdAt")` de `error_logs` à celui d'hier :
il ne doit jamais **avancer** sans qu'une purge soit inscrite quelque part. **Aujourd'hui, il a
avancé de 22 jours sans aucune inscription.**

**Gravité 1** sur le plan de la consigne — c'est le garde-fou nº 1 du dossier — et **gravité 3** sur
le plan technique : rien n'est en panne, aucune donnée d'exploitation n'est perdue.

*Note d'honnêteté : le 19/08 a vu au moins trois déploiements et une trentaine de commits, dont
plusieurs sur le bruit du centre d'alerte. Un nettoyage délibéré pendant cette passe est l'hypothèse
la plus simple. **L'audit ne l'affirme pas** : il constate que ni l'application, ni la rétention, ni
aucun journal n'en portent trace, et c'est ce constat-là qui est le sujet de la fiche.*

---

### TRK-033 🆕 — l'écran `/admin/vps` plante sur un champ que son propre manifeste ne contient pas

**Signature** — `frontend | ERROR | [uncaught] TypeError: undefined is not an object (evaluating
'e.chargeDeFond.<CHAMP>')`

Une ligne, le 19/08 à 10:32:05, sur `/admin/vps`, iPhone Safari, compte `admin@vizyoagency.com`.
**Elle est reproductible depuis la donnée, et le défaut est en ligne à l'instant où ceci est écrit.**

**Cause racine.** Le gabarit ouvre une garde sur l'objet parent puis déréférence l'enfant sans garde :

```
@if (idx.previsions; as p) {          ← garde sur previsions
  … {{ p.chargeDeFond.healthchecksParMinute }}   ← chargeDeFond n'est pas gardé
```

Or `previsions` vient de `docs/vps-audit/app/wiki.json`, **écrit par l'agent d'audit VPS**, et ce
fichier ne contient que deux clés : `disque` et `recuperable`. **`chargeDeFond` est absent.**
Vérifié aux trois endroits : dans le dépôt, dans `/opt/tracky-vps-audit/app/wiki.json` publié sur le
VPS, et tel que le conteneur `tracky-api` le voit.

> 🔑 **L'interface TypeScript déclare `chargeDeFond` obligatoire, et le compilateur est satisfait —
> mais la valeur vient d'un JSON analysé sans validation.** Un type posé sur du JSON non validé n'est
> pas une garantie, c'est une **promesse que le compilateur n'a aucun moyen de tenir**. Ici la
> promesse est faite au gabarit, qui y croit et déréférence.

**Ce que ça casse.** Pas seulement la phrase sur la charge de fond : le `@if` entier meurt, donc
**toute la carte « Prévisions » disparaît** — y compris le tableau du disque et le total récupérable,
qui n'ont rien à voir avec le champ manquant.

**Correctif proposé** — deux gestes, et il faut **les deux** :

1. **Côté écran, rendre le champ facultatif** : `chargeDeFond?` dans l'interface, `@if
   (p.chargeDeFond; as cf)` autour du seul bloc concerné. Le reste de la carte survit alors à
   n'importe quel manifeste partiel.
2. **Côté agent VPS, renseigner `chargeDeFond`** — les chiffres existent déjà (l'audit VPS mesure les
   healthchecks et les processus par minute). *Ce second geste seul ne suffit pas : il répare la
   donnée du jour, pas la fragilité du gabarit, et le prochain champ ajouté rejouera la même panne.*

**Vérification.** Ouvrir `/admin/vps` avec un `wiki.json` **volontairement amputé** de
`chargeDeFond` : la carte « Prévisions » doit s'afficher, sans la phrase de charge de fond, et la
console doit rester muette. **Ne pas** valider en ajoutant le champ puis en constatant que la page
marche : ça ne teste rien du défaut.

**Gravité 2.** Aucun impact sur les données ni sur la flotte, mais l'écran est cassé **maintenant**
pour tout le monde, et il fait partie de l'outillage d'exploitation.

---

### TRK-034 🆕 — la fenêtre de recalcul des trajets dépasse de 2,5 jours la rétention des positions

**Signature** — `TRIP_AUTOMATION | ERROR | Recalcul impossible sur <PLAQUE> : aucune position entre
<DATE> et <DATE>, alors que des trajets bruts y subsistent. Rien n'a été supprimé.`

Une ligne, le 20/08 à 00:32:01, sur FZ-862-VY, pour la fenêtre **19/06 → 21/06**.

**Cause racine — deux rétentions qui ne se parlent pas :**

| Réglage | Valeur | Portée |
|---|---|---|
| `trip_automation_settings.lookbackHours` | **1 500 h** = **62,5 jours** | jusqu'où le recalcul remonte |
| `POSITIONS_RETENTION_DAYS` | **60 jours** | jusqu'où les positions existent |
| rétention des trajets | **12 mois** | jusqu'où les trajets existent |

L'automatisation va donc chercher des positions **2,5 jours au-delà de l'horizon où il peut y en
avoir**, sur des trajets qui, eux, sont conservés vingt fois plus longtemps. Le 19/06 est à
**62 jours** de la collecte : dans la fenêtre de recalcul, hors de la rétention des positions. Et la
purge du 19/08 03:30 a justement retiré **22 674 positions** « > 60 j ».

> 🔑 **Ce n'est pas un aléa, c'est une soustraction.** Tant que `lookbackHours` dépasse
> `POSITIONS_RETENTION_DAYS`, la bande de recouvrement produit la même erreur **à chaque passage, pour
> toujours**, et elle s'élargira si l'un des deux réglages bouge sans l'autre.

**À porter au crédit du message.** Il est explicite, nomme le véhicule, donne les deux bornes et se
termine par *« Rien n'a été supprimé »*. Ce n'est ni un message circulaire ni un message menteur au
sens du §7 : il décrit exactement ce qui s'est passé. **Le défaut est le réglage, pas le cri.**

**Correctif proposé :**

1. **Borner la fenêtre de recalcul par la rétention des positions**, à la lecture du réglage plutôt
   qu'en dur : `min(lookbackHours, POSITIONS_RETENTION_DAYS × 24)`, avec une marge d'un jour pour ne
   pas courir après la purge nocturne.
2. **Classer ce cas en information, pas en erreur.** Une fenêtre sans position parce que la rétention
   est passée est un état **attendu** ; l'écrire en `ERROR` au centre d'alerte consomme l'attention
   qui doit aller ailleurs. *(Corollaire du §7 : « décrit un refus voulu → faux positif, ne doit pas
   être archivé ».)*

**Vérification.** Après correction, relancer l'automatisation et vérifier qu'**aucune** ligne
`TRIP_AUTOMATION` n'est écrite alors que des trajets antérieurs à 60 jours subsistent en base — il
en subsiste, la rétention des trajets étant de 12 mois. **Ne pas** vérifier en constatant la
disparition de la ligne le lendemain : elle ne réapparaît que quand un véhicule concerné entre dans
la bande.

**Gravité 3.** Aucune perte : le message le dit lui-même. C'est du bruit, mais du bruit **structurel
et permanent**.

---

### TRK-017 — 10ᵉ jour, et l'appelant s'est tu depuis 54 heures

| | 18/08 | 19/08 | **20/08** |
|---|---|---|---|
| Effacements de masse bloqués (cumul) | 49 | 49 | **49** *(inchangé)* |
| Appels normaux (cumul) | 178 | 208 | **234** |
| Préfixe de clé (`tenants.apiKeyPrefix`) | `vtx_48fe` | `vtx_48fe` | **`vtx_48fe`** |
| `tenants.updatedAt` | 05/06 | 05/06 | **05/06** |
| Jours depuis que le geste est écrit | 8 | 9 | **10** |

Dernière frappe : **17/08 19:24:59** — **54 h de silence**, la plus longue de la série. Les appels
normaux continuent : le dernier à **20/08 01:25:00**.

⚠️ **Pour la septième fois, ce silence n'est pas une révocation.** La clé est mesurée vivante :
`vtx_48fe`, `updatedAt` au 05/06. L'appelant est déjà revenu **trois fois** après des silences de 24
à 32 h ; 54 h est plus long, mais 54 h de silence et une clé valide ne font pas une clé fermée.
*Un compteur de frappes mesure l'activité de l'appelant, jamais son accès.*

**Condition d'échec du test daté : NON déclenchée.** `allowlist_entries` compte **44** entrées
(inchangé), la plus récente du 19/08 01:04:03. Aucune suppression de masse depuis le **10/08 14:25**,
soit **231 h**. La garde tient.

**L'action reste humaine et inchangée : révoquer et rotationner la clé d'API de la passerelle SMS,
et identifier l'instance hors serveur qui la porte.** Dixième jour, priorité nº 1 pour la dixième
fois.

---

## 🎯 Les tests datés d'hier — ce qu'ils ont répondu

### Nº 2 — TRK-031 : **CONFIRMÉ, à l'échelle exacte annoncée**

Le test écrit hier disait : *« FS-253-HR (9 épisodes ouverts) est un candidat. Au retour, compter les
épisodes refermés à la même seconde : s'il y en a plus d'un, la fiche est confirmée une seconde
fois. »*

**FS-253-HR est revenu le 19/08 à 13:48:56. Neuf épisodes ont été refermés à cette seconde-là.**

| Perte | Retour écrit | Durée déclarée | Réalité |
|---|---|---|---|
| **15/07 09:31:02** | 19/08 13:48:56 | 🔴 **35,18 j** | ❌ |
| 22/07 13:35:53 | 19/08 13:48:56 | 🔴 28,01 j | ❌ |
| 28/07 14:38:16 | 19/08 13:48:56 | 🔴 21,97 j | ❌ |
| 29/07 14:39:11 | 19/08 13:48:56 | 🔴 20,97 j | ❌ |
| 03/08 13:02:53 | 19/08 13:48:56 | 🔴 16,03 j | ❌ |
| 07/08 13:50:49 | 19/08 13:48:56 | 🔴 12,00 j | ❌ |
| 10/08 07:00:11 | 19/08 13:48:56 | 🔴 9,28 j | ❌ |
| 10/08 15:03:19 | 19/08 13:48:56 | 🔴 8,95 j | ❌ |
| **12/08 15:20:59** | 19/08 13:48:56 | **6,94 j** | ✅ **la seule vraie** |

**La réfutation est arithmétique** : pendant les « 35,18 jours » du premier épisode, le boîtier
`864035054757027` a émis **5 027 positions**. Il n'a pas été absent 35 jours.

**Et le dégât annoncé s'est matérialisé, sur l'écran même que le correctif devait protéger :**

| Zone | Épisodes clos | Médiane affichée | Absence réelle la plus longue |
|---|---|---|---|
| `79a8d0f2` — parking souterrain de FS-253-HR | **9** | 🔴 **16,03 j** | **6,94 j** |

[TRK-028](../REFERENCE-ERREURS.md#trk-028) a été écrite pour cesser de dire à l'exploitant « il
réapparaîtra en sortant » sans jamais dire quand. **Elle lui répond aujourd'hui « environ seize
jours » pour un parking dont le véhicule ressort en une semaine.** La fiche
[TRK-031](../REFERENCE-ERREURS.md#trk-031) passe de « prédite » à **« mesurée »**, et son ordre de
correction est confirmé : **assainir le stock d'abord, borner le `updateMany` ensuite.**

**Dette restante : 20 épisodes ouverts sur 9 véhicules**, dont **KSR370 avec 9** (le plus ancien du
15/07) et **FZ-862-VY avec 4**. KSR370 est mort depuis le 14/08 : le jour où son boîtier revient, il
écrira neuf absences se terminant à la même seconde, dont une de plus d'un mois.

### Nº 3 — la qualification automatique d'un parking : **EXERCÉE pour la première fois** 🟢

Le test disait : *« La zone neuve `55615ba3` (FL-787-KV) porte 1 occurrence : sa 2ᵉ perte au même
endroit sera le vrai test. »*

| Zone | Véhicule | Statut | Label | Occurrences | `reviewedAt` |
|---|---|---|---|---|---|
| **`55615ba3`** | **FL-787-KV** | `CONFIRMED_BENIGN` | `UNDERGROUND_PARKING` | **2** | 🟢 **NULL** |
| `79a8d0f2` | FS-253-HR | `CONFIRMED_BENIGN` | `UNDERGROUND_PARKING` | 10 | 15/07 *(manuel)* |
| `879699a3` | FZ-862-VY | `CONFIRMED_BENIGN` | `UNDERGROUND_PARKING` | 4 | 03/08 *(manuel)* |
| `44cfb9f4` | KSR370 | `CONFIRMED_BENIGN` | `UNDERGROUND_PARKING` | 2 | 17/07 *(manuel)* |

**`reviewedAt` est NULL et le label est posé** : personne n'est passé, la machine a qualifié seule, le
**19/08 à 21:55:15**, à la 2ᵉ occurrence. C'est la première fois depuis la création de la fiche.

⚠️ **Une réserve à ne pas effacer** : la zone `8fb73000` (KSR370) porte **3** occurrences et reste
`RECURRING` / `UNKNOWN`, jamais promue. Son boîtier est mort depuis le 14/08, elle n'a donc pas eu
d'occasion depuis que la règle est en ligne — mais **la règle n'est donc pas « 2 occurrences »**, et
un seul cas ne suffit pas à écrire laquelle c'est. *Un mécanisme exercé une fois est exercé, pas
caractérisé.*

### Nº 4 — `POWER_CUT` : **les trames ont cessé**, et c'est la seule raison

La question posée hier était terrain : *« une rafale isolée est un incident électrique ponctuel ; une
rafale quotidienne est un câblage ou une batterie. »* La réponse vient des trames brutes, mesurées
dans `wire_logs` **indépendamment de toute logique d'alerte** :

| Boîtier | Trames `ac alarm` | Première | **Dernière** |
|---|---|---|---|
| **DZ-034-CA** | **304** | 18/08 07:56:36 | **19/08 02:26:23** |
| **HD-292-SH** | **49** | 19/08 00:32:34 | 19/08 00:49:54 |

**Aucune trame `ac alarm` depuis le 19/08 02:26:23 — soit 23 heures**, sur une fenêtre `wire_logs` qui
couvre pourtant **quatre jours** (16/08 03:00 → 20/08 01:29) et **677 399 trames**. Le capteur est
vivant et il ne compte rien : **l'épisode électrique est terminé.**

> 🔑 **Trois conséquences, et il faut les tenir ensemble :**
> 1. La chute de 202 alertes/jour à 0 **ne prouve pas que le correctif marche** — elle prouve que la
>    source s'est tarie 30 minutes après son déploiement.
> 2. La déduplication à **6 h** livrée le 19/08 (`DEDUP_ALARME_MS`) — la réponse à
>    [TRK-022](../REFERENCE-ERREURS.md#trk-022) — n'a eu **aucune occasion** de s'exercer sur
>    `POWER_CUT`. Elle est en ligne, elle n'est pas vérifiée.
> 3. Le message enfin porté par les `POWER_CUT` (`messageCoupure`) — la réponse à
>    [TRK-023](../REFERENCE-ERREURS.md#trk-023) — n'a **aucune alerte** sur laquelle s'être écrit :
>    les 24 `POWER_CUT` des 24 h sont **toutes antérieures** à 01:56.
>
> *Les deux correctifs sont écrits, déployés, et non exercés. Les déclarer bons aujourd'hui serait
> lire un silence comme une réussite.*

**Le seul passage réellement exercé est celui de la mise en sourdine**, et il l'a été **une fois** :
`trackers.lastPowerNoticeAt` porte une valeur unique, **19/08 02:26:23.780**, sur DZ-034-CA — **25
millisecondes après la toute dernière trame `ac alarm` reçue**. La dernière trame de l'épisode est
donc exactement celle qui a été absorbée. Le mécanisme fonctionne sur cet unique exemplaire.

⚠️ **Et l'instrument choisi ne pourra jamais dire combien.** Le commentaire de la migration dit :
*« on cesse d'alerter, mais on GARDE la trace ici — sinon on remplacerait 202 fausses alertes par un
silence tout aussi trompeur. »* Or `lastPowerNoticeAt` est **une colonne « dernière valeur » sur le
boîtier**, écrasée à chaque fois : 42 boîtiers, donc **au plus 42 lignes**, chacune ne retenant que
la plus récente. *La trace posée pour éviter le silence est structurellement incapable de compter —
elle dit quand, jamais combien.* Un journal (une ligne par notice) serait la même quantité de code.

### Nº 5 — FS-253-HR : **revenu**, après **6,94 jours**

Retour du fix le **19/08 à 13:48:56**, soit 6,94 j après la perte du 12/08 15:20:59 — **sous le seuil
des 7 jours** que [TRK-027](../REFERENCE-ERREURS.md#trk-027) prescrivait de surveiller à la main. Le
contrôle terrain demandé depuis le 03/08 **n'a plus d'objet sur ce véhicule** : le boîtier fonctionne,
c'est bien le parking qui masquait le ciel. Il a d'ailleurs reperdu son fix à **15:05:02** le même
jour — il est reparti se garer.

**Réserve** : ce retour est ce qui a déclenché la fabrication des 9 durées de TRK-031. *La bonne
nouvelle du terrain est la mauvaise nouvelle de la donnée.*

### Nº 1 — TRK-030 : **non échu.** Aucune mise en service depuis le 19/08 01:04 ; le parc reste à **42** boîtiers. Le test attend le prochain boîtier posé.

### Nº 4 (TRK-026) — **non échu.** Cron hebdomadaire, premier verdict réel **lundi 24/08**, inchangé.

---

## 🟢 Une sonde neuve qui a fait son travail dès le premier jour

Les **4 lignes `CRITICAL`** du centre d'alerte viennent toutes de la même sonde, déployée le 19/08 au
matin (`aa2cf89`, *« la sonde des tâches silencieuses était elle-même invisible »*) :

> *Tâche planifiée à l'arrêt : « Automatisation des trajets » est ACTIVÉE mais ne tourne plus —
> dernier passage il y a <DURÉE> (cadence configurée : toutes les heures, seuil d'alerte 4 h).*

| Heure | Silence annoncé |
|---|---|
| 09:35:00 | 5 h |
| 10:35:00 | 6 h |
| 11:35:00 | 7 h |
| **12:35:00** | **8 h** — puis plus rien |

**Le diagnostic était exact et l'extinction aussi.** L'automatisation a bien tourné à 04:10 (passage
manuel), puis **plus rien jusqu'à 13:07** — 9 heures de trou, sur une tâche horaire. Elle a repris à
13:07:26, et la sonde s'est tue **au premier passage réussi**, sans qu'on la touche.

C'est le comportement attendu d'un détecteur : il crie tant que le fait dure, il se tait quand il
cesse. **À conserver comme ligne de base** : première alerte à 5 h de silence pour un seuil de 4 h —
l'écart s'explique par le déploiement de la sonde elle-même vers 09:30.

⚠️ **Un fait à surveiller, pas encore une tendance.** Les passages de l'automatisation ne sont plus
horaires : 13:07, 14:18, 16:37, 18:35, 20:38, 22:35, 00:35 — soit **environ toutes les 2 h**. La
cause est lisible dans le réglage : le dernier passage a duré **3 011 040 ms, soit 50 min 11 s**,
exactement le budget porté « de 20 à 50 min » par le commit `574011c`. Une tâche horaire qui occupe
50 minutes ne peut pas repartir à l'heure. **Le seuil d'alerte de 4 h ne s'en émeut pas — c'est une
marge, pas un oubli.** Deux points ne font pas une tendance (§4 bis) ; il en faut un troisième.

---

## 🟠 Connu, correctif en attente — l'état mesuré ce jour

| Fiche | Mesure du jour | Lecture |
|---|---|---|
| [TRK-012](../REFERENCE-ERREURS.md#trk-012) | **284** échecs « intervalle observé 20 s » sur 7 j (289 hier, même définition). **21 `ACK timeout`** dans le journal, ⚠️ **sur 8 h 16**. | Correctif écrit depuis le 11/08 : **10ᵉ jour d'attente d'accord**. Le compteur des `ACK timeout` n'est **pas** comparable à hier : fenêtre différente. |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) | Silences tracés 243 → **651**. Commandes du jour portant l'indice : **74/75**. `ackedAt` = **0 sur 4 733**. | **6ᵉ point** : 0/285 · 18/70 · 32/32 · 83/83 · 71/71 · **74/75**. ⚠️ **Et TRK-014 est désormais un aggravant de [TRK-032](../REFERENCE-ERREURS.md#trk-032)** : c'est parce qu'aucune commande n'est acquittée que « le moteur est coupé par nous » repose sur du vide. |
| [TRK-018](../REFERENCE-ERREURS.md#trk-018) | Moteur `SENT` > 24 h : **274** (254 · 257 · 261 · 270 · **274**). Passerelle : **343** `queued`. `ACKNOWLEDGED` 2 790 → **2 857**. | **Test daté passé pour la 9ᵉ fois** : strictement croissant. 🆕 **Et sa nature change** : [TRK-036](../REFERENCE-ERREURS.md#trk-036) montre que l'accusé existe, arrive et est jeté. |
| [TRK-013](../REFERENCE-ERREURS.md#trk-013) | Clôtures « sans effet » 72 → **81**. Clôtures **strictement fausses** (cadence réelle = cible) : 13 → **15**. | Les deux repartent après trois jours de pause. Série des fausses : 7·7·7·8·11·11·13·13·13·**15**. |
| [TRK-016](../REFERENCE-ERREURS.md#trk-016) | **89,3 %** de trajets sans recalage sur **1 311** trajets. Cumul **11 388 / 13 728 = 82,9 %**. | ⚠️ **Chiffre NON comparable** : les 1 311 « trajets du jour » sont majoritairement des **recalculs de masse**, pas des trajets roulés (le total en base a *baissé* de 14 113 à 13 728). Écrit pour ce qu'il est. |
| [TRK-023](../REFERENCE-ERREURS.md#trk-023) | **556 / 10 055 = 5,5 %** d'alertes sans message. | 🔴 **NE PAS LIRE COMME UN PROGRÈS.** Le taux passe de 81,6 % à 5,5 % **parce que le dénominateur a été effacé** ([TRK-035](../REFERENCE-ERREURS.md#trk-035)), pas parce qu'un correctif a agi. Le correctif existe et n'est **pas exercé** (aucune `POWER_CUT` depuis 01:56). |
| [TRK-022](../REFERENCE-ERREURS.md#trk-022) | Déduplication **livrée** (`DEDUP_ALARME_MS` = 6 h, tous types d'alarme). **0** survitesse et **0** rafale sur 24 h. | 🟠 **CORRECTIF DÉPLOYÉ, NON EXERCÉ.** Aucune rafale depuis sa mise en ligne : rien à dédupliquer, donc rien de prouvé. |
| [TRK-024](../REFERENCE-ERREURS.md#trk-024) | Définition écrite (5 min / 15 min) : mesure du jour reportée — **1** boîtier `ONLINE` en `FAILING` (HD-779-MA, vu à 01:10:20). | Le parc compte **4** hors ligne > 1 h, **0** jamais mis en service. Statut inchangé. |
| [TRK-020](../REFERENCE-ERREURS.md#trk-020) | `ackedAt` = **0**, `acknowledgedAt` = **30** (inchangé). Total commandes 4 656 → **4 733**. | Inoffensif, toujours prêt à mentir. |
| [TRK-015](../REFERENCE-ERREURS.md#trk-015) | **261** trames rejetées `stale_devicetime` — ⚠️ **sur la fenêtre de journal de 8 h 16**. Positions insérées 24 h : **27 086** (base). | **Chiffre NON comparable** aux 632 d'hier ni aux 3 191 de l'avant-veille : trois fenêtres différentes. |
| [TRK-021](../REFERENCE-ERREURS.md#trk-021) | **3 `shock_on` + 3 `sensitivity`** `FAILED` sur 7 j (4+4 hier — la fenêtre glissante a fait sortir une occurrence de chaque). | **Test daté RÉPONDU : non** pour la 4ᵉ fois — aucun nouvel essai. Gravité 1 maintenue. |
| [TRK-025](../REFERENCE-ERREURS.md#trk-025) | Blocages **49 — inchangés** depuis le 17/08 19:24. Lignes `sms-allowlist` : **0**. | **Non exercé** pour le 2ᵉ jour : sans nouvelle tentative retenue, le canal de remontée n'a pas d'occasion de se taire. |
| [TRK-001](../REFERENCE-ERREURS.md#trk-001) | **1 ligne** sur 24 h : FL-787-KV, 3 h sans fix, le 19/08 13:00:15. **Aucun rappel.** | Le correctif de la fenêtre hebdomadaire **tient pour le 3ᵉ jour**. La ligne est légitime : la zone n'était pas encore qualifiée à cet instant (elle l'a été à 21:55). |
| [TRK-019](../REFERENCE-ERREURS.md#trk-019) | **`main`** @ `f71ddc3`, 4ᵉ image consécutive conforme. | 🟢 tient. ⚠️ réserve inchangée : le tag `latest` empêche de compter les déploiements. |

---

## Angles morts examinés

| Section | Constat | Verdict |
|---|---|---|
| `trackers_failing` | **1** sur 42 — HD-779-MA, cible 30 s, réel 10 s, `ONLINE` | ⚠️ 0 → 1, à re-mesurer demain |
| `commandes_en_attente` | **0 ligne** | ✅ |
| `cadence_resume` | **0** cible sous 20 s ; la plus basse = 20 s ; **42** boîtiers | ✅ tenu depuis le 04/08 |
| `cadence_derive` | **0 ligne** | ✅ |
| `commandes_sante_7j` | 494 `fix_continuous` + 3 `shock_on` + 3 `sensitivity`, **toutes `FAILED`**, **0 acquittée** | 🔴 71,4/jour, **16ᵉ jour de métronome** |
| `commandes_motifs_7j` | 15 motifs, dominés par « intervalle observé 20 s » (**284**) | 🔴 TRK-012 |
| `gps_sans_fix` | **2** boîtiers : FZ-862-VY **60,8 h**, FS-253-HR **10,1 h** *(reperdu à 15:05 après son retour de 13:48)* | 🔵 terrain — silencieux par conception |
| **Parc de boîtiers** | **42**, inchangé. Aucune mise en service depuis le 19/08 01:04 | ✅ |
| **Table `alerts`** | **10 055** — contre 51 735 hier. `POWER_CUT` : 41 733 → **24** | 🔴 [TRK-035](../REFERENCE-ERREURS.md#trk-035) |
| **Trames `ac alarm` (`wire_logs`)** | **353** au total sur 4 jours, **dernière le 19/08 02:26:23**, aucune depuis 23 h | 🟢 **le capteur est vivant et il ne compte rien** — l'épisode est clos |
| **`notification_deliveries`** | **145** sur 24 h — 112 supprimées, 22 groupées, **11 envoyées** | ✅ cohérent avec la fin de la rafale (1 020 hier) |
| **`gps_dead_zones`** | **18** zones : 13 `LEARNING`, **4 `CONFIRMED_BENIGN`** (dont **1 non revue** → automatique), 1 `RECURRING` | 🟢 qualification automatique **exercée pour la 1ʳᵉ fois** |
| **`gps_loss_events`** | **34** épisodes, **14** refermés — dont **8 aux durées fabriquées** | 🔴 [TRK-031](../REFERENCE-ERREURS.md#trk-031) confirmée |
| **`trackers.lastPowerNoticeAt`** | **1** boîtier renseigné sur 42, le 19/08 02:26:23 | ⚠️ mécanisme exercé **une fois** ; instrument incapable de compter |
| **`backup_runs`** | **113** exécutions, **0 échec**, dernière le **19/08 03:04:34** | ✅ |
| **`allowlist_audit_logs`** (passerelle) | **283** appels : **234** `ok`, **49** `removals_blocked` (**+0**) | 🔴 TRK-017 · TRK-025 non exercé |
| **`allowlist_entries`** (passerelle) | **44**, la plus récente du 19/08 01:04:03 | ✅ aucune suppression depuis 231 h |
| **`messages`** (passerelle) | **343** `queued` (+2) · **231** `received` (**+1 le 19/08 08:28:58**) · 29 `failed` du 09/06 | 🆕 [TRK-036](../REFERENCE-ERREURS.md#trk-036) — le `+1` est un accusé de réception boîtier |
| **`sms_logs`** (Tracky) | 341 `queued` · **87 `received`**, le dernier le **19/08 08:28:58**, `imei` **NULL** | 🆕 TRK-036 |
| **`engine_control_commands`** | 2 857 `ACKNOWLEDGED` · **280 `SENT`** dont **274** > 24 h · 1 602 `FAILED` · 223 `REJECTED_SPEED` | 🔴 TRK-018 |
| **Dernière commande moteur par boîtier** | **33 `CUT`** · 6 `RESTORE` | 🔴 **[TRK-032](../REFERENCE-ERREURS.md#trk-032)** — l'angle mort du jour |
| **`_prisma_migrations`** | 4 migrations le 19/08, en **3 vagues** horaires | ⚠️ seule trace horodatée des déploiements |
| **Rétention (`RETENTION` / `logs_purged`)** | 19/08 03:00:20 : `wireDeleted: 163301`, **`errorDeleted: 0`** | ✅ la rétention n'est pas en cause dans TRK-035 |

---

## 🔵 Terrain

### KSR370 — mort depuis 143 heures, et 9 épisodes ouverts en attente

Dernière trame **14/08 02:05:24**. Aucun `POWER_CUT` n'a jamais été émis pour lui — le test daté du
19/08 a expliqué pourquoi : **il ne pouvait pas**, un boîtier qui perd son alimentation cesse
d'émettre avant de pouvoir le dire. Contrôle terrain demandé depuis le **15/08 — 6ᵉ jour**.

⚠️ **Il porte à lui seul 9 des 20 épisodes ouverts.** Le jour où il revient, il écrira neuf absences
se terminant à la même seconde, dont une du 15/07. C'est le prochain déclencheur de
[TRK-031](../REFERENCE-ERREURS.md#trk-031), et il est prévisible.

### FZ-862-VY — 60,8 h, épisode ouvert le 17/08

Perdu le 17/08 à 12:22:55. Zone `879699a3`, `CONFIRMED_BENIGN` / `UNDERGROUND_PARKING` depuis le
03/08, 4 occurrences. Silencieux par conception. Porte **4 épisodes ouverts**.

### HD-779-MA — seul boîtier `FAILING` du parc

Cible 30 s, cadence réelle **10 s**, 3 échecs, `ONLINE`, trame de la seconde de la collecte. Le parc
était à **0 FAILING** hier. Un seul point : à re-mesurer avant d'en dire quoi que ce soit.

### Deux boîtiers hors ligne sans véhicule, depuis des mois

`863378070030776` (dernière trame **05/06**) et `864035054756292` (**01/07**). Ni l'un ni l'autre
n'est rattaché à un véhicule. Ils comptent dans les « 4 hors ligne > 1 h » sans rien signifier pour
l'exploitation — **c'est du stock, pas une panne**.

---

## Pièges d'outillage payés ce jour

1. 🔴 **`pg_stat_user_tables` est le seul témoin d'une suppression hors application.** Ni
   `system_activity_logs`, ni le journal du conteneur, ni la rétention n'ont vu partir 41 709 alertes.
   `n_tup_del` et `last_autovacuum` l'ont dit en une requête. *Quand un compteur chute sans
   explication, la question n'est pas « qui l'a vidé ? » mais « qu'est-ce qui, dans la base
   elle-même, garde une trace de l'avoir été ? ».*
2. 🔴 **Ne jamais accuser un endpoint sur son nom.** `POST /api/alerts/acknowledge-all`, appelé trois
   fois le 19/08 dont une **à 09:31:45 — quatre minutes avant la plus ancienne ligne survivante** —
   était le suspect parfait. Lecture du service : c'est un `updateMany` sur `acknowledgedAt`, **il ne
   supprime rien**. Une coïncidence horaire à quatre minutes près n'est pas une preuve.
3. 🔴 **Le vocabulaire de `gps_dead_zones` a changé de colonne, pas de sens.** Le rapport d'hier
   parlait de `UNDERGROUND_PARKING` / `UNKNOWN` ; la table a un `status` (`LEARNING`,
   `CONFIRMED_BENIGN`, `RECURRING`) **et** un `label` (`UNDERGROUND_PARKING`, `UNKNOWN`). Grouper sur
   `status` puis comparer aux chiffres d'hier fabriquerait une évolution de toutes pièces (§4 bis).
   **La distinction manuel/automatique se lit sur `reviewedAt`, pas sur le label.**
4. **Trois tables portent un nom qui n'existe pas là où on le cherche.** `allowlist_entries`,
   `allowlist_audit_logs` et `messages` sont dans la base **`vizyo_texto`** du conteneur
   `texto-postgres` — pas dans `tracky_prod`. Et cette base ne s'appelle **pas** `texto` :
   `psql -d texto` échoue. `SELECT datname FROM pg_database` d'abord.
5. **Les colonnes, encore** : `trips` n'a pas de `mapMatched` (c'est **`polylineMatched`**),
   `tracker_commands` n'a pas de `resultMessage` (c'est **`observedResult`**), `positions` n'a pas de
   `vehicleId` (passer par **`trackerId`**), `messages` n'a pas de `from`/`to` (c'est **`fromNumber`**
   / **`toNumber`**), `trackers` n'a pas de `phoneNumber` (c'est **`simPhoneNumber`**), et
   `gps_loss_events` n'a pas de `deadZoneId` (c'est **`zoneId`**). *Une requête sur
   `information_schema.columns` coûte une seconde et a évité six allers-retours de plus.*
6. **`coalesce(label, '(null)')` sur une colonne `enum` échoue** — PostgreSQL refuse la chaîne comme
   valeur d'énumération. `label::text` d'abord, `coalesce` ensuite.
7. **Le tag `latest` détruit l'historique des déploiements.** `docker images` ne montrait qu'une
   image du 19/08 17:10 alors que trois vagues de migrations prouvent trois déploiements au moins.
   **Les migrations sont la seule horloge disponible** — et elles ne datent que les déploiements qui
   touchent le schéma.

---

## Fiches ajoutées ou mises à jour

**Nouvelles : 5** ([TRK-032](../REFERENCE-ERREURS.md#trk-032),
[TRK-033](../REFERENCE-ERREURS.md#trk-033), [TRK-034](../REFERENCE-ERREURS.md#trk-034),
[TRK-035](../REFERENCE-ERREURS.md#trk-035), [TRK-036](../REFERENCE-ERREURS.md#trk-036)).

| Fiche | Mise à jour |
|---|---|
| [TRK-032](../REFERENCE-ERREURS.md#trk-032) | 🆕 **33 boîtiers sur 42 ont leur alarme d'alimentation éteinte chaque nuit** — « coupure commandée par nous » est déduit de la dernière commande moteur, sans borne de temps ni vérification que la coupe est en vigueur. L'automatisation horaire coupe la flotte à 18:00/20:00. **Gravité 1.** |
| [TRK-036](../REFERENCE-ERREURS.md#trk-036) | 🆕 **L'accusé de réception SMS du boîtier arrive et est jeté** — « Resume engine Succeed » reçu le 19/08 08:28:58 depuis la SIM de GS-014-NY, écrit dans `sms_logs` avec `imei` NULL, jamais rapproché de la commande créée 3 h 50 plus tôt, toujours `SENT`. Gravité 2. |
| [TRK-035](../REFERENCE-ERREURS.md#trk-035) | 🆕 **41 709 alertes et ≥ 89 lignes d'erreur supprimées hors application, sans trace** — ni la rétention (`errorDeleted: 0`), ni aucun chemin applicatif, ni la migration. Seul `pg_stat_user_tables` en témoigne. **Rend le taux de TRK-023 illisible.** Gravité 1 (consigne) / 3 (technique). |
| [TRK-033](../REFERENCE-ERREURS.md#trk-033) | 🆕 **`/admin/vps` plante sur `chargeDeFond`** — champ déclaré obligatoire par le type, absent du JSON réel, déréférencé sans garde. Toute la carte « Prévisions » meurt. **En ligne au moment de l'écriture.** Gravité 2. |
| [TRK-034](../REFERENCE-ERREURS.md#trk-034) | 🆕 **Fenêtre de recalcul (1 500 h = 62,5 j) > rétention des positions (60 j)** — bande de 2,5 jours qui produira la même erreur à chaque passage, indéfiniment. Gravité 3. |
| [TRK-031](../REFERENCE-ERREURS.md#trk-031) | 🔴 **CONFIRMÉE, à l'échelle exacte prédite.** FS-253-HR revenu le 19/08 13:48:56 → **9 épisodes refermés à la même seconde**, durées de 6,94 à **35,18 j**, dont **8 fabriquées**. Réfutation : **5 027 positions** pendant la plus longue. **La médiane de la zone affiche 16,03 j pour une absence réelle de 6,94 j.** Dette restante : **20 épisodes ouverts sur 9 véhicules**, dont 9 sur KSR370. |
| [TRK-028](../REFERENCE-ERREURS.md#trk-028) | ⚠️ **Le dégât annoncé est arrivé sur l'écran que la fiche a créé** : la médiane de zone, écrite pour répondre « quand revient-il ? », répond **16 jours** pour un parking de 7 jours. |
| [TRK-023](../REFERENCE-ERREURS.md#trk-023) | ⚠️ **Taux passé de 81,6 % à 5,5 % — par effacement du dénominateur, pas par correction.** Le correctif (message porté par `POWER_CUT`) est déployé et **non exercé** : aucune `POWER_CUT` depuis 01:56 le 19/08. |
| [TRK-022](../REFERENCE-ERREURS.md#trk-022) | 🔴 → 🟠 **Déduplication livrée** (`DEDUP_ALARME_MS` = 6 h, tous types). **NON EXERCÉE** : aucune rafale depuis. 0 survitesse sur 24 h (6ᵉ jour). |
| [TRK-018](../REFERENCE-ERREURS.md#trk-018) | **Test daté passé 9ᵉ fois** (274). 🆕 **Sa nature change** : la preuve de remise n'est pas absente, elle est **jetée** — voir [TRK-036](../REFERENCE-ERREURS.md#trk-036). |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) | **6ᵉ point** : 74/75. Silences tracés 243 → **651**. 🆕 **Devient un aggravant de [TRK-032](../REFERENCE-ERREURS.md#trk-032)**. |
| [TRK-027](../REFERENCE-ERREURS.md#trk-027) | 🟢 **FS-253-HR est revenu à 6,94 j**, sous le seuil des 7 jours. Le contrôle terrain demandé depuis le 03/08 n'a plus d'objet sur ce véhicule : le boîtier fonctionne. |
| [TRK-017](../REFERENCE-ERREURS.md#trk-017) | **10ᵉ jour**, clé `vtx_48fe` inchangée. Blocages **49 (+0)**, appels normaux 208 → **234**. **54 h de silence**, le plus long — 7ᵉ silence lu comme un silence. |
| [TRK-013](../REFERENCE-ERREURS.md#trk-013) | Numérateur 13 → **15**, dénominateur 72 → **81**. Les deux repartent. |
| [TRK-012](../REFERENCE-ERREURS.md#trk-012) | **10ᵉ jour d'attente d'accord.** 284 échecs « intervalle observé 20 s » sur 7 j. |
| [TRK-001](../REFERENCE-ERREURS.md#trk-001) · [TRK-015](../REFERENCE-ERREURS.md#trk-015) · [TRK-016](../REFERENCE-ERREURS.md#trk-016) · [TRK-019](../REFERENCE-ERREURS.md#trk-019) · [TRK-020](../REFERENCE-ERREURS.md#trk-020) · [TRK-021](../REFERENCE-ERREURS.md#trk-021) · [TRK-024](../REFERENCE-ERREURS.md#trk-024) · [TRK-025](../REFERENCE-ERREURS.md#trk-025) · [TRK-026](../REFERENCE-ERREURS.md#trk-026) · [TRK-029](../REFERENCE-ERREURS.md#trk-029) · [TRK-030](../REFERENCE-ERREURS.md#trk-030) | Compteurs relevés, statuts inchangés. |

---

## 🗓️ Tests datés pour le prochain passage

1. **[TRK-032](../REFERENCE-ERREURS.md#trk-032) — la mesure de nuit.** Rejouer le comptage « dernière
   commande moteur par boîtier » **entre 20:00 et 06:00**. Attendu si rien n'est corrigé :
   **≈ 33 boîtiers en `CUT`**, donc autant d'alarmes d'alimentation éteintes. Un chiffre nettement
   plus bas voudrait dire que la coupe nocturne n'est pas systématique — ce qui changerait la portée
   de la fiche, **pas** son diagnostic.
2. **[TRK-031](../REFERENCE-ERREURS.md#trk-031) — KSR370 est le prochain déclencheur, et il est
   prévisible.** 9 épisodes ouverts, le plus ancien du 15/07. S'il revient, compter les épisodes
   refermés à la même seconde et relever la médiane de sa zone `44cfb9f4` **avant** et **après**.
   S'il ne revient pas, la dette reste — ce n'est pas une réponse négative.
3. **`POWER_CUT` — la vraie question n'est plus « combien d'alertes » mais « combien de trames ».**
   Compter les `ac alarm` dans `wire_logs` (rétention 3 j) **et** les alertes créées **et**
   `lastPowerNoticeAt`. Si des trames reviennent : combien produisent une alerte, combien une notice,
   combien rien du tout ? **C'est le seul test qui puisse exercer la déduplication de 6 h et le
   message de `messageCoupure`, tous deux déployés et non vérifiés.**
4. **[TRK-035](../REFERENCE-ERREURS.md#trk-035) — la borne basse.** Relever `min("createdAt")` de
   `error_logs` et `count(*)` de `alerts`. La borne ne doit pas **avancer** sans qu'une purge soit
   inscrite quelque part. Si elle avance à nouveau, le contournement est **récurrent** et non
   accidentel — ce qui changerait la gravité.
5. **[TRK-033](../REFERENCE-ERREURS.md#trk-033) — vérifier si l'écran est réparé, et comment.** Si
   `chargeDeFond` est apparu dans le manifeste **sans** que le gabarit ait été gardé, le défaut est
   masqué et non corrigé : le prochain champ ajouté le rejouera à l'identique.
6. **La cadence de l'automatisation des trajets.** Troisième point requis (§4 bis) : les passages
   sont-ils toujours espacés d'≈ 2 h, avec des durées proches de 50 min ? Deux points ne font pas une
   tendance ; trois permettront de dire si une tâche « horaire » l'est encore.
7. **[TRK-030](../REFERENCE-ERREURS.md#trk-030) — toujours en attente d'une mise en service.** Parc
   stable à 42 depuis le 19/08 01:04. Relever l'écart **login TCP → premier fix** au prochain boîtier
   posé.
8. **[TRK-017](../REFERENCE-ERREURS.md#trk-017) — la seule question qui compte reste la même.**
   `tenants.apiKeyPrefix` a-t-il changé ? Tant qu'il vaut `vtx_48fe` avec un `updatedAt` au 05/06, le
   nombre de frappes est une distraction. **11ᵉ jour.**
