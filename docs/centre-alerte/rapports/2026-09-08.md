# Audit du centre d'alerte — 2026-09-08

> Collecte : **2026-09-08 01:12 → 01:35 UTC**. Tous les horodatages de ce rapport sont en **UTC**,
> sauf les dates de `git log`, en heure de **Paris** (UTC+2) et signalées comme telles.
>
> ⚠️ **Un déploiement a eu lieu 10 minutes avant la collecte** — `tracky-api` redémarré le **08/09 à
> 01:01:57** (`restarts=0`, `healthy`, image de 00:54:13), `tracky-web` à **00:56:59** (image de
> 00:56:15). **Troisième déploiement non annoncé en trois jours, et le troisième à tomber dans le
> quart d'heure qui précède l'audit.** Tout ce qui est écrit ci-dessous sur l'artefact servi porte
> sur **cette** image.

**Verdict :** 118 lignes actives dont 56 défauts — et le fait du jour est qu'**un seul trajet du
8 juillet, dont les positions viennent de franchir le front de purge, est rejoué à chaque passage
horaire et écrit une erreur à chaque fois** : **15 des 17 défauts neufs des 24 h portent le même
`tripId`**, et les deux méthodes qui le rejouent portent chacune un commentaire promettant « on
n'alerte pas » — la ligne est écrite **un étage plus bas**, par la couche qui ne décide de rien.

---

## Chiffres

| | |
|---|---|
| Lignes `error_logs` **actives** | **118** — 56 défauts + 62 `DEGRADATION` (24 sur 24 h, dont **17 défauts**) |
| dont `CRITICAL` actives | **6** — inchangé *(4 `scheduled-task-heartbeat` du 06/09 + 2 `http`)* |
| Lignes archivées | **37** *(+1 : la 5ᵉ `agents-locaux`, archivée **d'elle-même** le 07/09 à 20:50)* |
| Constats de disparition | **1** *(celui du 04/09, aucun nouveau)* — `temoin_arme` rend ses **4 lignes**, toutes `actif = O` |
| Écart `ins − del − live` (`error_logs`) | **13 250** — inchangé, **12ᵉ point consécutif** |
| Trackers FAILING | **0** |
| Trackers hors ligne > 1 h | **14** (0 jamais mis en service) — 9 déclarés + 3 de stock + **2 NON déclarés** |
| Commandes en attente > 10 min | **3** — les 2 SMS de [TRK-062](../REFERENCE-ERREURS.md#trk-062) à **155,6 h** et **159,0 h**, plus 1 `fix_continuous` de **29 min** née cette nuit |
| Commandes échouées / jour (7 j) | **16,0** — total famille `fix_continuous` **409** |
| Acquittements matériels | **0 sur 409** — **11ᵉ point** |
| Cadence sous le minimum matériel | **0** sur 44, cible la plus basse **20 s** — **8ᵉ point consécutif** |

### D'où viennent les 24 lignes de plus qu'hier — décomposition exacte

| Origine | Lignes | Niveau |
|---|---|---|
| 🆕 **[TRK-075](#-trk-075)** — « Analyse impossible », **toutes le même trajet** | **15** | `ERROR` |
| `sentinelles` du 07/09 06:30 — vitesse contredite ×3 + pointes refusées ×1 | 4 | `DEGRADATION` |
| `trip-analysis` — Overpass injoignable ([TRK-037](../REFERENCE-ERREURS.md#trk-037)) | 3 | `DEGRADATION` |
| 🆕 **[TRK-076](#-trk-076)** — perte de contexte WebGL sur `/map` | 2 | `ERROR` |

94 + 24 = 118. **Aucune ligne non expliquée.**

---

## 🔴 À traiter

### 🆕 TRK-075 — le rejeu d'analyse n'a pas de sortie, et il crie depuis l'étage où l'on ne décide rien

**Signature** — `trip-analysis | ERROR | Analyse impossible : les positions de ce trajet ne sont plus
disponibles (purge de retention probable). Produire une analyse vide reviendrait a inventer un trajet
immobile.` Contexte : `stage: 'compute'`, `tripId`, `vehicleId`, `fleetId`, parfois
`repeatedSuppressed`.

**15 occurrences actives**, du 07/09 03:45 au 08/09 00:50 — **toutes le même trajet**,
`b644fe50-dca8-4999-b332-5b652e391e8f`.

#### Ce que la mesure établit

| Fait | Mesure |
|---|---|
| Le trajet | 08/07 15:10 → 15:30, **3 186 m**, `HD-597-XY` (cdef31) — **61,4 jours** |
| Ses positions | **0** dans sa fenêtre |
| La position la plus ancienne de **toute** la base | 09/07 03:30 — **60,9 jours** |
| Le réglage **servi** (`docker inspect`) | `POSITIONS_RETENTION_DAYS=60` → horizon = J−59 |
| Son analyse | **elle existe** — `limitsCoverage = 0,714`, calculée le **20/08 11:28** |
| Passages de l'automatisation le 07/09 | **17**, pour **15** lignes d'erreur |

> Le trajet a franchi le front de purge il y a environ vingt-quatre heures : **ses positions étaient
> là le 20/08 quand son analyse a été écrite, elles n'y sont plus.** Ce n'est pas une panne, c'est
> une échéance — et rien dans le code ne la traite comme telle.

#### Cause racine — deux défauts qui se composent

**(a) La décision d'alerter appartient à l'appelant ; l'écriture est faite par l'appelé.**

`TripAnalysisService.analyze()` (`trip-analysis.service.ts:92-101`) enregistre au centre d'alerte
dans son **propre** `catch`, avec `stage: 'compute'`, **puis re-lève**. Les trois appelants décident
ensuite — et **deux d'entre eux ont explicitement décidé de se taire** :

| Appelant | Ce que son commentaire promet | Ce qui arrive |
|---|---|---|
| `analyserEtNarrer` (`trip-automation.service.ts:922`) | fige sous l'horizon — **« Aucune alerte — le figeage EST la trace »** (`:939`) | la ligne est **déjà** écrite |
| `rejouerAnalysesIncompletes` (`:1365`) | **« On n'alerte pas, sinon le centre d'alerte se remplirait d'un fait sans remède »** (`:1370`) | la ligne est **déjà** écrite |
| `reprendreAnalysesAnciennes` (`:1470`) | **« Un échec ne remonte PAS au centre d'alerte »** (`:1413`) | la ligne est **déjà** écrite |

**La preuve tient dans le contexte de la ligne.** Il porte `stage: 'compute'` — écrit **uniquement**
à `trip-analysis.service.ts:98` — et **non** `phase: 'analyze'`, qui est le marqueur de
`trip-automation.service.ts:956`. La ligne ne vient donc pas de la branche qui a le droit d'alerter.

> 🔑 **Trois commentaires promettent un silence que leur couche n'a pas le pouvoir d'accorder.**
> C'est une variante inédite du motif de [TRK-026](../REFERENCE-ERREURS.md#trk-026) : là, un
> instrument était incapable de crier ; ici, trois appelants sont incapables de se taire.

**(b) `rejouerAnalysesIncompletes` n'a pas le plancher de rétention que sa jumelle possède.**

`reprendreAnalysesAnciennes` borne ses candidats par `t."startedAt" > horizonRetention()` (`:1441`),
avec un commentaire qui explique exactement pourquoi (« *le rattrapage COURT CONTRE LA PURGE* »).
`rejouerAnalysesIncompletes` (`:1347-1355`) ne filtre que sur `limitsCoverage < 0.8` et
`computedAt < now − 12 h` : **aucune borne sur l'âge du trajet.**

Un candidat dont les positions sont purgées **ne peut plus jamais améliorer sa couverture** — il
reste donc candidat pour toujours. Et le tri `limitsCoverage ASC, computedAt ASC` le fait remonter.
Mesuré ce matin : **17 candidats, dont 3 sous l'horizon**, pour un plafond de 25 — **tous sont donc
tentés à chaque passage.**

> 🔑 **Le rejeu est le seul travail de cette automatisation qui ne converge pas.** Les autres files se
> vident : un trajet analysé sort de `sansAnalyse`, un trajet figé sort du front de recalcul. Un
> candidat au rejeu dont la matière première a disparu n'a **aucune sortie** — ni succès, ni figeage,
> ni abandon. *C'est la forme exacte de [TRK-062](../REFERENCE-ERREURS.md#trk-062) — une commande
> sans état terminal — transposée à une file de calcul.*

#### Ce qui n'est PAS le défaut

Le refus d'écrire une analyse vide est **juste**, et c'est la garde du 21/08 (60 analyses fausses
retrouvées en base). **L'erreur n'est pas de refuser** : c'est de refuser quinze fois par jour,
indéfiniment, en criant depuis une couche qui n'a pas le droit de juger si le cri est justifié.

#### Correctif proposé — deux gestes, et le second ne remplace pas le premier

1. **Remonter la décision au bon étage.** Donner à `TripAnalysisService.analyze()` un drapeau
   explicite (`{ signalerAuCentre?: boolean }`, **vrai par défaut**) plutôt qu'un enregistrement
   inconditionnel. Les trois appelants deviennent alors capables de tenir la promesse écrite dans
   leurs propres commentaires.
   ⚠️ **Ne PAS supprimer purement et simplement le `record` de `:95`.** La route
   `POST /trips/:id/analysis` est un vrai chemin utilisateur : un calcul demandé par un humain et qui
   échoue doit rester visible. *Un correctif qui vide l'écran en retirant le journal n'est pas un
   correctif.*

2. **Poser à `rejouerAnalysesIncompletes` le plancher que sa jumelle a déjà** —
   `t."startedAt" > horizonRetention()`, avec la même justification écrite. Une analyse dont le
   trajet est sous l'horizon ne gagnera jamais de couverture ; la rejouer est du budget dépensé pour
   un résultat impossible, pris sur les trajets récents que le rejeu existe pour sauver.

#### Vérification — double condition, et elle porte sur la CAUSE

- `error_logs` ne doit plus porter **aucune** ligne `stage: 'compute'` pour un trajet **sous**
  l'horizon ; **ET**
- le compte de candidats au rejeu doit tomber de **17 à 14**, **sans** que `stats.rejouees` tombe
  à zéro.

⚠️ *Si les deux tombent, on a éteint le rejeu au lieu de le borner* — et les analyses mal couvertes
des trajets **récents** cesseraient d'être reprises.
⚠️ Et un refus **au-dessus** de l'horizon doit **continuer** de crier : c'est le seul cas où
l'absence de positions est une vraie anomalie, et le code le dit déjà lui-même (`:942-945`).

#### Pourquoi ça reviendra tout seul

**Le front de purge avance d'un jour par jour.** Chaque jour, de nouvelles analyses mal couvertes
passent sous l'horizon et deviennent des candidats immortels. Ce défaut n'est pas un accident isolé :
c'est une **fuite lente**, dont aujourd'hui est simplement le premier jour visible.

---

## 🟢 Ce qui s'est prouvé aujourd'hui

### 🆕 TRK-076 — la carte survit à une perte de contexte WebGL : diagnostiqué, corrigé et **déployé** entre deux audits

**Signature** — `frontend | ERROR | [uncaught] TypeError: Cannot read properties of null (reading
'getSource')`. Contexte : `page: /map`, `userId`, `fleetId`, `sessionId`, `userEmail`.
**2 occurrences**, 07/09 **13:01:04** et **13:04:24**, `standard@cdef31.org` (cdef31).

**Statut : 🟢 CORRIGÉ ET DÉPLOYÉ** — commit `09d04e2b` (*« la carte se reconstruit vraiment après une
perte de contexte WebGL »*, 07/09 19:33 Paris), en ligne depuis le déploiement de **01:01:57**.
**Vérifié sur l'artefact servi**, jamais sur la fiche : `webglcontextlost` **et** `carteUtilisable`
sont présents dans `/usr/share/nginx/html/chunk-3BYQORV7.js`.

**La cause, telle que le correctif la documente** (`map.component.ts:3128-3163`) : le navigateur peut
retirer le contexte WebGL à tout moment (pilote qui redémarre, bascule GPU, onglet longtemps en
arrière-plan). MapLibre 5.24 fait alors `this.style.destroy(); this.style = null` — **l'objet `Map`
reste vivant**, donc **toutes les gardes `if (!this.map) return` passent**, mais `map.getSource(...)`
lit `this.style.getSource(...)` et lève. Il y a **dix-neuf** appels `getSource` dans ce fichier.

> 🔑 **La garde existante testait la mauvaise chose.** Elle demandait « la carte existe-t-elle ? »
> quand la question était « son style existe-t-il encore ? ». *Une garde qui passe toujours ne garde
> rien* — c'est le motif de [TRK-026](../REFERENCE-ERREURS.md#trk-026) sur une garde de front.

⚠️ **Déployé n'est pas prouvé.** À la collecte, l'artefact corrigé est en ligne depuis **26 minutes**
et aucune ligne `getSource` n'est apparue — c'est **trop court pour conclure quoi que ce soit**. La
preuve attendue : **aucune** nouvelle ligne `getSource` sur sept jours, **et** l'apparition, si le cas
se reproduit, du bandeau de reprise plutôt qu'un « Une erreur est survenue ».

*Cette fiche est créée close, et c'est délibéré : le défaut a été instruit et corrigé par une autre
session entre les deux audits. L'audit n'a rien à y ajouter — il enregistre la signature pour que la
prochaine occurrence soit reconnue, et il vérifie la mise en ligne.*

### Le silence de la sentinelle « boîtiers muets » est un refroidissement, pas une panne

Le 06/09, elle écrivait ses 2 lignes (preuve de **V24**). Le 07/09 à 06:30 : **4 `DEGRADATION` et
zéro ligne `ERROR`**, alors que les 10 boîtiers sont toujours muets. **Vérifié dans le code plutôt
que supposé** : cette sentinelle porte
`fenetreMs: REFROIDISSEMENT_HEBDOMADAIRE_MS` — **6,5 jours** (`sentinelles-coherence.service.ts:435`).
**Prochaine ligne possible : le passage du 13/09 06:30.** ✅ **V24 n'est pas en régression.**

*Le réflexe qui a évité l'erreur : ne pas lire un zéro sans vérifier que l'instrument était en mesure
de rendre autre chose. Ici, il ne l'était pas — et c'est voulu.*

---

## 🟠 Connu, correctif en attente

| Fiche | État au 08/09 |
|---|---|
| [TRK-073](../REFERENCE-ERREURS.md#trk-073) · [TRK-074](../REFERENCE-ERREURS.md#trk-074) | **Non corrigées, non déployées** — vérifié sur l'artefact : `EN_COURS` **absent** de `trip-automation.service.js`, « résolution automatique » **absente** de `scheduled-task-heartbeat.service.js`. Les 4 `CRITICAL` du 06/09 sont **toujours actives**, 29 h après la fin de la condition |
| [TRK-062](../REFERENCE-ERREURS.md#trk-062) | **Non déployé — 5ᵉ vérification négative** (`SENT_UNCONFIRMED` : 0 fichier dans `tracker-commands/`). Les 2 commandes atteignent **155,6 h** et **159,0 h** |
| [TRK-070](../REFERENCE-ERREURS.md#trk-070) · [TRK-068](../REFERENCE-ERREURS.md#trk-068) | Déployés, **toujours non exercés** — `ASSISTANCE` inchangée depuis le 05/09 17:00 (**56 h**), `http CRITICAL` depuis le 04/09 12:57 (**84 h**) |
| [TRK-071](../REFERENCE-ERREURS.md#trk-071) | **Inchangée.** Anthropic à sec depuis le 03/09 00:13 — **121 h**, 6ᵉ jour. OpenAI constaté vide le 05/09 |
| [TRK-072](../REFERENCE-ERREURS.md#trk-072) | **Muette, et pour la bonne raison** — 9 envois de notification sur 24 h, très en dessous du seuil. Série : 22 · 84 · 34 · 16 · **9** |
| [TRK-060](../REFERENCE-ERREURS.md#trk-060) · [TRK-065](../REFERENCE-ERREURS.md#trk-065) · [TRK-066](../REFERENCE-ERREURS.md#trk-066) | Déployés, **non exercés**. TRK-065 : ligne hebdomadaire attendue vers le **11/09** ; TRK-066 : toujours **1 seule** occurrence (04/09 03:00) |
| [TRK-064](../REFERENCE-ERREURS.md#trk-064) | 2 sociétés armées sur 5, **inchangé**. Le sujet reste sur TRK-072 |
| [TRK-067](../REFERENCE-ERREURS.md#trk-067) · [TRK-043](../REFERENCE-ERREURS.md#trk-043) · [TRK-059](../REFERENCE-ERREURS.md#trk-059) | ✅ **Tiennent.** 0 ligne `system-activity` depuis le 04/09 16:55 ; les 15 motifs de clôture citent tous la cible **demandée** (**J+3**) |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) · [TRK-018](../REFERENCE-ERREURS.md#trk-018) · [TRK-027](../REFERENCE-ERREURS.md#trk-027) · [TRK-035](../REFERENCE-ERREURS.md#trk-035) · [TRK-037](../REFERENCE-ERREURS.md#trk-037) | Inchangés. TRK-037 : **62 `DEGRADATION` / 0 `ERROR`**, **7ᵉ point** — le classement tient |

---

## ⚪ Bruit / 🔵 Terrain

- **62 `DEGRADATION` `trip-analysis`** (Overpass HTTP 500/502/504) — dépendance tierce assumée,
  TRK-037. **3 lignes neuves** en 24 h, cadence habituelle.
- **4 `DEGRADATION` `sentinelles`** du 07/09 06:30 — vitesses contredites par la distance
  (« cdef31 » 17/66, « A2R » 7/26, « mh cars » 3/9) et **16 pointes refusées sur « A2R »**.
  ⚠️ **Fait mesuré, pas tendance** : fenêtre glissante de 24 h sur des journées différentes.
- **5 `gps-integrity`** actives — `FG-669-DQ`, `HD-603-XY`, `GS-928-NX`, `GLA•KC•31`. Action terrain.
- 🔵 **2 boîtiers hors ligne NON déclarés** — `GLA•KC•31` muet depuis le **03/09 01:53 (119 h)** et
  `FG-669-DQ` depuis le **06/09 14:18 (35 h)**.
- ⚪ **1 `frontend`** « Clipboard : Write permission denied » (06/09) — refus du navigateur sur un
  clic de copie, pas un défaut applicatif.

---

## Angles morts examinés

| Section | Mesure | Lecture |
|---|---|---|
| `temoin_arme` | **4 déclencheurs, tous `actif = O`** | L'instrument est armé : les comptes de ce rapport sont des **mesures**, pas des planchers |
| `disparitions` | **1 constat**, celui du 04/09 — **aucun nouveau** | Rien n'a été supprimé depuis **96 h** |
| `comptes_disparition` | écart `error_logs` **13 250**, inchangé (**12ᵉ point**) ; `alerts` **0** | Pas de `TRUNCATE` |
| `retention_declaree` | `errorDeleted = 0` sur **5 nuits**, rétention 90 j ; 79 159 trames purgées cette nuit | L'application ne supprime aucune erreur, de son propre aveu |
| `trackers_failing` | **0** | — |
| `commandes_sante_7j` | **0 acquittement matériel sur 409** — **11ᵉ point**. Total famille 491 · 475 · 455 · 437 · 419 · **409** | [TRK-014](../REFERENCE-ERREURS.md#trk-014) tient |
| `commandes_motifs_7j` | **15 motifs, tous `fix_continuous`**, tous citant la cible **demandée** | ✅ TRK-059 tient à **J+3** |
| `cadence_resume` | **0** sous le minimum matériel, cible la plus basse **20 s** — **8ᵉ point** | TRK-057 et TRK-008 tiennent |
| `cadence_derive` | **0 ligne** | — |
| `cadence_reelle` | **0** émetteur rapide réel *(1 hier)*, **2** mesures périmées sur 44 | ⚠️ **Le 1 d'hier était une fluctuation** — c'est le 2ᵉ point, et la réserve écrite hier vient de s'auto-vérifier |
| `gps_sans_fix` | **0 ligne** | ⚠️ **Zéro trompeur, 4ᵉ jour** : les deux boîtiers muets sortent du filtre **par le bas** (`OFFLINE`), pas parce qu'ils ont retrouvé leur fix |
| **`trip_automation_runs`** | **17 passages sur 24** le 07/09 | 🔴 [TRK-073](../REFERENCE-ERREURS.md#trk-073) **tient**. Série : 04/09 **22** · 05/09 **23** · 06/09 **16** · 07/09 **17** |
| **couverture des limites** | **160 analyses** sur 24 h, **135** avec couverture, moyenne **0,949** | ✅ Le lot V3 écrit, et bien |

### ⚠️ `trip_automation_runs` ne garde que 100 lignes — la série s'efface par le bas

Relevée ce matin, la journée du **03/09 rend 21 passages** ; le rapport du 06/09, qui la mesurait
alors, en comptait **23**. Ce n'est pas une perte : `KEEP_RUNS = 100` élague l'historique à chaque
insertion, et le total des six journées de la fenêtre vaut **exactement 100**.

> 🔑 **Conséquence pour [TRK-073](../REFERENCE-ERREURS.md#trk-073) : le « 16 sur 24 » du 06/09 sera
> illisible dans quelques jours.** Une preuve qui doit être relue plus tard doit être **recopiée**
> quelque part qui ne s'élague pas — c'est ce que fait le journal des passages, et c'est pour ça
> qu'il existe.

### 📏 TRK-016 — sixième point sous le nouveau code

| Fenêtre | Trajets | Sans recalage | Taux d'échec |
|---|---|---|---|
| 24 h | 159 | 136 | **85,5 %** |

Série ouvrée : 91,3 · 87,0 · 89,3 · 91,1 · 91,9 · 89,2 · 83,3 · 87,9 · 87,2 · **85,5**.
**Stable autour de ~87 %** sur quatre points frais. Le chantier T13 peut s'ouvrir sur cette valeur.
⚠️ *Mesuré sur `trips."polylineMatched" IS NULL` — cette colonne est du **texte**, pas un booléen ;
un `IS NOT TRUE` avait fabriqué « 100 % d'échec » le 17/08.*

### 📏 OVERSPEED — quatrième point

| Jour | 04/09 | 05/09 | 06/09 | 07/09 |
|---|---|---|---|---|
| `OVERSPEED` | 12 | 6 | 0 | **1** |
| nées d'un trajet | 12 | 6 | — | **1** |

Le zéro du 06/09 a été expliqué hier par le réglage des flottes, pas par une panne. **Quatre points,
et rien qui ressemble encore à une tendance** — la seule chose acquise est qu'on reste très loin des
1 317 alertes de [TRK-022](../REFERENCE-ERREURS.md#trk-022).

---

## 🗓️ Tests datés — l'état au 08/09

| Fiche | En attente | État |
|---|---|---|
| [TRK-053](../REFERENCE-ERREURS.md#trk-053) | 🔴 **8 j — ÉCHÉANCE DÉPASSÉE** | ✅ Pas de régression : `alertes_depuis_declaration` = **7**, **5ᵉ point identique**. Toujours **non exercé** → **à PROVOQUER ou REQUALIFIER** |
| [TRK-032](../REFERENCE-ERREURS.md#trk-032) | 🔴 **18 j** | **À REQUALIFIER** — l'occasion ne viendra pas seule |
| [TRK-051](../REFERENCE-ERREURS.md#trk-051) | 🔴 **14 j** | **À CONFIER À UN HUMAIN** — geste d'interface, 30 s pour qui a l'écran |
| [TRK-052](../REFERENCE-ERREURS.md#trk-052) | 8 j | ✅ Pas de régression — la seule `OVERSPEED` du 07/09 est née d'un trajet (`tripId` non nul) |
| [TRK-076](#-trk-076) | 0 j | **Déployé ce matin**, 26 min de recul — non exercé |
| TRK-060 · 064 · 065 · 066 · 068 · 070 | 1 à 4 j | Déployés, **non exercés** |

---

## 🤝 Ce qui attend un humain

1. 🔴🔴 **Recharger un compte IA — les DEUX sont à sec, 6ᵉ jour.** Anthropic depuis le 03/09 00:13
   (**121 h**), OpenAI constaté vide le 05/09. *(T1)*
2. 🔴 **TRK-066 — les trois questions du coupe-circuit** : 10 s sur un dernier recours, le réessai et
   son idempotence, le niveau d'une immobilisation demandée **qui n'a pas eu lieu**. *(T3)*
3. **Calibrer les alertes de vitesse — des DEUX côtés** ([TRK-072](../REFERENCE-ERREURS.md#trk-072),
   T4) : le seuil pour les deux flottes armées, **et** la question d'A2R, non armée, qui a touché
   137 km/h le 06/09.
4. **Trancher la migration de TRK-062** — 5ᵉ vérification d'absence. *(T5)*
5. **Prévenir le second titulaire de compte sans appareil** (TRK-065, T6).
6. **TRK-053 (8 j, échéance dépassée), TRK-032 (18 j) et TRK-051 (14 j)** — provoquer ou requalifier.
   *(T14, T19, T20)*
7. 🔵 **Terrain** : `GLA•KC•31` (119 h) et `FG-669-DQ` (35 h), muets et **non déclarés**. *(T9)*

---

## Fiches ajoutées ou mises à jour

| Fiche | Mouvement |
|---|---|
| 🆕 **TRK-075** | Créée : le rejeu d'analyse n'a pas d'état terminal, et l'alerte est écrite par la couche qui ne décide pas — trois commentaires promettent un silence que leur étage ne peut pas accorder |
| 🆕 **TRK-076** | Créée **close** : perte de contexte WebGL sur `/map`, diagnostiquée et corrigée par une autre session entre les deux audits, **déployée et vérifiée sur l'artefact servi** |
| ✅ **V24** | **Confortée** — son silence du 07/09 est un refroidissement **hebdomadaire** lu dans le code, pas une panne |
| 📏 **TRK-016** | 6ᵉ point : **85,5 %** — stable autour de ~87 % |
| 📏 **TRK-072** | 4ᵉ point : **1** `OVERSPEED` le 07/09 ; notifications retombées à **9** sur 24 h |
| 🔎 **TRK-073** | **Tient** — 17 passages sur 24, et sa preuve s'effacera de `trip_automation_runs` (cap à 100 lignes) |

---

## Contrôle de sortie

`pgrep -x docker` sur le VPS après la passe : **aucun client Docker résiduel**. Toutes les commandes
`docker` de cette passe portaient `timeout` **et** une borne de volume (`exec` et `inspect` bornés,
**aucun `docker logs`**), conformément au garde-fou du §0.
