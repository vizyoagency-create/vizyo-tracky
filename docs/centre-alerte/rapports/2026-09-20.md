# Audit du centre d'alerte — 2026-09-20

> Collecte : **2026-09-20 01:11 → 01:26 UTC** (03:11 Paris — la tâche planifiée à 03:09 est partie à l'heure). Tous les
> horodatages de ce rapport sont en **UTC** (sauf mention « Paris », = UTC + 2).
>
> ⚠️ **Ce passage couvre DEUX jours** (18/09 01:26 → 20/09 01:26) : **l'audit du 19/09 n'a pas eu lieu** — le poste dormait à
> 03:09, le rattrapage est parti à **14:12 UTC** (16:12 Paris, à la même seconde que les audits VPS et PC — VPS-M57, 4ᵉ
> collision) et **a été interrompu par l'utilisateur 2 min plus tard** (`Request interrupted by user`, 0 rapport). Rien n'est
> perdu : `error_logs` n'a rien effacé (témoin 4/4) et toutes les lignes du 19 sont lues ici.
>
> ✅ **La production est à `ad0abff4` = `main`** : `deploy.sh` a recréé `tracky-api` / `tracky-web` le 19/09 à **16:36:28**
> (`8e289f38`, `sante: healthy`, **7 037 s** dont l'attente de la fin du passage 15:45 — **T29, 3ᵉ preuve**), puis un passage
> `perimetre: marketing` à 18:26:48 (`ad0abff4`, conteneur `lp` seul, API/web non recréés) ; `restarts=0`, API `healthy`.

**Verdict :** 309 lignes actives dont 175 défauts, **57 `CRITICAL`** (+12) — **les nuits du 18 au 19 et du 19 au 20 sont la
3ᵉ et la 4ᵉ nuits probantes du coupe-circuit : 59 coupes (4 + 25, puis 4 + 26), 59 acquittées par TCP en 0,4 à 6,1 s, 0 SMS,
0 refus, 0 « non confirmée » ; 35 reprises du 19/09 (4 MH Cars 03:00, 27 CDEF31 05:00–05:01, 4 manuelles) acquittées en 0,4 à
7,5 s, dont HM-769-GA coupé puis remis en route pour la première fois en 5 nuits (fix retrouvé le 18/09 ~08:30).** Mais **le S21 a
été hors ligne 2 h 22 dans la nuit du 18 au 19** (02:23:55 → 04:46) : les preuves de 04:30 et 06:30 Paris sont restées en file,
**deux verdicts `INDETERMINEE`** au centre d'alerte, **9 `CRITICAL` `sms-gateway-watchdog` en 2 h** (une toutes les 16 min),
3 e-mails, 5 notifications poussées (T69 (3) et (4) **exercés**) — puis les deux SMS ont été **remis à 04:46:41 et 04:46:53**
quand le téléphone est revenu, l'interlock a retrouvé une preuve fraîche, et **rien ne l'a écrit** : à 07:00 Paris le centre
d'alerte disait encore « l'interlock refusera les coupes de ce soir », ce qui était faux depuis 04:47 — 🆕 **[TRK-089](../REFERENCE-ERREURS.md#trk-089)**,
gravité 3 (*la sentinelle crie toutes les 16 min comme l'interlock de TRK-081, et la preuve remise en retard ne referme jamais
son verdict*). 🔴🔴 **Hors centre d'alerte, et c'est la seule chose à faire en premier : le VPS est étranglé.** V33 (`docker logs`
bloqué depuis le 16/09 12:41) est **toujours vivant (84 h 37)** et un **second `docker logs` sans `timeout` s'est bloqué le 19/09 à
18:30:17** (PID 2060212, `docker logs --tail 500 ae54b0ff3f79` = `foodsqan-traefik`, script « PROXY/ROUTEURS » lancé 4 min après le
déploiement marketing — **VPS-016 nº 7**) : `dockerd` a pris le **second cœur à 18:40** (`%idle` 37 → **7 %**), puis l'hyperviseur
a commencé à reprendre le CPU — **`%steal` 3 % à 22:00 → 21 % à 22:10 → 46 % à 23:10 → 72 % à 00:10 → 89 % à 01:10** ; à 01:17
la charge est à **56 / 41 / 27** pour **6 % d'user**. Vizyo Auth a été injoignable **00:00:26 → 00:02:20** (2 lignes `http`,
TRK-068 exercé, une 3ᵉ fondue par l'anti-flood) et l'API a mis **57 s** à rendre un 503 dont le délai est de 8 s. **Les 31
reprises de ce matin (03:00 MH Cars, 05:00 CDEF31) partent sur une VM qui dispose d'un dixième de son CPU** — la course de
TRK-083 devient probable, et son prix est un SMS payant plus une `CRITICAL` par reprise réussie. 🔴 **Les agents du poste sont
en pause depuis le 17/09 08:50** (T75 non fait) : **0 récit de trajet depuis 75 h**, 3 `CRITICAL` « passage manqué » de plus —
*la vérification quotidienne ignore la pause* (reste de T34/T58). 📏 **T30 : 1 paire de doublons le 18/09** (FV-941-LZ, 141,7
km/h, 1ʳᵉ alerte **sans instant**) — la lecture « au plafond 130 pile » d'avant-hier était encore trop étroite.

---

## 🟢 Coupe-circuit — 24 h de preuve *(T65, 4ᵉ lecture — deux nuits, heures de lecture 01:11 → 01:26 UTC)*

| # | Ce qu'on vérifie | Lu | Verdict |
|---|---|---|---|
| 1 | **Preuve SMS quotidienne** 04:30 / 06:30 / **21:30** Paris, `verdict=OK` à +15 min | `sms_logs` sur 60 h : **18/09** `gateway_daily_proof` `delivered` à 02:30 (+8 s), 04:30 (+10 s), 19:30 (+10 s), écho entrant à chaque fois ; **19/09** : `02:30:00` **remise à 04:46:41 (+2 h 16)**, `04:30:00` **remise à 04:46:53 (+16 min)**, échos entrants 04:46:41 / 04:46:52, **19:30** (+10 s). **2 lignes `sms-daily-proof` `ERROR` « INDETERMINEE »** à 02:45 et 04:45 (« 1 message sur 1 n'a jamais quitté son statut de soumission après 15 min ») — **exactes à l'instant où elles sont écrites, jamais refermées ensuite** | ⚠️ **6 preuves sur 6 remises, dont 2 en retard** ; 🆕 TRK-089 (b) |
| 2 | **Sentinelle Android** muette 24 h | **0 ligne sur 20 h** — mais **9 `CRITICAL`** le 19/09 : 02:29 `STALE` (305 s), puis `OFFLINE` à 02:45, 03:00, 03:16, 03:32, 03:48, 04:04, 04:20, 04:36 (1 265 → 7 925 s, `lastSeen` figé à **02:23:55**), `relayQueueDepth` 390 → 392 ; **aucune ligne de retour** ; e-mails « erreurs critiques — sms-gateway-watchdog » à 02:30, 03:40, 04:40 | 🔴 **Un épisode de 2 h 22 hors ligne** (cause côté téléphone, T55) ; 🆕 TRK-089 (a) : 9 lignes pour une cause |
| 3 | **Le téléphone ping** toutes les 60 s | `lastSeen` **01:20:43** (lu à 01:21:41 → 58 s) puis **01:22:43** (lu à 01:23:15 → 32 s) — deux valeurs, 120 s d'écart | ✅ **OK** (revenu depuis le 19/09 04:46) |
| 4 | **Conditions de l'interlock** | `GET /api/admin/sms/status` non lu *(pas de jeton)*. **Par le comportement** : **59 `CUT` `SCHEDULER`** (18/09 18:00 ×4, 20:00–20:06 ×25 ; 19/09 18:00 ×4, 20:00–20:06 ×26) **toutes passées**, **0 ligne `engine-control-interlock`** (dernière : 16/09 02:24) ; dernière remise terminale avant le soir du 19 : 19:30:10 | ✅ **VERT — par le comportement, 59 fois, nuits 3 et 4** *(la preuve remise à 04:46 a suffi : sans elle, 18:00 était à 22 h 30 de la preuve du 18/09 19:30 et 20:00 à 24 h 30 — l'interlock aurait retenu les 26 coupes CDEF31)* |
| 5 | **Aucune RESTORE non prouvée** qui traîne | **0 ligne** (< 24 h). **Reprises du 19/09** : 03:00:00–03:00:09 **4 MH Cars** ACK TCP 0,8–7,5 s ; 05:00:01–05:01:00 **27 CDEF31** ACK TCP 0,4–5,3 s (HM-769-GA 0,6 s, HD-584-BF et BP-434-RD « TCP seul » 4,5 s chacun) ; HM-787-GA manuelle 05:07:47 (1,0 s) **puis `SCHEDULER` 05:09:00 (0,4 s)** — un doublon 73 s après le geste humain, motif de TRK-084 point 4. **0 SMS, 0 `SENT_UNCONFIRMED`, 0 relance** sur 60 h | ✅ **OK, 35/35** |
| 6 | **Véhicules « TCP seul »** (T62) | **3 lignes** `engine-control-tcp-only` : HD-584-BF (18/09 20:03, 19/09 20:03), BP-434-RD (19/09 20:00 ; *pas de ligne le 18/09 — refroidissement 24 h à cheval sur 20:00:20 → 20:00:20*), `streak 3`, dernier échec **17/09 06:01** ; coupés par TCP les deux nuits (0,7 / 4,0 s ; 4,7 / 2,2 s), remis en route par TCP le 19/09 05:00 | ✅ **OK** — liste inchangée (HD-584-BF, BP-434-RD) |
| 7 | **Quelqu'un a été prévenu** (T69) | 19/09 : **`passerelle-sms` 2 `SENT`** à 02:29, 03:32, 04:36 (refroidissement 1 h) et **`preuve-sms` 2 `SENT`** à 02:45 et 04:45 (+ 1 `FAILED no_device` tyger.bcn et 1 `SUPPRESSED preference_disabled` à chaque fois) ; `agent-local-absent` 2 `SENT` à 18/09 02:50, 03:50 et 19/09 03:50 ; **0 `coupe-retenue`, 0 `restore-non-prouvee`** (rien à dire) | ✅ **OK — T69 (3) « téléphone hors ligne » et (4) « preuve KO » exercés pour la première fois** : à 04:29 Paris le propriétaire savait que le S21 était mort |

**Bilan : 5 OK · 1 ⚠️ (preuves remises en retard, verdict jamais refermé) · 1 🔴 (téléphone hors ligne 2 h 22).** Le mécanisme
a tenu de bout en bout : *l'interlock aurait retenu les coupes de 20:00 si le téléphone n'était pas revenu avant 04:47 — et
c'est exactement ce qu'on lui demande.* Ce qui manque est **le récit** : à 07:00 Paris, l'écran disait « preuve indéterminée,
coupes de ce soir menacées » alors que la preuve était remise depuis deux heures.

### Ce que les deux nuits ont fait

| Heure UTC | Fait | Lecture |
|---|---|---|
| 18/09 ~08:30 | **HM-769-GA retrouve son fix** (dernière ligne TRK-086 à 08:01 : 15 `ERROR` au total du 17/09 18:29 au 18/09 08:01, une par passage ; silence depuis) | ✅ la boucle TRK-086 s'éteint d'elle-même — **le défaut reste** (elle repart à la prochaine fenêtre vide) |
| 18/09 08:41 → 14:39 | SMS entrants « C'est bon la nouvelle voi[ture] », « Power: ON Bat: 0 % GPRS: OFF », « Bat: 17 % GPRS: ON », « Parfait merci ! » ; `CUT`/`RESTORE` manuelles **HM-787-GA** 14:16 (4,7 / 0,5 s) | 🔵 mise en service d'un boîtier sur HM-787-GA (véhicule et planning existants depuis juin/juillet) — **CDEF31 passe à 26 coupes le 19/09** |
| 18/09 12:45 | `gps-integrity` **HD-686-QX** « sans position depuis 2 h » (zone morte nº 1), `GPS_LOST` 2 `SENT`, acquittée ; position à 20/09 01:14 | 🔵 TRK-001, épisode clos |
| 18/09 16:29 | `TRIP_AUTOMATION` **GS-187-NY** « aucune position entre 15:07 et 15:35, un trajet brut y subsiste » — vrai trou (positions avant et après), 1 ligne | ⚪ TRK-047 fait son travail |
| **18/09 18:00:01 → 18:00:50** | **4 `CUT` MH Cars**, ACK TCP 3,1 / 5,2 / 4,0 / 0,4 s | ✅ |
| **18/09 20:00:00 → 20:06:06** | **25 `CUT` CDEF31** (+ HM-769-GA, 3,2 s), ACK TCP 0,4–6,1 s ; puis `RESTORE` manuelles HD-603-XY 20:06:13 (1 min 43 après sa coupe), GS-187-NY 20:35 (CUT + RESTORE), HM-787-GA 22:33 (CUT + RESTORE, puis CUT), **HM-733-GA 22:34 (4ᵉ nuit sur 4)** | ✅ **nuit 3 probante** ; 🔵 plannings |
| 19/09 **02:23:55** | Dernier ping du S21 ; 02:29 `STALE`, 02:45 → 04:36 `OFFLINE` ×8 ; preuves 02:30 et 04:30 **en file** ; 02:45 et 04:45 « INDETERMINEE » | 🔴 T55 ; 🆕 TRK-089 |
| 19/09 03:00:00 → 03:00:10 | **4 `RESTORE` MH Cars** ACK 0,8–7,5 s (EP-047-TY 7,5 s) — *pendant que le téléphone est hors ligne : le TCP suffit* | ✅ |
| 19/09 **04:46:41 / 04:46:53** | Le S21 revient ; le relais pousse `delivered` sur les deux preuves, échos reçus — **aucune ligne** ne le dit | ⚠️ TRK-089 (b) |
| 19/09 05:00:01 → 05:01:00 | **27 `RESTORE` CDEF31** ACK TCP 0,4–5,3 s ; 05:07:47 HM-787-GA manuelle ; **05:09:00 HM-787-GA `SCHEDULER` « entrée dans la plage »** pour un véhicule remis en route 73 s plus tôt | ✅ 27/27 ; ⚠️ 1 doublon (TRK-084 pt 4) |
| 19/09 14:12 | Audit du centre d'alerte lancé en rattrapage, interrompu 2 min plus tard | ⚪ pas de rapport du 19 |
| 19/09 **16:36:28** | `deploy.sh --attendre` `8e289f38` : 7 037 s d'attente (passage 15:45 → 16:36, 51 min), `healthy` ; passage 16:45 en 19 min | ✅ **T29, 3ᵉ preuve** |
| 19/09 18:00:01 → 18:00:52 | **4 `CUT` MH Cars** ACK 1,0–4,0 s ; 19:23 : `RESTORE` manuelles FV-941-LZ (puis re-`CUT` 19 s après) et **EP-047-TY ×2 à 5 s d'écart** (double clic) | ✅ ; 🔵 MH Cars aussi rallume à la main |
| 19/09 **18:30:17** | **Second `docker logs` sans `timeout`** (PID 2060212, `--tail 500 ae54b0ff3f79`) ; 18:40 : `%idle` **37 → 7 %** | 🔴🔴 **VPS-016 nº 7** |
| 19/09 19:30:00 | Preuve de 21:30 Paris `delivered` +10 s, écho +10 s | ✅ T69 (1), 4ᵉ soir |
| 19/09 **20:00:01 → 20:06:22** | **26 `CUT` CDEF31** (+ HM-787-GA), ACK TCP 0,4–5,6 s ; manuelles : GS-187-NY `RESTORE` ×2 20:07 (double clic), HM-787-GA `RESTORE` 21:05 puis `CUT` 21:05, **HM-733-GA `RESTORE` 21:05 (5ᵉ nuit sur 5)** | ✅ **nuit 4 probante** |
| 19/09 22:10 → 20/09 01:10 | `%steal` **21 → 46 → 72 → 89 %** ; 00:00:26–00:02:20 Vizyo Auth injoignable (503 rendu en 57 s) ; 00:04 Vizyo Auth répond | 🔴🔴 V33 + nº 7 |
| *20/09 02:30 · 03:00 · 05:00* | preuve 04:30 Paris ; 4 `RESTORE` MH Cars ; 27 `RESTORE` CDEF31 — **sur une VM à 10 % de CPU** | 👉 **à lire à 07:10 Paris** |

*Ce qui n'a PAS eu lieu :* **0 refus d'interlock, 0 `SENT_UNCONFIRMED`, 0 SMS de commande (60 h), 0 rappel T51, 0 ligne
`engine-control-restore`, 0 `coupe-retenue`.** `schedule_history` 60 h : 87 `CUT`, 60 `RESTORE`.

---

## Chiffres

| | |
|---|---|
| Lignes `error_logs` **actives** | **309** — 175 défauts + 134 `DEGRADATION` (**23** nées sur 24 h, 23 actives, **15** défauts ; **45** nées depuis la collecte du 18/09, **45** actives) |
| dont `CRITICAL` actives | **57** *(45 → 57)* — 25 `engine-control-interlock` *(nuit 15→16, TRK-081)* + **10 `sms-gateway-watchdog`** *(1 → 10, 🆕 TRK-089)* + **9 `agents-locaux`** *(6 → 9, TRK-069)* + 5 `engine-control-restore` + 4 `sms-gateway-status` + 2 `TRIP_AUTOMATION` + 2 `http` |
| Lignes archivées | **88** *(inchangé — aucune résolution automatique en 48 h)* |
| Constats de disparition | **1** *(celui du 04/09 08:25, aucun nouveau depuis **379 h**)* — `temoin_arme` rend ses **4 lignes**, toutes `actif = O` |
| Écart `ins − del − live` (`error_logs`) | **13 250** — inchangé, **21ᵉ point consécutif** |
| Trackers FAILING | **0** |
| Trackers hors ligne > 1 h | **10** (0 jamais mis en service) — 5 déclarés + 3 de stock + **2 NON déclarés** *(GLA•KC•31 407 h, FG-669-DQ 323 h)* ; *FZ-862-VY ne figure plus dans `hors_service` (6 lignes, 7 avant)* |
| Commandes en attente > 10 min | **0** — *6ᵉ collecte de suite* |
| Commandes échouées / jour (7 j) | **21,4** — total famille `fix_continuous` **441** *(432 → 441)* |
| Acquittements matériels | **0 sur 441** — **20ᵉ point** |
| Cadence sous le minimum matériel | **0** sur 44, cible la plus basse **20 s** — **17ᵉ point consécutif** |

### 264 → 309 : décomposition exacte

| Mouvement | Lignes |
|---|---:|
| Nées après la collecte du 18/09 (01:26) et **encore actives** | **+45** — **9 `sms-gateway-watchdog`** *(🆕 TRK-089)*, **8 `TRIP_AUTOMATION`** *(7 HM-769-GA jusqu'à 08:01 — TRK-086 ; 1 GS-187-NY — TRK-047)*, 7 Overpass *(TRK-037)*, **10 `sentinelles`** *(5 `ERROR` + 5 `DEGRADATION`, ci-dessous)*, 3 `engine-control-tcp-only` *(T62)*, **3 `agents-locaux` `CRITICAL`** *(TRK-069)*, **2 `sms-daily-proof`** *(🆕 TRK-089)*, **2 `http`** *(TRK-068)*, 1 `gps-integrity` *(TRK-001)* |
| Archivées | **0** |

264 + 45 − 0 = 309. **Aucune ligne non expliquée.**

---

## 🔴 À traiter

### 🔴🔴 V33 + VPS-016 nº 7 — deux clients Docker bloqués, les deux cœurs pris, et l'hyperviseur reprend 89 % du CPU

*Hors centre d'alerte, mais c'est ce qui menace les reprises de ce matin.* Mesuré à 01:17–01:18 :

| Fait | Mesure |
|---|---|
| Client nº 6 (V33) | PID 3366708 `docker logs --tail 15 vizyo-auth-api`, parent `bash -c` 3366707 → `init`, **304 576 s = 84 h 37**, toujours vivant |
| **Client nº 7** | **PID 2060212 `docker logs --tail 500 ae54b0ff3f79`** (= `foodsqan-traefik`), parent `bash -c` 2060192 → `init` (« === PROXY/ROUTEURS === … `docker ps --filter ancestor=traefik` … »), né le **19/09 18:30:17** — **4 min après le déploiement marketing** (`ad0abff4` 18:26:48) |
| Effet immédiat | `sar` 19/09 : `%idle` **37 % à 18:30 → 7,3 % à 18:40**, `%system` 35 → 53 %, jusqu'à 22:00 — le second cœur est parti avec le second client |
| Effet différé | `%steal` **2,8 % (22:00) → 20,8 % (22:10) → 45,8 % (23:10) → 67,5 % (00:10) → 72,9 % (01:00) → 88,7 % (01:10)** ; à 01:18 `vmstat` : user 6, system 4, **steal 90**, idle 0 ; `/proc/loadavg` **43 → 56** en une minute ; `dockerd` 45 % instantané, **35,4 % de moyenne depuis 46 jours** (928 Mo de RSS) |
| Conséquence lue | Vizyo Auth injoignable 00:00:26 → 00:02:20 (TRK-068), réponse 503 rendue **57 s** après un délai de 8 s ; passage 23:45 en 19 min, 00:45 en 8 min (3 à 5 min les nuits d'avant) |

**Cause racine, en une phrase.** Un `docker logs` lancé depuis le poste sans `timeout` ne rend jamais la main sur cet hôte
(VPS-016, écrit depuis le 05/08, **7 occurrences sur 7 depuis le poste**) ; le 7ᵉ a pris le cœur que le 6ᵉ laissait, et une VM à
2 × 100 % pendant 3 h 40 se fait reprendre son CPU par l'hôte — que ce soit un voisin bruyant ou une limitation d'équité, **le
seul levier est le nôtre**. *Le steal a démarré 3 h 40 après le second client ; s'il retombe dans l'heure qui suit les deux
`kill`, c'est bien une limitation ; sinon, c'est l'hébergeur — et la mesure vaut d'être faite.*

**Geste (VPS, 10 s, humain — garde-fou nº 4)** : `kill 3366707 2060192` (les PARENTS d'abord, puis les enfants s'ils survivent),
`pgrep -x docker` vide, `dockerd` sous 5 %, puis `sar -u 1 30` : `%steal` doit redescendre. **Et un garde-fou côté poste** (🆕 V34) :
aucun `docker logs` sans `timeout 20` et `--tail ≤ 2000`, écrit dans `CLAUDE.md` et dans la mémoire des agents — c'est le poste
qui produit ces clients, pas le VPS.

### 🆕 TRK-089 — la sentinelle du téléphone crie toutes les 16 min, et la preuve remise en retard ne referme jamais son verdict

**Signatures** : `sms-gateway-watchdog | CRITICAL | Passerelle SMS indisponible : téléphone <ÉTAT> : dernier ping il y a <N> s (seuil
<N> s). Les coupes automatiques sont bloquées par sécurité ; les RESTORE restent prioritaires.` (**9 lignes** 02:29 → 04:36 le 19/09,
une par balayage de 16 min, **3 e-mails**) et `sms-daily-proof | ERROR | Preuve SMS quotidienne INDETERMINEE : <N> message(s) sur <N>
n'ont jamais quitté leur statut de soumission après 15 min. […] Sans remise prouvée sous 24 h, l'interlock refusera les coupes
automatiques de ce soir.` (**2 lignes**, 02:45 et 04:45).

**Ce qui a été mesuré.** Le S21 a cessé de pinger à **02:23:55** et est revenu entre 04:36 et **04:46** (2 h 22). Pendant l'épisode,
le relais a gardé en file les preuves de 02:30 et 04:30 ; à 04:46:41 et 04:46:53 elles sont parties, remises, **écho entrant reçu**
(`statusUpdatedAt` poussé par le relais — T45). L'interlock lit `lastTerminalSuccessAt` : à 18:00 et 20:00 il était **vert grâce à
ces deux remises** (la preuve précédente, 18/09 19:30, aurait eu 24 h 30 à 20:00). **Trois défauts dans la même nuit :**

1. **9 `CRITICAL` pour une cause** — le motif exact de [TRK-081](../REFERENCE-ERREURS.md#trk-081) (T68, « une ligne par cause et
   espacée ») sur une seconde source : `sms-gateway-watchdog` réécrit à chaque balayage, et l'anti-flood de `ErrorLogger` (60 s)
   ne les fond pas (16 min d'écart). 3 e-mails « N erreurs critiques » à 02:30, 03:40, 04:40 pour le même téléphone.
2. **Aucune ligne de retour** : le téléphone est revenu à 04:46, rien ne l'écrit — la 9ᵉ `CRITICAL` (04:36, « 7 925 s ») reste
   la dernière parole du sujet. Un lecteur à 07:00 Paris ne sait pas que l'épisode est clos.
3. **Le verdict `INDETERMINEE` est définitif à +15 min** : `verify` tourne une fois (02:45, 04:45) et conclut « l'interlock
   refusera les coupes de ce soir » ; quand le relais pousse `delivered` deux heures plus tard, **personne ne relit le verdict**.
   La ligne est juste au moment où elle est écrite et **fausse dès 04:47** — famille *mensonger par omission* : elle envoie
   préparer une preuve manuelle pour un soir qui n'en avait plus besoin.

**Cause racine, en une phrase.** Deux témoins écrits pour *l'instant* (un balayage, une vérification à +15 min) sur un état qui
*dure* et se *résout* — sans ligne d'entrée unique, sans rappel espacé, sans ligne de sortie, et sans relecture du verdict quand la
donnée qu'ils jugent change. Famille **à lire, pas à notifier**, gravité **3** *(le mécanisme a tenu ; c'est le récit qui ment)*.

**Correctif proposé** *(sans toucher au watchdog ni à l'interlock)* :

1. `sms-gateway-watchdog` : **une** `CRITICAL` à l'entrée en `STALE`/`OFFLINE` (avec `lastSeen`), rappels `DEGRADATION` **horaires**
   tant que ça dure, et **une ligne de sortie** « téléphone revenu après H h MM (hors ligne de HH:MM à HH:MM) » qui **archive** les
   lignes de l'épisode (`resolvedAt`, comme T74 pour le heartbeat) — même dessin que T68.
2. `sms-daily-proof` : quand le relais pousse un statut terminal sur un message `gateway_daily_proof` **après** la vérification,
   écrire **une** ligne « preuve de 04:30 finalement remise à 04:46 (+2 h 16) — interlock vert » et archiver la ligne
   `INDETERMINEE` correspondante ; à défaut, relire le verdict à +60 et +120 min.
3. Les pushs T69 (3) et (4) sont **justes** (1 par heure, 1 par preuve) : ne pas y toucher.

**Vérification (double condition)** : au prochain épisode hors ligne, **1 `CRITICAL` + N `DEGRADATION` horaires + 1 ligne de
sortie**, les `CRITICAL` archivées au retour, **et** la notification `passerelle-sms` part toujours dans le quart d'heure ; à la
prochaine preuve remise en retard, **une** ligne qui le dit, et la ligne `INDETERMINEE` archivée.

### 🔴 TRK-069 / T75 — les agents du poste sont en pause depuis 3 jours, et la vérification quotidienne ignore la pause

`pauses_agents_locaux` : pause `echecs-consecutifs` posée le **17/09 08:50**, `leveeA` **NULL**. `passages_agents_locaux` 60 h :
**26 `rattrapage-recits` « suspendu »** (toutes les 2 h), `agent-recit-trajet` suspendu 18/09 05:19 et 19/09 04:30, `courrier-ia`
« 0 travail pris » ×3 ; `agent-limites-vitesse` 10/10 et `agent-qualite-gps` 2/2 tournent. **0 récit depuis le 16/09 22:00 — 75 h.**
**3 nouvelles `CRITICAL`** : 18/09 02:50 « dernier passage en échec », 18/09 03:50 et 19/09 03:50 « **passage manqué** :
agent-recit-trajet attendu à 03:15 — en pause » — **la vérification « manqué » du quotidien écrit une `CRITICAL` par jour et
pousse 2 notifications (`agent-local-absent` `SENT` ×2) alors que la cause est la pause, déjà dite en `DEGRADATION`** : T34 promettait
« agents suspendus sans nouvelle `CRITICAL` » — c'est vrai pour les passages, faux pour le contrôle quotidien (reste de T58 : la
clé de refroidissement ne porte pas la cause « pause »). Le geste est humain : `claude auth login` sur le poste, puis
« Reprendre maintenant » ; et 4 jours de récits à rattraper.

### 📏 TRK-078 / T30 — 1 paire de doublons le 18/09, et « au plafond 130 pile » était encore trop étroit

48 h : **8 `OVERSPEED`**, toutes acquittées, **6 avec `startAt`**. **FV-941-LZ 141,7 km/h** : alerte à **07:48:51 sans instant**
(trajet `bfadd2a4`, 06:16–07:34, créé 07:45) et à **09:18:30 avec instant** (trajet `d96d1397`, 07:07–07:58, créé 08:58 par le
recalcul) — trajets chevauchants, même pointe : **doublon**. EZ-259-DB 130 (11:06, sans instant) et 129 / limite 80 (12:13, avec
instant) sont deux excès distincts. **Rectification** : ce n'est ni `absolu` (16/09) ni « 130 pile » (17/09) qui prive la pointe
d'instant — **141,7 n'a pas d'instant non plus** ; la première alerte d'un trajet fraîchement segmenté part sans `startAt`, et la
clé de T30 ne peut alors pas la rapprocher de la suivante. Série depuis le correctif : 14/09 0 · 15/09 0 · 16/09 **2 / 9** · 17/09
0 / 12 · 18/09 **1 / 6** · 19/09 0 / 2. Le complément (1) de la fiche se reformule : **donner un instant à toute pointe**, et
dédupliquer sur (véhicule, vitesse max arrondie, chevauchement de trajets) quand l'instant manque.

---

## 🟢 Ce qui s'est prouvé depuis le 18/09

| Tâche | Preuve |
|---|---|
| **T29** (garde relue avant `up -d`) | 3ᵉ preuve : `journal.jsonl` 19/09 **16:36:39**, `dureeS` **7 037**, `attente: true`, `sante: healthy` ; passage 15:45 `done` en 51 min, recréation à 16:36:28 ; 48 passages `done` / 0 `interrupted` sur 2 jours |
| **T62** (SIM injoignable = TCP seul) | 3 lignes en 2 nuits, `streak 3`, dernier échec inchangé (17/09 06:01) : **0 SMS du planificateur vers ces SIM** depuis le verdict ; coupés et remis en route par TCP |
| **T65** (les 24 h de preuve) | 4ᵉ lecture (16, 17, 18, 20/09) — 7 verdicts, deux nuits |
| **T69** (notifications) | (1) preuve 19:30 4 soirs sur 4 ; **(3) `passerelle-sms` ×3 et (4) `preuve-sms` ×2 exercés le 19/09** — les deux causes bloquantes restantes ont poussé |
| **T45** (relais pousse ses statuts) | 6 preuves / 6 avec `statusUpdatedAt`, **y compris les deux remises en retard** (04:46:41 / 04:46:53) — c'est ce report qui a rendu l'interlock vert |
| **T28** (mesure du recalage) | 16/09 **216 / 216 / 157 / 0 / 0** (3ᵉ point, figé) ; 17/09 figé **186 / 186** ; 18/09 figé **232 / 232** (152 à la clôture, **232 / 232 depuis la création**) ; 19/09 ouvert 197 / 196 |
| **T40** (clé RESTORE) | RESTORE créées le 19/09 pour les 31 véhicules coupés la veille, puis CUT le soir |
| **TRK-068** (message Vizyo Auth) | 2 lignes 20/09 00:00:26 / 00:01:36 : « Vizyo Auth est injoignable … 503 … aucune réponse en 8 s » — message et code **exemplaires** ; 2ᵉ date à minuit (12/09, 20/09), cette fois sous 72 % de steal — *deux points, toujours pas trois* |
| **TRK-082** (T70) | 2ᵉ faux positif : « 1 trajet de mh cars a un excès sans alerte » (18/09 06:30, `8bfc7f77` FV-941-LZ 17/09 07:21–07:43, max 127,03) — **l'alerte existe** (17/09 07:45, 127 km/h) sur l'ancien `tripId` |

---

## 🟠 Connu, correctif en attente

| Fiche | État au 20/09 |
|---|---|
| [TRK-066](../REFERENCE-ERREURS.md#trk-066) *(T38 · T65 · T69)* | 🟢 **nuits 3 et 4 probantes** : 59 coupes / 59 ACK TCP ≤ 6,1 s, 35 reprises ≤ 7,5 s, 0 SMS, 0 refus ; **HM-769-GA coupé et remis en route** (fix retrouvé) ; le 19/09 l'interlock a tenu sur une preuve remise 2 h en retard |
| 🆕 [TRK-089](../REFERENCE-ERREURS.md#trk-089) | gravité 3 — 9 `CRITICAL` watchdog pour un épisode, 0 ligne de retour, verdict `INDETERMINEE` jamais refermé ; 👉 **T76** |
| [TRK-084](../REFERENCE-ERREURS.md#trk-084) *(T72)* | non exercé (API vivante) ; **point 4 exercé une 7ᵉ fois** : HM-787-GA `SCHEDULER` 05:09:00, 73 s après une RESTORE manuelle acquittée |
| [TRK-085](../REFERENCE-ERREURS.md#trk-085) *(T73)* | exercé à 00:02:20 : 3ᵉ appel Vizyo Auth (req 2248) fondu dans la ligne de 00:01:36 (44 s) — 2 lignes pour 3 refus |
| [TRK-086](../REFERENCE-ERREURS.md#trk-086) *(T74)* | **15 lignes** au total (17/09 18:29 → 18/09 08:01), éteinte par le retour du fix — le test daté « 0 ligne tant que sans fix » est sans objet ; le défaut attend la prochaine fenêtre vide (HM-733-GA 65 trajets bruts, HM-779-GA 39) |
| [TRK-083](../REFERENCE-ERREURS.md#trk-083) *(T71)* | **0 `SENT_UNCONFIRMED`** sur 94 commandes en 60 h — non exercé ; **ce matin la base tourne sous 89 % de steal** |
| [TRK-081](../REFERENCE-ERREURS.md#trk-081) *(T68)* | 0 ligne interlock ; **même motif rejoué par le watchdog** (TRK-089 (a)) |
| [TRK-078](../REFERENCE-ERREURS.md#trk-078) *(T30)* | 1 paire / 6 le 18/09 ; rectification : toute pointe peut naître sans instant |
| [TRK-069](../REFERENCE-ERREURS.md#trk-069) *(T34 · T58 · T75)* | 🔴 pause depuis 17/09 08:50, **75 h sans récit**, 9 `CRITICAL` (+3 « manqué » malgré la pause) |
| [TRK-072](../REFERENCE-ERREURS.md#trk-072) | 18/09 06:30 : admin@ et rougie31@ **15** notifications / 24 h dont **11 `OVERSPEED`** (12 alertes le 17/09) — type saturant redevenu `OVERSPEED` |
| [TRK-065](../REFERENCE-ERREURS.md#trk-065) | hebdo 18/09 : **12** non remises (tyger.bcn), `comptesTechniquesEcartes: 1` — 21 → 12 |
| [TRK-053](../REFERENCE-ERREURS.md#trk-053) *(T14 · T9)* | `alertes_depuis_declaration` = **2** (3 → 2 ; FS-808-CE) ; ⚠️ **la sentinelle « boîtiers muets » (hebdo cdef31, 19/09 06:30) liste 4 véhicules tous DÉCLARÉS hors service** (HD-292-SH immobilisé, DZ-034-CA, FS-253-HR, FS-808-CE débranchés) — jumeau de TRK-053 : la déclaration ne fait pas taire cette sentinelle non plus (8 le 06/09, 5 le 12/09, 4 le 19/09) |
| [TRK-071](../REFERENCE-ERREURS.md#trk-071) *(T1)* | Anthropic à sec depuis le 03/09 00:13 — **409 h, 18ᵉ jour** ; **0 appel IA en 72 h, tous canaux** (agents en pause) |
| [TRK-077](../REFERENCE-ERREURS.md#trk-077) *(T29 · T66)* | ✅ T29 3ᵉ preuve ; T66 : passage `perimetre: marketing` journalisé (`attente: false`, pas de recréation API) |
| [TRK-062](../REFERENCE-ERREURS.md#trk-062) *(T5)* | `commandes_en_attente` **0** *(6ᵉ collecte)* |
| [TRK-037](../REFERENCE-ERREURS.md#trk-037) | **81 `DEGRADATION` Overpass** (+7 : 4 HTTP 504, 3 « aborted ») |
| [TRK-016](../REFERENCE-ERREURS.md#trk-016) *(T28 · T13)* | ✅ 3 journées figées 100 % ; rattrapage non recompté ce jour |
| [TRK-075](../REFERENCE-ERREURS.md#trk-075) *(T26)* | 0 ligne `stage=compute` depuis le 08/09 15:55 — **273 h** |
| [TRK-079](../REFERENCE-ERREURS.md#trk-079) · [TRK-080](../REFERENCE-ERREURS.md#trk-080) · [TRK-070](../REFERENCE-ERREURS.md#trk-070) · [TRK-060](../REFERENCE-ERREURS.md#trk-060) · [TRK-066 message](../REFERENCE-ERREURS.md#trk-066) · [TRK-087](../REFERENCE-ERREURS.md#trk-087) · [TRK-088](../REFERENCE-ERREURS.md#trk-088) | Aucune ligne neuve |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) · [TRK-018](../REFERENCE-ERREURS.md#trk-018) · [TRK-027](../REFERENCE-ERREURS.md#trk-027) · [TRK-035](../REFERENCE-ERREURS.md#trk-035) | Inchangés |

---

## ⚪ Bruit / 🔵 Terrain

- **81 `DEGRADATION` `trip-analysis`** (Overpass) — TRK-037.
- **44 `DEGRADATION` `sentinelles`** (+5) — 18/09 06:30 : EP-047-TY (68 % de 19) et FV-941-LZ (50 % de 10) chez mh cars, HM-779-GA
  (56 % de 9) chez cdef31 ; 19/09 06:30 : GS-187-NY (**71 %** de 7) chez cdef31, **HD-779-MA (55 % de 11) chez « A2R »** et **« 10
  pointes de A2R refusées comme excès »** — *première parole des sentinelles sur la flotte A2R (4 véhicules, créée en mai) : 36
  analyses en 24 h, la flotte est active*.
- **23 `ERROR` `sentinelles`** (+5) — 18/09 : 1 « excès sans alerte » (TRK-082, faux positif), 2 « saturés » (TRK-072), 1 « 12
  notifications non remises » (TRK-065) ; 19/09 : « 4 boîtiers de cdef31 muets » (tous déclarés — TRK-053).
- **10 `gps-integrity`** (+1 HD-686-QX, clos) ; `gps_sans_fix` **vide** — HM-769-GA a retrouvé son fix.
- 🔵 **HM-733-GA rallumé à la main 5 nuits sur 5** (18/09 22:34, 19/09 21:05 UTC), GS-187-NY 2 nuits (20:35, 20:07 ×2), HD-603-XY
  (18/09 20:06), HM-787-GA (boîtier neuf, 4 gestes), **et MH Cars aussi** (EP-047-TY ×2, FV-941-LZ le 19/09 19:23) : **plannings à
  ajuster chez les deux clients** — chaque nuit = 3 à 6 gestes manuels, dont des doubles clics.
- 🔵 **2 boîtiers hors ligne NON déclarés** — `GLA•KC•31` (**407 h**) et `FG-669-DQ` (**323 h**).
- 🔵 **8 alertes sur 48 h** (6 `OVERSPEED` le 18/09, 2 le 19/09, 1 `GPS_LOST`), toutes acquittées ; T30 : 1 paire.
- ⚪ `notification_deliveries` 48 h : `ALERT` `OVERSPEED` 16 `SENT` / 16 `SUPPRESSED`, `GPS_LOST` 2 / 2 ; `SYSTEM` 24 lignes
  (`agent-local-absent` ×3, `passerelle-sms` ×3, `preuve-sms` ×2 — chacune 2 `SENT` / 1 `FAILED no_device` / 1 `SUPPRESSED`).
- ⚪ `email_logs` 60 h : **8** — 2 recette RDV (17/09 13:43), 2 « agents-locaux » (18/09 03:00, 04:10), « 7 erreurs en 1 h » (18/09
  06:40), **3 « erreurs critiques — sms-gateway-watchdog »** (19/09 02:30, 03:40, 04:40).

---

## Angles morts examinés

| Section | Mesure | Lecture |
|---|---|---|
| `temoin_arme` | **4 déclencheurs, tous `actif = O`** | Les comptes de ce rapport sont des **mesures** |
| `disparitions` | **1 constat**, celui du 04/09 — aucun nouveau | Rien n'a été supprimé depuis **379 h** |
| `comptes_disparition` | écart `error_logs` **13 250**, inchangé (**21ᵉ point**) ; `alerts` **0** | Pas de `TRUNCATE` |
| `retention_declaree` | `errorDeleted = 0` sur **5 nuits** ; **94 070** trames purgées le 19/09 03:00 | L'application ne supprime aucune erreur |
| `trackers_failing` | **0** | — |
| `commandes_sante_7j` | **0 acquittement matériel sur 441** — **20ᵉ point** ; 21,4 échecs/jour | Régime ordinaire |
| `commandes_motifs_7j` | **15 motifs, tous `fix_continuous`**, cible **demandée** citée | ✅ TRK-059 tient à **J+15** |
| `commandes_en_attente` | **0 ligne** *(6ᵉ fois)* | ✅ TRK-062 |
| `cadence_resume` | **0** sous le minimum, cible la plus basse **20 s** — **17ᵉ point** | TRK-057 / TRK-008 tiennent |
| `cadence_derive` · `cadence_reelle` | **0** ligne · **0** émetteur rapide, **1** mesure périmée sur 44 | — |
| `gps_sans_fix` | **0 ligne** — HM-769-GA a retrouvé son fix le 18/09 (~08:30) | TRK-086 éteinte, non corrigée |
| `hors_service` | **6 lignes** (7 avant : FZ-862-VY absent) ; `alertes_depuis_declaration` = **2** | ✅ TRK-053 ; ⚠️ sentinelle « muets » aveugle à la déclaration |
| `recalage_journees_closes` | 16/09 **216 / 216** (3ᵉ point) · 17/09 **186 / 186** · 18/09 **232 / 232** · 19/09 ouvert 197 / 196 ; **100 % < 2 h depuis la création** | ✅ T28 |
| `trip_automation_runs` | 18/09 **24 / 24** `done` (7 > 45 min) ; 19/09 **24 / 24** (7 > 45 min ; 15:45 = 51 min, la garde T29 l'a attendu) ; 20/09 00:45 = 8 min | ✅ ; VPS-M103 |
| `engine_control_commands` 60 h | **168 lignes** : 59 `CUT` + 35 `RESTORE` `SCHEDULER`, 19 `MANUAL` (11 RESTORE, 8 CUT) ; 168 `ACKNOWLEDGED` TCP, **0 SMS, 0 `SENT_UNCONFIRMED`** | ✅ nuits 3 et 4 |
| `vehicle_schedules` | cdef31 **26 `OUT_OF_WINDOW`** (éval. 19/09 20:02–20:06, HM-787-GA compris) ; mh cars 4 | 30 RESTORE dues ce matin + 1 (HM-787-GA) |
| `sms_logs` 60 h | **8 sortants** (7 preuves + 1 `stop123456` du 17/09), 8 avec `statusUpdatedAt` ; **17 entrants** (7 échos, 2 « Succeed », 3 `oil`, 4 trames de configuration HM-787-GA, 1 message humain) | ✅ T45 ; 🔵 boîtier neuf |
| `notification_deliveries` `SYSTEM` | **24 lignes** (3 causes × 8) | ✅ T69 (3) (4) exercés |
| `passages_agents_locaux` 60 h | **50 passages** : 12 réussis (sans modèle), **38 « suspendu » / « 0 travail »** | 🔴 TRK-069 / T75 |
| `pauses_agents_locaux` | 1 pause active depuis **17/09 08:50** (`leveeA` NULL) — **3 j** | 👉 geste humain |
| `ai_usage_logs` 72 h | **0 ligne** — aucun canal IA n'a été appelé | T1 · T75 |
| `journal.jsonl` · conteneur | 2 lignes le 19/09 (16:36 `8e289f38` 7 037 s ; 18:26 `ad0abff4` marketing) ; conteneur `3ec0c6d1` = `main`, `restarts=0`, `healthy` | ✅ T29 |
| **Clients Docker** *(sortie de passe)* | 🔴🔴 **2 vivants, aucun de cette passe** : PID 3366708 (V33, **84 h 37**) et **PID 2060212 (nº 7, depuis 19/09 18:30:17)** ; `%steal` **89 %**, charge **56 / 41 / 27** | 🔴🔴 **VPS-016 nº 6 et nº 7** — pas tués (garde-fou 4) |
| Fenêtre `docker logs tracky-api --tail 2000` *(sous `timeout 20`)* | **19/09 21:05:20 → 20/09 01:20:22** (4 h 15, 2 000 lignes) | *citée, pas supposée* |

---

## 🗓️ Tests datés — l'état au 20/09

| Fiche / tâche | Échéance | État |
|---|---|---|
| **Reprises du 20/09** *(T65 nº 5)* | 03:00 (MH Cars ×4) et **05:00 UTC (CDEF31 ×27)** — **sous 89 % de steal** | 31 `ACKNOWLEDGED` ≤ 15 min ; toute `SENT_UNCONFIRMED` = TRK-083 sous charge ; un SMS `resume` = le secours a servi |
| **V33 + nº 7** | dès le réveil | `pgrep -x docker` vide, `%steal` retombé dans l'heure — sinon c'est l'hébergeur |
| TRK-089 | prochain épisode S21 hors ligne / prochaine preuve en retard | 1 `CRITICAL` + rappels + sortie ; 1 ligne « finalement remise » |
| TRK-069 / T75 | dès la ré-authentification | `leveeA` posé, récits écrits, 9 `CRITICAL` archivées seules |
| TRK-078 *(T30)* | chaque matin | 0 paire ; `startAt` sur **toute** pointe |
| TRK-084 pt 4 | prochain redémarrage en plage | 0 `RESTORE` « entrée dans la plage » après une manuelle acquittée |
| TRK-068 | minuit | un 3ᵉ point à 00:00 tranche « logrotate » — mais celui du 20/09 est confondu avec le steal |
| TRK-086 | prochaine fenêtre vide (26 h sans fix) | la boucle repart tant que T74 n'est pas fait |
| TRK-075 *(T26)* | échu | paquet 54–56 j non relevé — à faire |

---

## 🤝 Ce qui attend un humain

0. 🔴🔴 **Tuer les DEUX clients Docker bloqués** *(V33 + nº 7)* : `kill 3366707 2060192` sur le VPS — 84 h et 7 h, **89 % de
   steal, charge 56**, les reprises de 05:00 UTC partent là-dessus. Puis `sar -u 1 30` pour trancher limitation / hébergeur.
1. 🆕 **V34** — plus jamais un `docker logs` sans `timeout` depuis le poste : ligne dans `CLAUDE.md`, mémoire des agents, et
   vérifier les scripts de diagnostic (« PROXY/ROUTEURS », « dernières lignes vizyo-auth-api ») — **7 sur 7 viennent du poste**.
2. 🔴 **Ré-authentifier la session Claude Code du poste** puis **« Reprendre maintenant »** *(T75)* — 75 h sans récit.
3. 🔴 **T55** — le S21 a dormi 2 h 22 dans la nuit du 18 au 19 (02:24 → 04:46 UTC) : secteur, écran, ou second téléphone.
4. 🆕 **TRK-089 / T76** — une ligne par épisode pour le watchdog, une ligne de sortie, et un verdict de preuve qui se relit.
5. **T38** — élargir : 4 nuits, 3 probantes, 59 / 59 ces deux nuits ; MH Cars 7 / 7 puis autres flottes.
6. 🔵 **Plannings** : HM-733-GA (5 nuits sur 5), GS-187-NY, HD-603-XY, HM-787-GA chez CDEF31 ; EP-047-TY, FV-941-LZ chez MH Cars ;
   `GLA•KC•31` (407 h) et `FG-669-DQ` (323 h) à déclarer ; sortir HD-292-SH / DZ-034-CA / FS-253-HR / FS-808-CE de la sentinelle
   « muets » (TRK-053).
7. 🔴 **Recharger le compte Anthropic** *(T1, 409 h)*.
8. ⚪ **Le rattrapage d'audit** : quand le poste dort, les trois audits partent ensemble au réveil (4ᵉ collision) et celui-ci a été
   interrompu — V6 (`WakeToRun`) reste la réponse.

---

## Fiches ajoutées ou mises à jour

| Fiche | Mouvement |
|---|---|
| 🆕 **TRK-089** | **Nouvelle** — S21 hors ligne 2 h 22 : 9 `CRITICAL` watchdog pour une cause, 0 ligne de retour, 2 verdicts `INDETERMINEE` jamais refermés alors que les preuves ont été remises à 04:46 ; gravité 3 ; 👉 **T76** |
| 📏 **TRK-066** | **nuits 3 et 4 probantes** (59 / 59, 35 reprises ≤ 7,5 s, 0 SMS) ; HM-769-GA coupé pour la première fois ; interlock vert sur une preuve remise en retard |
| 📏 **TRK-069** | pause **3 j**, 75 h sans récit ; **3 `CRITICAL` « manqué » malgré la pause** (reste de T34 / T58) |
| 📏 **TRK-078** | 1 paire / 6 le 18/09 ; **rectification** : toute pointe peut naître sans instant (141,7 aussi) |
| 📏 **TRK-082** | 2ᵉ faux positif mesuré (`8bfc7f77`, alerte présente à 07:45) |
| 📏 **TRK-086** | 15 lignes au total, éteinte par le retour du fix (18/09 08:01) — non corrigée |
| 📏 **TRK-068** | 2 lignes 20/09 00:00–00:01, message exemplaire ; 2ᵉ date à minuit, sous 72 % de steal |
| 📏 **TRK-084** | point 4 : HM-787-GA doublon `SCHEDULER` 73 s après une manuelle |
| 📏 **TRK-085** | 3ᵉ refus Vizyo Auth (00:02:20) fondu dans la 2ᵉ ligne |
| 📏 **TRK-053** | la sentinelle « boîtiers muets » liste 4 véhicules déclarés hors service (jumeau) |
| 📏 **TRK-077** · **TRK-062** · **TRK-071** · **TRK-072** · **TRK-065** · **TRK-037** | T29 3ᵉ preuve · 0 (6ᵉ) · 409 h · 15 / 11 `OVERSPEED` · 12 non remises · 81 |

---

## Contrôle de sortie

`pgrep -x docker` sur le VPS après la passe : **2 clients vivants — aucun de cette passe** : PID 3366708 (V33, né le 16/09
12:41:08) et PID 2060212 (né le **19/09 18:30:17**, parent `bash -c` 2060192 « === PROXY/ROUTEURS === »), tous deux 7 h et 84 h avant
ma première commande. Toutes les commandes `docker` de cette passe portaient `timeout` **et** une borne de volume (`exec` ×9 dont
6 `psql` en lecture seule et 3 `wget` du relais, `inspect` ×3, `images` ×1 *(n'a rien rendu sous `timeout 15` — le démon est
saturé)*, `ps -a --filter id` ×1, **un seul `docker logs`, `--tail 2000` sous `timeout 20`**, fenêtre mesurée et citée : 21:05:20 →
01:20:22) et ont **toutes rendu la main**. Je n'ai tué ni l'un ni l'autre (garde-fou nº 4). Aucune écriture en base, aucun
acquittement, aucun redémarrage ; la seule écriture sur le VPS est la copie de `docs/centre-alerte/` dans `/opt/tracky-centre-alerte`.
