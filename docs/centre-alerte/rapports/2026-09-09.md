# Audit du centre d'alerte — 2026-09-09

> Collecte : **2026-09-09 01:12 → 01:55 UTC**. Tous les horodatages de ce rapport sont en **UTC**,
> sauf les dates de `git log`, en heure de **Paris** (UTC+2) et signalées comme telles.
>
> ⚠️ **Un déploiement a eu lieu 8 heures avant la collecte, et il est le sujet de la journée** —
> `tracky-api` redémarré le **08/09 à 17:12:05** (`restarts=0`, `healthy`, image de 17:11:52),
> `tracky-web` sur une image de 17:10:58. **Quatrième déploiement non annoncé en quatre jours**,
> mais le premier à ne pas tomber dans le quart d'heure précédant l'audit : il a **8 heures et
> 8 passages horaires** de recul, ce qui rend ses effets lisibles au lieu d'indécidables.
>
> Trois autres images de la journée témoignent de deux bascules antérieures : `avant-console-demo`
> (00:54:13) et `avant-trace-sentinelle` (**06:16:52**). Tout ce qui est écrit ci-dessous sur
> l'artefact servi porte sur l'image de **17:11:52**.

**Verdict :** 138 lignes actives dont 69 défauts, et le fait du jour est qu'**une preuve écrite
d'avance est tombée au mot près** — le 08/09 porte **24 passages d'automatisation sur 24, dont UN
marqué « interrompu »**, là où le 06/09 en perdait huit sans laisser de trace ; dans le même
mouvement, le trajet qui criait toutes les heures s'est tu (**0 ligne sur 8 passages**) et la mesure
de [TRK-016](../REFERENCE-ERREURS.md#trk-016) a **cessé de mesurer ce qu'elle mesurait**.

---

## Chiffres

| | |
|---|---|
| Lignes `error_logs` **actives** | **138** — 69 défauts + 69 `DEGRADATION` (21 lignes sur 24 h, dont **13 défauts**) |
| dont `CRITICAL` actives | **7** *(+1 : la ligne « passage interrompu » du 08/09 05:45, **qui est un témoin, pas une panne** — voir ci-dessous)* |
| Lignes archivées | **38** *(+1 : la 6ᵉ `agents-locaux`, archivée **d'elle-même** le 08/09 à 16:50)* |
| Constats de disparition | **1** *(celui du 04/09, aucun nouveau depuis **120 h**)* — `temoin_arme` rend ses **4 lignes**, toutes `actif = O` |
| Écart `ins − del − live` (`error_logs`) | **13 250** — inchangé, **13ᵉ point consécutif** |
| Trackers FAILING | **0** |
| Trackers hors ligne > 1 h | **14** (0 jamais mis en service) — 9 déclarés + 3 de stock + **2 NON déclarés** |
| Commandes en attente > 10 min | **2** — les 2 SMS de [TRK-062](../REFERENCE-ERREURS.md#trk-062), à **179,6 h** et **183,0 h** |
| Commandes échouées / jour (7 j) | **16,3** — total famille `fix_continuous` **405** |
| Acquittements matériels | **0 sur 405** — **12ᵉ point** |
| Cadence sous le minimum matériel | **0** sur 44, cible la plus basse **20 s** — **9ᵉ point consécutif** |

### D'où viennent les 20 lignes de plus qu'hier — décomposition exacte

| Origine | Lignes | Niveau |
|---|---|---|
| [TRK-075](../REFERENCE-ERREURS.md#trk-075) — « Analyse impossible », **toujours le même trajet**, **et toutes ANTÉRIEURES au correctif de 17:12** | **12** | `ERROR` |
| `sentinelles` du 08/09 06:30 — vitesse contredite ×3 + pointes refusées ×1 | 4 | `DEGRADATION` |
| `trip-analysis` — Overpass injoignable ([TRK-037](../REFERENCE-ERREURS.md#trk-037)) | 3 | `DEGRADATION` |
| 🆕 **`TRIP_AUTOMATION` — « Passage d'automatisation interrompu »** *(témoin neuf, voir ci-dessous)* | **1** | `CRITICAL` |

118 + 20 = 138. **Aucune ligne non expliquée.**

---

## 🟢 CE QUI S'EST PROUVÉ AUJOURD'HUI

### ✅ T24 · TRK-073 — la ligne au départ : la preuve écrite d'avance est tombée au mot près

**La consigne datée du 08/09**, recopiée telle quelle de `taches.json` :

> *« Au prochain jour comportant un redéploiement, `trip_automation_runs` doit porter **24 lignes,
> dont une ou plusieurs MARQUÉES INTERROMPUES** — et non 16 lignes toutes en succès. Si le compte
> monte à 24 **sans qu'aucune ne soit marquée interrompue**, on a maquillé le carnet au lieu de le
> tenir. »*

**La mesure du 09/09 :**

| Jour | Passages | `done` | `interrupted` | `failed` |
|---|---:|---:|---:|---:|
| 04/09 | 19 *(tronqué, voir ci-dessous)* | 19 | 0 | 0 |
| 05/09 | 23 | 23 | 0 | 0 |
| 06/09 | **16** | 16 | 0 | 0 |
| 07/09 | 17 | 17 | 0 | 0 |
| **08/09** | **24** | **23** | **1** | 0 |

**Les deux moitiés du test sont franchies séparément** : le compte monte à **24 sur 24**, *et* la
ligne manquante n'a pas été fabriquée — elle est **marquée `interrupted`**, et elle correspond
exactement au passage de **05:45:00** que le déploiement de 05:45 a tué en vol.

Vérifié sur l'**artefact servi** (image de 17:11:52) : `running` **×7** et `interrupted` **×2** dans
`dist/trip-analysis/trip-automation.service.js`. *Hier, l'audit cherchait `EN_COURS` et le trouvait
absent — le correctif a livré la même idée sous un autre nom (`status = 'running'`, migration
`20260908053000`). ⚠️ **Chercher le mot d'une spécification plutôt que le comportement qu'elle
décrit fait manquer un correctif présent.***

#### 🆕 La signature neuve, et pourquoi elle n'ouvre PAS de fiche

```
TRIP_AUTOMATION | CRITICAL | Passage d'automatisation interrompu : commencé le <DATE>
(planifié), jamais terminé — l'API a redémarré <DURÉE> plus tard.
```
Contexte : `phase: 'interrompu'`, `runId`, `origin`, `minutes`, `startedAt`.

**C'est la voix du correctif, pas un défaut nouveau.** Elle est enregistrée ici pour que la
prochaine occurrence soit reconnue au lieu d'être ré-instruite, et elle est rattachée à
[TRK-073](../REFERENCE-ERREURS.md#trk-073).

⚠️ **Sa toute première ligne dit « 0 min » — et c'était un défaut de formulation, déjà corrigé.**
Le commit `3d6834b8` (08/09 07:51 Paris, *« un passage tué dans la minute dit "moins d'une minute",
pas "0 min" "»*) est postérieur de **six minutes** à la ligne. L'artefact servi porte la formulation
corrigée (`moins d'` ×3). *La ligne du 05:45 restera donc pour toujours la seule à dire « 0 min » :
c'est un fossile, pas une régression.*

🔴 **Conséquence à connaître, et elle est déjà traitée** — depuis `f988c74e`, **une `CRITICAL`
suffit à déclencher un e-mail**. Un déploiement pendant un passage horaire enverra donc désormais
une alerte à chaque fois. Le garde-fou existe : `deploy.sh` **refuse de partir** quand une ligne
`status = 'running'` est ouverte (`fd87ca46`). **Vérifié présent sur le VPS** dans
`/opt/vizyo-tracky/deploy/vps/deploy.sh`, arbre git **propre** au commit `578038d0`.
*Sans ce garde-fou, on aurait remplacé huit passages muets par un e-mail à chaque mise en ligne.*

#### ⚠️ Et le 04/09 à 19 n'est pas une baisse — c'est l'élagage annoncé hier

`trip_automation_runs` garde **100 lignes** (`KEEP_RUNS`). Le total des six journées présentes vaut
**exactement 100** : 1 + 24 + 17 + 16 + 23 + 19. Le 04/09 valait **22** quand il a été mesuré le
06/09 ; il vaut **19** aujourd'hui, **par le bas**. *La journée la plus ancienne de cette fenêtre
s'érode ; la lire comme une mesure fabriquerait une dégradation de toutes pièces.* C'est exactement
la mise en garde écrite hier, et c'est la première fois qu'elle sert.

### ✅ T26 · TRK-075 — le trajet du 8 juillet s'est tu, et le correctif ne l'a pas fait en éteignant le rejeu

**8 passages horaires depuis le déploiement de 17:12** (17:45 · 18:45 · 19:45 · 20:45 · 21:45 ·
22:45 · 23:45 · 00:45), **tous `done`**, et **zéro ligne** `stage: 'compute'`. La dernière remonte à
**08/09 15:55:05**. Auparavant : **une par passage, sans exception, depuis le 07/09 03:45** — 27 au
total, dont 12 dans les 24 h qui ont précédé le correctif.

Vérifié sur l'**artefact servi** : `PositionsIntrouvablesException` présente (`dist/common/`),
citée **×2** dans `trip-analysis.service.js`, et les deux marqueurs de gel `fige-retention` **×5** /
`fige-sans-positions` **×5** dans `trip-automation.service.js`. `horizonRetention` y apparaît **×7**
— *le plancher que la jumelle avait déjà.*

#### La double condition : **une moitié prouvée, l'autre non mesurable en lecture seule** — et c'est écrit

| Condition | État |
|---|---|
| Plus **aucune** ligne `stage: 'compute'` pour un trajet sous l'horizon | ✅ **0 sur 8 passages** |
| Les candidats au rejeu tombent de **17 à 14**, **sans** que `stats.rejouees` tombe à zéro | ⚠️ **non tranchable telle qu'écrite** |

La seconde condition a été **mal formulée hier**, et il faut le dire plutôt que de la déclarer
satisfaite : le correctif ne retire aucune ligne de `trip_analyses` — il **exclut** les candidats
sous l'horizon *au moment de la sélection*. Une requête en lecture ne voit donc pas « 14 » : elle
voit le **vivier**, qui a **grossi**.

| Mesure | 08/09 | 09/09 |
|---|---:|---:|
| Vivier (`limitsCoverage < 0,8` et `computedAt` > 12 h) | 17 | **20** |
| dont **sous** l'horizon de rétention | 3 | **4** |

**Le vivier grossit de 3 par jour et sa part immortelle de 1 par jour — exactement la fuite lente
annoncée hier.** Ce que le correctif change n'est pas le vivier : c'est que les 4 immortels ne sont
plus tentés. Et le rejeu **n'est pas éteint** : les 8 passages depuis la bascule ont analysé
**2 à 8 trajets chacun**, et **30 analyses** portent un `computedAt` postérieur au déploiement.

👉 **Consigne datée pour le 10/09** — le test qui tranche vraiment : **24 h pleines sans aucune
ligne `stage: 'compute'`**, pendant que le vivier sous horizon passe de **4 à ~5**. *Si les lignes
reviennent, le gel ne tient pas ; si le vivier sous horizon cesse de croître, c'est qu'on a purgé
au lieu de borner.*

---

## 🔴 À traiter

### 🆕 TRK-016 — la mesure du recalage a cessé de mesurer ce qu'elle mesurait

**Ce n'est pas une bonne nouvelle qu'on peut écrire telle quelle**, et c'est le seul point du jour
où le chiffre brut induit en erreur.

| Fenêtre 24 h | Trajets | Sans recalage | Taux d'échec |
|---|---:|---:|---:|
| 08/09 (hier) | 159 | 136 | **85,5 %** |
| **09/09 (ce jour)** | **186** | **0** | **0,0 %** |

**Témoin impossible posé avant de conclure** : `polylineMatched` vide = **0**, longueur **minimale**
**265** caractères, moyenne **20 188**, maximum **79 967**. Ce sont de vraies géométries, pas une
colonne remplie de vide. *Un 0 % obtenu par une colonne remplie de chaînes vides aurait la même
tête.*

**Mais la fenêtre n'a pas bougé — le PASSÉ, lui, a bougé.** Par jour civil :

| Jour | Trajets | Sans recalage |
|---|---:|---:|
| 04/09 | 202 | 168 *(83,2 %)* |
| 05/09 | 151 | 132 *(87,4 %)* |
| 06/09 | 93 | 82 *(88,2 %)* |
| **07/09** | 161 | **2** *(1,2 %)* |
| **08/09** | 186 | **0** |

🔴 **Le 07/09 rendait 85,5 % d'échec hier matin et rend 1,2 % ce matin, pour les mêmes trajets.**
Ils ont été recalés **après leur clôture** — par le rattrapage `recalerAnciensTraces` (15 par
passage, déployé à 17:12), par le recalage à la demande du rejeu (`c2afb01f`), ou par les deux.

> 🔑 **Conséquence méthodologique, et elle vaut plus que le chiffre :** un taux calculé sur
> `polylineMatched IS NULL` dans une fenêtre glissante **mélange désormais deux choses** — ce que le
> recalage fait **à la clôture** et ce que le rattrapage fait **rétroactivement**. La série de dix
> points de TRK-016 (91,3 · 87,0 · 89,3 · 91,1 · 91,9 · 89,2 · 83,3 · 87,9 · 87,2 · 85,5) **ne se
> prolonge pas par ce 0,0 %** : la définition a changé sous elle.

🔴 **Et la table ne permet pas de trancher** : `trips` porte un `createdAt`, **pas d'`updatedAt`**.
*Une colonne de qualité réécrite rétroactivement et sans horodatage rend toute tendance indécidable
— on ne peut pas distinguer « bien fait tout de suite » de « rattrapé la nuit suivante ».*

**Ce qui reste vrai et mesurable** — le stock d'historique, lui, ne se réécrit pas tout seul :

| Mesure | 09/09 |
|---|---:|
| Trajets de 2 à 60 jours | **10 032** |
| dont **déjà** recalés | **1 150** |

👉 **Consigne datée pour le 10/09** : à 15 trajets par passage et 24 passages, le rattrapage doit
porter ce chiffre à **~1 510**. *S'il ne bouge pas, le rattrapage ne tourne pas ; s'il bondit bien
au-delà, c'est qu'un autre chemin recale en masse et il faut le nommer.*

👉 **Et la tâche neuve qui en découle : [T28] redéfinir la mesure** — un taux de recalage doit se
prendre **à la clôture du trajet**, sur une fenêtre fermée, et non sur une colonne que le rattrapage
réécrit. *Sans ça, personne ne peut dire si T13 est fait.*

---

## 🟠 Connu, correctif en attente

| Fiche | État au 09/09 |
|---|---|
| [TRK-074](../REFERENCE-ERREURS.md#trk-074) *(T25)* | 🔴 **Toujours NON déployée** — « résolution automatique » **absente** de `dist/observability/scheduled-task-heartbeat.service.js`. Les **4 `CRITICAL` du 06/09** sont actives depuis **4 jours**, alors que la condition est close depuis le 06/09 20:45. **Sa jumelle, elle, a encore archivé toute seule** le 08/09 à 16:50 |
| [TRK-062](../REFERENCE-ERREURS.md#trk-062) *(T5)* | **Non déployé — 6ᵉ vérification négative** (`SENT_UNCONFIRMED` : 0 fichier dans `dist/tracker-commands/`). Les 2 commandes atteignent **179,6 h** et **183,0 h** — **+24 h par jour, exactement**, 4ᵉ point |
| [TRK-070](../REFERENCE-ERREURS.md#trk-070) · [TRK-068](../REFERENCE-ERREURS.md#trk-068) *(T10, T11)* | Déployés, **toujours non exercés** — `ASSISTANCE` inchangée depuis le 05/09 17:00 (**80 h**), `http CRITICAL` depuis le 04/09 12:57 (**108 h**) |
| [TRK-071](../REFERENCE-ERREURS.md#trk-071) *(T1)* | **Inchangée.** Anthropic à sec depuis le 03/09 00:13 — **145 h, 7ᵉ jour**. OpenAI constaté vide le 05/09 |
| [TRK-072](../REFERENCE-ERREURS.md#trk-072) *(T4)* | **Muette.** 49 remises de notification sur 24 h (29 `SENT`, 18 `SUPPRESSED`, 2 `FAILED`) — ⚠️ *mesure par **remise**, pas par destinataire : elle ne se compare pas au « 9 envois » d'hier, qui comptait autre chose* |
| [TRK-060](../REFERENCE-ERREURS.md#trk-060) · [TRK-065](../REFERENCE-ERREURS.md#trk-065) · [TRK-066](../REFERENCE-ERREURS.md#trk-066) | Déployés, **non exercés**. TRK-065 : ligne hebdomadaire attendue vers le **11/09** ; TRK-066 : toujours **1 seule** occurrence (04/09 03:00) |
| [TRK-076](../REFERENCE-ERREURS.md#trk-076) *(T27)* | ✅ **La preuve avance** : **0 ligne `getSource`** depuis le déploiement du 08/09 01:01 — **48 h**. Barre fixée à 7 jours, donc **échéance au 15/09** |
| [TRK-067](../REFERENCE-ERREURS.md#trk-067) · [TRK-043](../REFERENCE-ERREURS.md#trk-043) · [TRK-059](../REFERENCE-ERREURS.md#trk-059) | ✅ **Tiennent.** 0 ligne `system-activity` depuis le 04/09 16:55 (**5 j**) ; les 15 motifs de clôture citent tous la cible **demandée** (**J+4**) |
| [TRK-014](../REFERENCE-ERREURS.md#trk-014) · [TRK-018](../REFERENCE-ERREURS.md#trk-018) · [TRK-027](../REFERENCE-ERREURS.md#trk-027) · [TRK-035](../REFERENCE-ERREURS.md#trk-035) | Inchangés |

### ⚠️ Rectification d'une mesure d'hier — TRK-037

Le rapport du 08/09 écrit, en section *Bruit*, **« 62 `DEGRADATION` `trip-analysis` (Overpass) »**.
**62 était le total de TOUTES les `DEGRADATION` actives**, pas celui de la famille Overpass, qui en
valait **43**. Le compte juste ce jour : **46 `DEGRADATION` Overpass** *(+3 en 24 h, cadence
habituelle)* et **0 `ERROR`** — 16 lignes `ERROR` de cette famille existent encore mais sont
**archivées** et toutes antérieures au 26/08. **Le classement de TRK-037 tient, 8ᵉ point.**
*Un total pris pour une famille surestime la famille de 44 % ; c'est sans conséquence ici, et c'est
exactement pour ça qu'il faut le corriger tout de suite.*

---

## ⚪ Bruit / 🔵 Terrain

- **46 `DEGRADATION` `trip-analysis`** (Overpass HTTP 500/502/504) — dépendance tierce assumée,
  TRK-037. **3 lignes neuves** en 24 h.
- **4 `DEGRADATION` `sentinelles`** du 08/09 06:30 — vitesses contredites par la distance
  (« cdef31 » 23/82, « A2R » 8/27, « mh cars » 18/47) et **16 pointes refusées sur « A2R »**.
  ⚠️ **Fait mesuré, pas tendance** : fenêtre glissante de 24 h sur des journées différentes.
  🗓️ **Ces quatre lignes sont les DERNIÈRES de leur forme** : `8fa14cb4` (déployé à 17:12) fait que
  la sentinelle **ne désigne plus une flotte entière** mais **les véhicules qui sortent du lot**
  (≥ 5 analyses touchées **et** ≥ 50 % des leurs). **Test daté au 10/09 06:30** : le message doit
  commencer par « *N véhicule(s) de « … » annonce(nt) une vitesse …* » et porter
  `vehiculesHorsNorme` au contexte. *Et il doit rester possible qu'elle se taise : une flotte sans
  véhicule hors norme ne doit plus produire de ligne du tout.*
- **5 `gps-integrity`** actives — `FG-669-DQ`, `HD-603-XY`, `GS-928-NX`, `GLA•KC•31`. Action terrain.
- 🔵 **2 boîtiers hors ligne NON déclarés** — `GLA•KC•31` muet depuis le **03/09 01:53 (143 h)** et
  `FG-669-DQ` depuis le **06/09 14:18 (59 h)**.
- ⚪ **1 `frontend`** « Clipboard : Write permission denied » (06/09) — refus du navigateur.

---

## Angles morts examinés

| Section | Mesure | Lecture |
|---|---|---|
| `temoin_arme` | **4 déclencheurs, tous `actif = O`** | L'instrument est armé : les comptes de ce rapport sont des **mesures**, pas des planchers |
| `disparitions` | **1 constat**, celui du 04/09 — **aucun nouveau** | Rien n'a été supprimé depuis **120 h** |
| `comptes_disparition` | écart `error_logs` **13 250**, inchangé (**13ᵉ point**) ; `alerts` **0** | Pas de `TRUNCATE` |
| `retention_declaree` | `errorDeleted = 0` sur **5 nuits**, rétention 90 j ; **77 725** trames purgées cette nuit | L'application ne supprime aucune erreur, de son propre aveu |
| `trackers_failing` | **0** | — |
| `commandes_sante_7j` | **0 acquittement matériel sur 405** — **12ᵉ point**. Total famille 475 · 455 · 437 · 419 · 409 · **405** | ⚠️ **La baisse n'est PAS une amélioration** : 44 boîtiers émetteurs, inchangé — le plafond est atteint, pas franchi |
| `commandes_motifs_7j` | **15 motifs, tous `fix_continuous`**, tous citant la cible **demandée** | ✅ TRK-059 tient à **J+4** |
| `cadence_resume` | **0** sous le minimum matériel, cible la plus basse **20 s** — **9ᵉ point** | TRK-057 et TRK-008 tiennent |
| `cadence_derive` | **0 ligne** | — |
| `cadence_reelle` | **0** émetteur rapide réel, **2** mesures périmées sur 44 | 3ᵉ point identique |
| `gps_sans_fix` | **0 ligne** | ⚠️ **Zéro trompeur, 5ᵉ jour** : les boîtiers muets sortent du filtre **par le bas** (`OFFLINE`), pas parce qu'ils ont retrouvé leur fix |
| **`trip_automation_runs`** | **24 sur 24** le 08/09, dont **1 `interrupted`** | ✅ **TRK-073 corrigé et PROUVÉ.** Série : 04/09 *(tronquée)* · 05/09 **23** · 06/09 **16** · 07/09 **17** · 08/09 **24** |
| **couverture des limites** | **186 analyses** sur 24 h, **174** avec couverture, moyenne **0,972** | ✅ Le lot V3 écrit, et mieux qu'hier (0,949) |
| **recalage** | **0,0 %** d'échec sur 24 h — ⚠️ **et la mesure a changé de nature** | Voir la section 🔴 ci-dessus |

### 📏 OVERSPEED — cinquième point

| Jour | 04/09 | 05/09 | 06/09 | 07/09 | 08/09 |
|---|---|---|---|---|---|
| `OVERSPEED` | 12 | 6 | 0 | 1 | **9** |
| nées d'un trajet | 12 | 6 | — | 1 | **9** |

**Cinq points, et toujours rien qui ressemble à une tendance** — la seule chose acquise est qu'on
reste très loin des 1 317 alertes de [TRK-022](../REFERENCE-ERREURS.md#trk-022). **9 sur 9 sont nées
d'un trajet**, donc la garde de [TRK-052](../REFERENCE-ERREURS.md#trk-052) tient.

---

## 🗓️ Tests datés — l'état au 09/09

| Fiche | En attente | État |
|---|---|---|
| [TRK-053](../REFERENCE-ERREURS.md#trk-053) *(T14)* | 🔴 **9 j — ÉCHÉANCE DÉPASSÉE DEPUIS HIER** | ✅ Pas de régression : `alertes_depuis_declaration` = **7**, **6ᵉ point identique**. Toujours **non exercé**. 👉 **La décision ne peut plus attendre : PROVOQUER au prochain retour de LLD, ou REQUALIFIER** |
| [TRK-032](../REFERENCE-ERREURS.md#trk-032) *(T19)* | 🔴 **19 j** | **À REQUALIFIER** — l'occasion ne viendra pas seule |
| [TRK-051](../REFERENCE-ERREURS.md#trk-051) *(T20)* | 🔴 **15 j** | **À CONFIER À UN HUMAIN** — geste d'interface, 30 s pour qui a l'écran |
| [TRK-076](../REFERENCE-ERREURS.md#trk-076) *(T27)* | 2 j | ✅ 0 ligne `getSource` sur **48 h** — barre à 7 j, **échéance 15/09** |
| [TRK-075](../REFERENCE-ERREURS.md#trk-075) *(T26)* | 0 j | ✅ 0 ligne sur **8 passages** — **24 h pleines attendues le 10/09** |
| [TRK-073](../REFERENCE-ERREURS.md#trk-073) *(T24)* | — | ✅ **PROUVÉ**, la consigne est close |
| TRK-060 · 064 · 065 · 066 · 068 · 070 | 2 à 5 j | Déployés, **non exercés** |

---

## 🤝 Ce qui attend un humain

1. 🔴🔴 **Recharger un compte IA — les DEUX sont à sec, 7ᵉ jour.** Anthropic depuis le 03/09 00:13
   (**145 h**), OpenAI constaté vide le 05/09. *(T1)* — **rien d'autre ne débloque quoi que ce soit.**
2. 🔴 **TRK-066 — les trois questions du coupe-circuit** : 10 s sur un dernier recours, le réessai et
   son idempotence, le niveau d'une immobilisation demandée **qui n'a pas eu lieu**. *(T3)*
3. **TRK-053 (9 j), TRK-032 (19 j) et TRK-051 (15 j)** — **les trois échéances sont dépassées** :
   provoquer ou requalifier. *(T14, T19, T20)*
4. **Calibrer les alertes de vitesse** *(TRK-072, T4)* et **trancher la migration de TRK-062**
   *(T5, 6ᵉ vérification d'absence)*.
5. **Prévenir le second titulaire de compte sans appareil** (TRK-065, T6).
6. 🔵 **Terrain** : `GLA•KC•31` (143 h) et `FG-669-DQ` (59 h), muets et **non déclarés**. *(T9)*

---

## Fiches ajoutées ou mises à jour

| Fiche | Mouvement |
|---|---|
| ✅ **TRK-073** | 🟢 **CORRIGÉ, DÉPLOYÉ ET PROUVÉ** — 24/24 le 08/09 dont 1 `interrupted` ; la consigne écrite d'avance est tombée **au mot près**. Signature du témoin neuf enregistrée dans la fiche |
| ✅ **TRK-075** | 🟢 **CORRIGÉ ET DÉPLOYÉ** — 0 ligne sur 8 passages ; la seconde moitié de la double condition est **reformulée** parce qu'elle était invérifiable telle qu'écrite |
| 🔴 **TRK-016** | **La mesure a changé de nature** — le passé est réécrit par le rattrapage, `trips` n'a pas d'`updatedAt`, la série de dix points ne se prolonge pas. 🆕 tâche **T28** |
| 📏 **TRK-037** | **Rectification** : 46 `DEGRADATION` Overpass (et non 62, qui était le total toutes sources) — 8ᵉ point, le classement tient |
| 📏 **TRK-076** | 0 ligne sur 48 h — la preuve avance, échéance 15/09 |
| 🔎 **TRK-074** | **Toujours non déployée** — 4 `CRITICAL` actives depuis 4 jours pendant que sa jumelle archive seule |

---

## Contrôle de sortie

`pgrep -x docker` sur le VPS après la passe : **aucun client Docker résiduel**. Toutes les commandes
`docker` de cette passe portaient `timeout` **et** une borne de volume (`exec` et `inspect` bornés,
**aucun `docker logs`**), conformément au garde-fou du §0.
