# Audit VPS — 2026-09-20

> ## Verdict
>
> **🔴🔴 LA MACHINE NE REÇOIT PLUS QU'UN DIXIÈME, PUIS UN CINQUIÈME, DE SON CPU — ET CE N'EST PLUS `dockerd` :
> C'EST L'HÔTE (🆕 VPS-045, gravité 1).** Le 19/09 à **18:30:17 UTC**, un 7ᵉ `docker logs` sans `timeout` lancé
> depuis le poste (`--tail 500` sur `foodsqan-traefik`, script *« PROXY/ROUTEURS »*, 4 min après le déploiement
> marketing) a pris le second cœur pendant que le 6ᵉ (V33, `vizyo-auth-api`, depuis le 16/09 12:41) tenait le
> premier : `sar` **37 → 7 % d'inactivité à 18:40**. Trois heures quarante plus tard, l'hyperviseur a commencé à
> **reprendre le CPU par paliers, à :10 de chaque heure** — `%steal` **2,8 → 20,8 → 45,8 → 67,5 → 88,7 %** de 22:00
> à 01:10 — soit une part *servie* de **97 → 79 → 54 → 32 → 11 %**. **Les deux clients ont été tués vers 05:25**
> (session « Tracky down » du propriétaire, `sar` : idle 0,1 → 5,8 → 11,6 %) : `dockerd` est à **0–3 %**, **0
> client**, cumul figé à **395,5 h** — *la cause côté machine est éteinte*. **Et pourtant, six heures plus tard, la
> part servie plafonne à ~21 %** (steal 75–82 %, plateau plat de 05:30 à 11:10), charge **13 → 58**, file
> d'exécution 45–49 pour 6 % d'`user`. Deux plateaux plats (10 % puis 21 %) et des paliers à heure fixe sont la
> signature d'une **politique**, pas d'un voisin bruyant : *le seul levier de la machine a été tiré, et l'hôte n'a
> pas rendu le CPU.* La production répond (**200 en 0,5–2,1 s**), mais elle vit sur **0,4 vCPU**.
>
> **🔴 CE QUE ÇA A DÉJÀ COÛTÉ, MESURÉ.** Les **30 reprises de ce matin** (03:00 MH Cars, 05:00 CDEF31) : **11
> acquittées ≤ 10 s, 16 en 10–60 s, 1 en 60–180 s, 2 en 220 s** — la veille, **30 / 30 ≤ 10 s**. **Deux reprises de
> 03:00 sont passées par le secours SMS** (`channel=SMS`, 3 tentatives puis 2, `smsAttemptCount` 3 au total) :
> *TRK-083 / T71 s'est exercé, et son prix est facturé.* La sauvegarde Tracky de 03:01 a duré **64 min** (37 s la
> veille), les passages d'automatisation de nuit **8 · 11 · 12 · 18 min** au lieu de **3 · 1 · 5 min à la même heure
> la veille** (VPS-M103 tenu : lu à heure égale). Vizyo Auth injoignable 00:00–00:02 (lu au centre d'alerte).
> ⚠️ **Et l'audit lui-même y a pesé** : la collecte a duré **647 s** (7,2×) et fait monter la charge de **15,8 à
> 81,7** — sur une VM qui ne recevait que 12 % de son CPU, ses `gzip -dc` de 170 Mo étaient la majorité de ce
> qu'elle recevait. 🆕 **VPS-M107** : le collecteur mesure désormais le steal *avant* de commencer et passe en
> **mode allégé** au-delà de 50 %.
>
> **🔴 ET UNE PANNE SANS RAPPORT AVEC LE CPU, TROUVÉE PARCE QU'UNE UNITÉ A RENDU `exit-code` (🆕 VPS-046).** Le
> rafraîchissement hebdomadaire de la **démo** (04:00) a **échoué** : *« The column `managedByManagerAt` does not
> exist in the current database »*. L'importeur tourne sur `tracky-api:latest` (code du 19/09) contre une base de
> démo dont le schéma date d'avant le lot D (17/09) — **la pile démo n'a pas été recréée depuis** : le compose de
> la démo le dit lui-même, *« la démo ne peut pas être en retard sur la prod autrement que par l'oubli de cette seule
> commande »*, et `deploy.sh` ne la lance pas. La démo affiche les données du **13/09** et son API a été arrêtée
> **8 min 35** pour rien.
>
> **✅ CE QUI TIENT.** **Copie hors-site : 4 nuits de suite OK** (17/09 `pairesCopiees 2`, puis 1 · 1 · 1) — le seuil
> de gravité 1 de VPS-037 **n'a pas été franchi** (cause intacte : `WakeToRun=False`) ; **7 sauvegardes / 7, 9
> archives relues / 9** ; **34 émetteurs** (un boîtier neuf, HM-787-GA) sur `positions`, `wire_logs`, décisions —
> **34 / 34 / 34**, dimanche à ×0,94 du dimanche précédent (VPS-M71 tenu) ; comptes **4 = 4**, dossiers **12 = 12** ;
> disque **52 Go / 96 (54 %)**, 45 Go libres ; 0 OOM ; 0 échec SSH sur root, 0 empreinte inconnue ; hyperviseur
> 52 / j × 6, 0 inattendue ; ✅ **VPS-044, 2ᵉ moitié de la preuve** : `avant-20260919-1537-1d1521b2` =
> `84cc3fe36db2` (construite le 17/09 14:45, en service du 17/09 15:37 au 19/09 16:36) — juste, *encore une fois
> parce que `latest` n'était pas pré-construit* ; `until=24h` intact, elle meurt demain 00:40.
>
> **⚠️ CE PASSAGE COUVRE TROIS JOURS** (17/09 03:22 → 20/09 11:29) : l'audit du **18/09** a été **interrompu par
> l'utilisateur** (70 messages, 0 rapport), celui du **19/09** est parti en rattrapage à **14:12** et a traîné
> jusqu'à ce matin **10:36** (poste endormi), où sa collecte s'est **bloquée derrière son propre `docker system df`
> sans `timeout`** (🆕 VPS-M104, corrigé — et le correctif était **faux** : 🆕 VPS-M105, corrigé) ; celui-ci est
> parti à **11:10** (+528 min). Trois passages, un rapport.

---

## 1. Tableau par domaine

| Domaine | État | En une phrase |
|---|:--:|---|
| **Charge & capacité** | 🔴🔴 | 🆕 **VPS-045** : part de CPU **servie par l'hôte 10 % (01:10–05:20) puis ~21 % (05:30–11:10)** ; `vmstat` **st 89–91, id 0, r 45–49** ; charge **15,8 → 81,7** pendant la collecte ; `dockerd` **0,9–3 %** (cause côté VM éteinte à ~05:25) ; production **200 en 0,5–2,1 s**. |
| **Docker** | 🟠 | VPS-016 nº 6 **et** nº 7 **clos vers 05:25** (0 client à 10:44 · 11:14 · 11:29 · 11:36, cumul figé 395,5 h) ; `docker ps -q` **18–21 s**, `docker system df` **> 90 s** ; 31 images ; **7 journaux troués** dont `tracky-postgres` revenu (45 %) ; `tracky-api`/`web` recréés 19/09 16:36 (journal T33 ✅), `tracky-lp` 18:26 ✅. |
| **Démo** | 🔴 | 🆕 **VPS-046** : import hebdo **ÉCHEC 04:07:58** (`managedByManagerAt` absente), API démo arrêtée 04:00:03 → 04:08:38, données figées au **13/09** ; pile démo **2 déploiements en retard** (17/09 ×2, 19/09). |
| **Sauvegardes** | 🟢 | 7 / 7 (Tracky **64 min**, 3 min 59 de CPU — 37 s / 22 s la veille), 9 / 9 relues, Verify OK 03:38 ; **copie hors-site OK 4 nuits de suite** (JSON 20/09 04:30:31, `pairesCopiees 1`) ; `alpine` absent (23ᵉ prédiction). |
| Sécurité | 🟢 | 14 échecs / 7 j, **0 sur root** ; 4 empreintes vues / 6 déclarées, 0 inconnue ; `vaultbk` **253 h** ; hyperviseur 52 / j, 0 inattendue ; `fail2ban` redémarré 18/09 06:22 par `unattended-upgrades` (libperl/libaom, compteurs à zéro depuis) ; certificats 51 j. |
| Données | 🟢 | `positions` **34** / `wire_logs` **34** / décisions **34** ; dimanche **16 783** lignes = ×0,64 de samedi, **×0,94 du dimanche J-7** (17 859) ; rétention échantillonnage **20,4 %**, part `MOVING` **10,6 %** (dimanche) ; `tracky_prod` **1 210 Mo**, démo 581 Mo. |
| Disque | 🟢 | **52 Go / 96 (54 %)**, 45 Go libres ; `/var/backups` 5,0 Go ; `/opt` **NON MESURÉ** (9 / 21 sous-dossiers > 12 s — VM sans CPU) ; cache de build **NON MESURÉ** (disjoncteur) ; VPS-030 inchangé (161 Mo + 3 fichiers racine). |
| Mémoire | 🟢 | 3,4 Gi / 7,8, swap 1,1 Gi, PSI mémoire 0,00, **0 OOM 30 j** ; ⚠️ `Committed_AS` 11,7 Go. |
| Planification | 🟠 | 25 unités : 21 succès, **1 ÉCHEC** (`tracky-demo-refresh`, VPS-046) ; `logrotate` 00:00:03 → **00:03:43**, 7,2 s de CPU (V29 non faite, 8ᵉ nuit) ; `until=24h` intact (V32 a non faite) ; `apt` **NON MESURABLE** (cache 10 h). |
| Mises à jour | 🟠 | 70 en retard (indicatif), noyau -136 / -139, `libc6` sous 6 services, uptime **46,6 j** — V4 débloquée par la mort des clients, **re-bloquée par VPS-045** (un redémarrage sur une VM à 20 % de CPU = ~5 min de coupure, et il ne rend pas ce que l'hôte retient). |
| **L'agent lui-même** | 🔴 | **3 passages, 1 rapport** ; **+528 min** ; **647 s** (7,2×), charge **15,8 → 81,7** ; disjoncteur armé à 11:22:23 (sections 4–5 NON FAITES, refaites en marge par 4 `docker exec` bornés) ; **4 correctifs posés** (M104 réparé, M105, M106, M107), rejoués en script entier (passe 2). |

---

## 2. Ce qui va bien — avec les chiffres qui le prouvent

| Ce qui va bien | La mesure qui le prouve | Source |
|---|---|---|
| **Les deux clients Docker sont morts, et la boucle avec eux** | `pgrep -a -x docker` **vide** à 10:44, 11:14, 11:29, 11:36 ; `dockerd` **0 %** (10:50), **3 %** (11:14), **0,9 %** (11:18) ; cumul **395,4 → 395,5 h** en 45 min (figé) ; `read()` non mesuré (VM sans CPU) | marges A, E, état 11:14, §1 |
| **La copie hors-site a tenu quatre nuits** — le seuil de gravité 1 n'a pas été franchi | `copie.log` : **17/09 06:30 OK 2 paires** (16 + 17), 18/09 OK 1, 19/09 OK 1, **20/09 06:30:36 OK 1** (27 s) ; JSON du VPS **2026-09-20T04:30:31Z**, `pairesCopiees 1`, `plusRecente verify-db_20260920-033038` ; **0 manifeste plus récent que le JSON** | §11, poste |
| Toutes les sauvegardes de la nuit ont réussi | `tracky` 04:05:08 (172 Mo, **64 min**), `verify` 03:38:52 (`DERNIER-ETAT.json` OK), `auth` 04:06, `manager` 04:31, `texto` 04:41, `capcom6` 04:54, `dpkg` 00:00 | §7, §11 |
| Les archives se relisent | **9 sur 9** décompressées **et** marqueur de fin présent (`pg_dump` ×7, `mysqldump` ×2) | §11 |
| Les trois compteurs s'accordent, et la flotte a gagné un boîtier | `positions` **34** · `wire_logs` **34** · décisions **34** (33 le 17/09 ; HM-787-GA mis en service le 18/09) ; par jour calendaire : 33 · 33 · 32 · **34 · 34** · 34 partiel | marges I, J |
| Le dimanche est un dimanche, pas un effondrement | 16 783 lignes / 24 h = **×0,64 de la veille** (samedi 26 373) mais **×0,94 du dimanche J-7** (17 859) — VPS-M71 tenu ; `wire_logs` 98 641, **121 trames / h / boîtier** (120 le 17/09) | marge I |
| Le périmètre humain n'a pas bougé | comptes **4 = 4**, dossiers **12 = 12** (la liste des conteneurs, elle, est NON MESURÉE — disjoncteur — et le collecteur ne dit plus « 38 disparus », VPS-M106) | §4 |
| L'étiquette de repli est juste, 2ᵉ fois | `tracky-api:avant-20260919-1537-1d1521b2` = **`84cc3fe36db2`** (créée 17/09 14:45:58 = l'image de `1d1521b2`, en service 17/09 15:37 → 19/09 16:36) ; `tracky-web:avant-…` = `99811d3a61a2` (14:44:48) ; `tracky-lp:avant-…-ad0abff4` = image du **07/08** (la LP n'avait pas été reconstruite depuis) | marge H |
| Toutes les clés ayant servi sont déclarées | 4 vues / 6 déclarées, 0 inconnue ; Conductor a déposé à 01:04 (29,8 Ko `prd`), Dispocar 01:31 / 01:39 | §6, marge F |
| L'hyperviseur n'a rien envoyé d'inattendu | 348 exécutions / 8 663 sondages ; **52 / j × 6** ; INATTENDUES **0** ; un seul nom de sonde | §7 |
| Rien n'a été tué faute de mémoire | 0 OOM 30 j, PSI mémoire `full` 0,00, 4,4 Gi disponibles | §2 |
| La production répond — sur 0,4 vCPU | `/` 200 en **499 ms**, `/api/health` 200 en **2 103 ms**, LP 2 001 ms, Verify 302 en 404 ms ; à 11:14 : 1 196 / 603 ms | §6, état 11:14 |
| `alpine:latest` s'est comporté comme prédit, 23ᵉ fois | présent à 11:31 (`294b683cb724`, tiré **17/09 20:37**) — donc **absent** au ménage du 20/09 00:40 (52 h) puis re-tiré par la sauvegarde Verify de 03:30 ; le bloc §11 le dit « ABSENT » parce qu'il lit `docker image inspect` sous disjoncteur — *NON MESURÉ, pas absent* | §11, marge H |

---

## 3. 🆕 VPS-045 — l'hôte retient 80 à 90 % du CPU, et il ne l'a pas rendu quand la cause a disparu

**Gravité 1 · `A_TRAITER` · 🆕 V35 (ticket hébergeur + le seul levier interne) · V4 re-bloquée**

### La mesure — `sar -u`, part de CPU **servie** = 100 − `%steal`, heure par heure

| Heure UTC | 19/09 18:00 | 19:00 | 20:00 | 21:00 | 22:00 | **22:10** | 23:00 | **23:10** | 20/09 00:00 | **00:10** | 01:00 | **01:10** | 02:00 → 05:20 | **05:30** | 06:00 → 11:10 |
|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| servie % | 96,7 | 96,7 | 97,0 | 96,9 | 97,2 | **79,2** | 78,8 | **54,2** | 32,5 | 32,5 | 27,1 | **11,3** | **9,4–10,7** | 15,3 | **18,1–24,6** |
| idle % | 37,7 | 7,3 | 5,5 | 8,0 | 7,4 | 6,9 | 6,1 | 4,8 | 1,7 | 1,7 | 2,2 | 0,6 | 0,1–0,8 | 5,8 | 9,2–15,7 |

- **Les paliers tombent à :10** (22:10, 23:10, 00:10, 01:10), chacun retire ~un quart de ce qui reste ; les plateaux
  sont **plats** : 89,3–90,6 % de steal pendant 4 h 10, puis 75,4–82,4 % pendant 5 h 40. Un voisin bruyant ne fait
  ni des marches à heure fixe ni des plateaux à ±1 point.
- **Le déclencheur est daté** : 2 × 100 % de CPU à partir de 18:40 (nº 7 s'ajoute à nº 6) ; le premier palier à 22:10,
  **3 h 30 plus tard**. La sonde horaire de l'hébergeur (`ps -eo … --sort=-%cpu`, VPS-027) tourne à :35 de chaque
  heure, `usage-telemetry.py` à :15 — *l'hôte se donne les moyens de lire ce qu'il limite.*
- **La cause côté VM a disparu vers 05:25** (mémoire du poste : les deux parents puis les deux clients tués par le
  propriétaire, `pgrep -xc docker` 0, latence `/api/health` 5 s → 0,4–1,4 s en 2 min ; `sar` : idle **0,08 → 5,81 →
  11,62 %** sur 05:20–05:40). **La part servie a doublé (10 → 21 %) et s'est arrêtée là**, six heures durant.
- **Ce que la VM fait de ses 21 %** : `user` 5,3 + `sys` 3,5 = ~9 %, idle 10–15 % — *il lui arrive de n'avoir rien à
  faire, et l'hôte ne sert quand même pas au-delà de ~21 %* : c'est un **plafond**, pas une contention. Charge
  13–58, `r` 45–49 : tout ce qui devrait prendre 20 ms en prend 200.

### QUOI — ce que l'audit peut dire, et ce qu'il ne peut pas

- **Peut dire** : la VM ne reçoit qu'une fraction fixe de ses 2 vCPU depuis 22:10 hier ; cette fraction a changé deux
  fois (01:10, 05:30) ; la seconde coïncide avec la disparition de la cause interne ; depuis, rien.
- **Ne peut pas dire** : *pourquoi* — une limitation d'équité déclenchée par 11 h à 2 × 100 % (l'hypothèse que
  dessinent les paliers), une fenêtre glissante qui se viderait seule, un voisin, une maintenance. **Ça se lit côté
  hôte, pas côté invité.** ⚠️ La mémoire du dispositif dit depuis le 08-10 : *« un `%steal` élevé ne prouve PAS une
  contention d'hôte : une machine saturée en déclare mécaniquement plus »* — c'était vrai **pendant** la boucle
  (`dockerd` demandait 2 cœurs). **Ce n'est plus le cas** : `dockerd` est à 1 %, la VM a de l'idle, et le steal tient.
- **Test écrit d'avance** (VPS-M41, avec sa précondition) : *si* la politique est une fenêtre glissante de 24 h sur la
  consommation, la part servie remontera **par paliers entre le 20/09 22:10 et le 21/09 05:30** sans aucun geste ; *si*
  elle reste à ~21 % demain à 02:20 (passage suivant), c'est une limite qui **ne se lève pas seule** → ticket.
  Précondition : aucun build, aucun `docker compose up --build`, aucun `du` d'ici là.

### `pourquoiInvisible`

Rien ne surveille le CPU *servi* : `/admin/system` montre la charge (58) sans dire que 90 % des cycles ne sont pas
donnés ; le centre d'alerte a vu l'effet (Vizyo Auth 503 en 57 s) et nommé la cause visible (les deux clients) —
juste — sans pouvoir voir que l'hôte, lui, ne suivrait pas. **Angle mort n° 1 du 17/09 (35ᵉ report)** : rien entre
deux passages ; et ce passage-ci est arrivé **13 h** après le premier palier.

### QUOI FAIRE — V35, dans cet ordre

**1. Le ticket, maintenant** (0 s côté machine, quelques minutes côté poste) — avec **cet extrait exact** :

```bash
sar -u -f /var/log/sysstat/sa19 -s 18:00:00 ; sar -u -f /var/log/sysstat/sa20   # steal 3 → 89 %, paliers 22:10 / 23:10 / 00:10 / 01:10 ; plateau 78 % depuis 05:30
```

*Question à l'hébergeur* : *« Une limitation de CPU est-elle appliquée à `srv1201617` depuis le 19/09 22:10 UTC ?
Selon quelle règle, et quand se lève-t-elle ? La cause interne (2 processus `docker logs`) est éteinte depuis le
20/09 05:25. »* **Gain** : la seule réponse qui dit si le CPU reviendra. **Risque** : nul.

**2. Le seul levier interne — rendre à la production ce que l'hôte laisse** *(décision humaine, 20 s, réversible)* :
sur 38 conteneurs, **18 ne servent pas la production** (`maalem-dev-*` ×6, `maestroo-dev-*` ×4, `tracky-demo-*` ×4,
`dronely-*` ×2, `foodsqan-maquettes`, `dg-epaviste-website`) et portent ~la moitié des sondes de santé (VPS-011 :
72 / min). **Les arrêter — pas les supprimer** — libère leur part des 0,4 vCPU pour Tracky, Auth, Texto, Vault.

```bash
cd /opt/maalem && docker compose ps --format '{{.Name}} {{.State}}'   # relire AVANT, pile par pile : docker compose stop, jamais down
```

**Gain** : à mesurer à heure égale (`sar` demain 02:00 contre 02:00 ce matin — VPS-M103) ; **contrepartie** : les sites
de dev et la démo indisponibles jusqu'au `start` ; **risque** : nul pour la production. ⚠️ **Avant 18:00 UTC** : les
**59 coupes** de 18:00 et 20:00 puis les **31 reprises** de 03:00 / 05:00 partent sur cette VM — ce matin, 2 sur 30
sont déjà passées par le SMS payant.

**3. Ne rien ajouter d'ici là** : pas de déploiement (`deploy.sh` à 2 cœurs = 4 min ; ici, inconnu), pas de rebuild,
pas de V4 (un redémarrage ne rend pas ce que l'hôte retient, et coûte ~5 min de coupure à ce régime).

### `aNePasFaire`

- ❌ Ne pas relire « steal 88 % » comme « la boucle `dockerd` continue » : `dockerd` est à 1 %, 0 client. **C'est un
  autre constat** — d'où la fiche séparée.
- ❌ Ne pas conclure de la coïncidence 05:25 / 05:30 que « tuer les clients a rendu la moitié du CPU » : deux points,
  et le second plateau est resté plat six heures. *Un palier n'est pas une pente.*
- ❌ Ne pas lancer `docker system df`, `docker stats`, `du /opt`, ni un build « pour voir » : chaque commande prend
  dix fois son temps et le prend à la production (VPS-M107, ci-dessous).
- **Seuil** : `SURVEILLANCE` gravité 2 quand la part servie est **> 90 % sur un relevé `sar` entier (24 lignes)** ;
  gravité 1 tant qu'elle est < 50 %.

---

## 4. VPS-016 — nº 6 et nº 7 clos vers 05:25, par le propriétaire ; la classe reste ouverte (V34)

**Gravité 1 → 2 · `A_TRAITER` (la classe) · V33 ✅ FAIT · V34 ouverte**

| Heure UTC | Événement | Source |
|---|---|---|
| 16/09 12:41:08 | nº 6 : `docker logs --tail 15 vizyo-auth-api` (pid 3366708, parent `bash -c` 3366707) — un cœur | rapport du 17/09 |
| **19/09 18:26:48** | `deploy.sh` `perimetre: marketing` (`ad0abff4`, 29 s, `tracky-lp` seul) | `journal.jsonl` |
| **19/09 18:30:17** | **nº 7** : `docker logs --tail 500 ae54b0ff3f79` (= `foodsqan-traefik`, pid 2060212), parent `bash -c` 2060192 *« === PROXY/ROUTEURS === … docker ps --filter ancestor=traefik … »* → `init` — **le second cœur** ; `sar` 18:40 : idle **37,36 → 7,33 %**, `sys` 35 → 53 % | centre d'alerte 20/09 01:17, `sa19` |
| 19/09 22:10 → 20/09 01:10 | l'hôte reprend le CPU par paliers (§3) | `sa19`, `sa20` |
| 20/09 ~05:25 | **les deux parents puis les deux clients tués** (SIGTERM, aucun `restart docker`) ; latence 5 s → 0,4–1,4 s en 2 min | mémoire du poste (05:24 Z), `sar` 05:30 |
| 10:44 · 11:14 · 11:29 · 11:36 | `pgrep -a -x docker` **vide** ×4 ; `dockerd` 0 / 3 / 0,9 % ; cumul **395,5 h** figé ; 0 connexion sur la socket (3 sondages) | marges A, E, état, §4 |

**L'arithmétique du cumul** : 319,5 h (17/09 03:21) → 395,5 h (20/09 10:50) = **+76 h en 79 h 30**. À un cœur du 17/09
03:21 au 19/09 18:40 (63,3 h → ~62 h), puis deux cœurs de 18:40 à 22:10 (7 h), puis **de moins en moins** — le steal
a affamé la boucle elle-même : 76 − 69 = 7 h pour les 7 h 15 restantes à 2 cœurs demandés, servis à 10–30 %.
*La machine était si étranglée que même la boucle ne tournait plus à plein.*

**Sept sur sept.** Le nº 7 est la **troisième** occurrence en huit jours (13/09, 16/09, 19/09) et la **septième** depuis le
05/08 à venir d'une session SSH du poste : un `docker logs` sans `timeout`, dans un `bash -c` qui meurt avant lui. Le
script *« PROXY/ROUTEURS »* ne contenait **qu'un seul** `docker logs` — tuer le client aurait suffi ici ; celui du 16/09
en contenait **trois** en série (cas du 08-20 : tuer le parent d'abord). **La règle est écrite au référentiel depuis le
05/08 et dans `PROCEDURE-AUDIT.md` depuis le 20/08 ; elle manque là où les commandes sont tapées** : `CLAUDE.md` ne
la porte toujours pas ce matin (diff relu : seul le paragraphe Codex a été ajouté). La mémoire du poste, elle, a été
mise à jour à 05:24 (occurrences « 7ᵉ et 8ᵉ » — le compte de la mémoire est décalé d'une unité par rapport au
référentiel : ce sont les nº 6 et nº 7).

**QUOI FAIRE — V34, inchangé** : (1) **une ligne dans `CLAUDE.md`** — *sur le VPS, `docker logs` / `exec` / `inspect` /
`images` = `timeout 20`, `--tail ≤ 2000`, jamais depuis un `bash -c` qui peut se fermer avant* — hors périmètre
d'écriture de cet audit ; (2) les deux scripts de diagnostic connus corrigés ; (3) `pgrep -x docker` lu **avant** chaque
audit et dit (fait ce passage : 4 lectures). ✅ **V33 est FAITE** : preuve mesurée ci-dessus.

**`aNePasFaire`** : ❌ ne pas fermer VPS-016 sur « 0 client » : le seuil `SURVEILLANCE` demande **deux passages** —
c'est le premier. ❌ Ne pas attribuer à ces deux `kill` le retour du CPU (§3).

---

## 5. 🆕 VPS-046 — la démo n'est mise à jour que par une commande que le déploiement ne lance pas, et l'import hebdomadaire vient d'échouer dessus

**Gravité 2 · `A_TRAITER` · 🆕 V36 (10 s sur le VPS, hors fenêtre HH:42–HH:46 et hors VPS-045 ; puis `deploy.sh`)**

```
[2026-09-20T04:00:03Z] Passage planifié — import complet
[2026-09-20T04:00:03Z] Arrêt de l'API de démo
[2026-09-20T04:02:32Z] Import…
[import-demo] Source : 2 société(s), 37 véhicules, 37 boîtiers, 9 conducteurs, 15628 trajets, 10176 alertes
❌ Import de la démo en échec : Invalid `prisma.fleet.upsert()` invocation:
   The column `managedByManagerAt` does not exist in the current database.
[2026-09-20T04:07:58Z] IMPORT EN ÉCHEC — la base de démo est restée dans son état précédent
[2026-09-20T04:08:38Z] API de démo redémarrée          → tracky-demo-refresh.service : exit-code (status=1)
```

- **La cause** : `docker-compose.demo.yml` promet *« les MÊMES IMAGES que la prod … un `up -d` sur ce fichier recrée
  les conteneurs de la démo sur ces images et joue les migrations. La démo ne peut pas être en retard sur la prod
  autrement que par l'oubli de cette seule commande »*. **`deploy.sh` ne lance pas cette commande** (relu : `up`,
  `up-lp`, jamais la démo). Depuis, la production a été déployée **trois fois** (17/09 13:37 `1ba0cc25`, 15:37
  `1d1521b2` — lot D, qui ajoute `managedByManagerAt` —, 19/09 16:36 `8e289f38`) ; `tracky-demo-api` n'est **pas** dans
  les conteneurs recréés depuis 48 h et tourne sur une image d'avant. L'**importeur**, lui, est `image:
  tracky-api:latest` — le code du 19/09 — et parle à une base au schéma du 13/09.
- **Ce que ça coûte** : la démo (`demo-tracky.vizyoagency.com`) montre des données du **13/09** ; son API a été
  **arrêtée 8 min 35** (04:00:03 → 04:08:38) pour un import qui ne pouvait pas réussir ; le 13/09, le même passage
  avait pris **4 min 30** et réussi. Sur une VM à 10 % de CPU l'import a mis 5 min 26 à échouer — *le CPU n'est pas la
  cause, il n'a fait que ralentir l'échec*.
- **`pourquoiInvisible`** : le passage est **hebdomadaire** (dimanche 04:00) : le décalage date du 17/09 et n'a eu son
  premier effet mesurable que ce matin. Le compose le savait et l'a écrit ; personne ne relit un en-tête de compose
  au moment d'écrire `deploy.sh`. Le bloc « unités en échec » de la section 7 l'a rendu **en une ligne** — c'est lui
  qui a déclenché la lecture du journal.
- **QUOI FAIRE — V36** : (a) sur le VPS, **quand VPS-045 est levé** (un `up -d` recrée deux conteneurs et joue les
  migrations : ~1 min à 2 cœurs, inconnu à 0,4) :

```bash
cd /opt/vizyo-tracky/deploy/vps && docker compose --env-file .env.demo -f docker-compose.demo.yml ps   # relire AVANT
```

```bash
cd /opt/vizyo-tracky/deploy/vps && docker compose --env-file .env.demo -f docker-compose.demo.yml up -d   # recrée api/web démo sur :latest + prisma migrate deploy (CMD de l'image)
```

  **Gain** : la démo à jour du code et du schéma ; le prochain import (27/09 04:00) réussit. **Contrepartie** : démo
  indisponible ~1 min. **Risque** : nul pour la production (réseaux séparés ; l'importeur seul touche `vizyo-tracky`,
  en lecture). (b) **Dans `deploy.sh`** (code applicatif, hors périmètre) : après `up`, `up -d` sur la démo — ou une
  garde qui refuse de laisser la démo à plus d'un déploiement de retard. **Preuve** : `journalctl -u
  tracky-demo-refresh` du 27/09 = *« Import réussi »*, et `tracky-demo-api` dans « conteneurs RECRÉÉS » le lendemain
  de chaque déploiement.
- **`aNePasFaire`** : ❌ ne pas relancer `tracky-demo-refresh-demande` « pour voir » : même importeur, même échec, et
  8 min d'API démo arrêtée de plus. ❌ Ne pas jouer la migration à la main dans `tracky-demo-postgres` (incident du
  17/09 : une migration éditée ou rejouée à la main a mis l'API à terre 56 min). ⚠️ V27 (décision du 15/09 : la démo
  n'analyse pas) n'est pas concernée.

---

## 6. VPS-037 / VPS-M73 / VPS-M57 — la copie hors-site a tenu, le poste non

| Heure UTC | Événement | Source |
|---|---|---|
| 17/09 04:30 | copie **OK, 2 paires** (16 + 17) — le seuil de gravité 1 (3ᵉ paire sans copie / 7 j) **n'est pas franchi** | `copie.log` 06:30:13 Paris |
| 18/09 02:20:43 | audit VPS lancé à l'heure — **interrompu par l'utilisateur** (70 messages, 0 rapport, 0 collecte) | `list_task_runs`, transcription |
| 18/09 04:30 · 19/09 04:30 | copies **OK, 1 paire** chacune | `copie.log` |
| 19/09 ~02:20 | audit VPS **non lancé** (poste endormi) ; rattrapage à **14:12:04** — *à la même seconde que le centre d'alerte et l'audit du PC* (VPS-M57, **4ᵉ collision**, lue au centre d'alerte) ; celui du centre d'alerte interrompu 2 min plus tard | `list_task_runs`, rapport CA |
| 20/09 01:33:44 Z → 10:36 | le rattrapage du 19/09 **traîne 20 h** (poste endormi entre-temps) et collecte à **10:36:57** — bloqué dans `docker system df` (VPS-M104), arrêté proprement 10:49:44 ; sections 1–3 + marges A–G archivées dans `collectes/2026-09-20.txt` | collecte partielle |
| 20/09 04:30 | copie **OK, 1 paire** (`pairesCopiees 1`, 27 s) | JSON du VPS |
| 20/09 11:00:38 → 11:09:22 Z | le poste **entre en veille et en sort** (événements 42 / 107) ; ce passage part à **11:10:29** — +528 min sur 02:22 | journal Windows |

**Ce que ça change** : VPS-037 reste `A_TRAITER` gravité 2 — *le symptôme s'est refermé parce que le poste était éveillé
à 04:30 quatre matins de suite* (VPS-015, même motif), et `WakeToRun` est **toujours `False`**. VPS-M73 : **3
passages, 1 rapport** — deux causes nouvelles au catalogue : *interruption humaine* (18/09) et *rattrapage qui traîne
au-delà du passage suivant* (19/09 → 20/09 : le planificateur n'a pas lancé le passage de 02:20 tant que le précédent
« tournait »). `WakeToRun` sur la tâche de copie **et** une tâche Windows de réveil à 02:15 restent le geste (30 s).

---

## 7. Les autres constats, re-mesurés

| Fiche | Grav. | Mesure du 20/09 | Écart / 17-09 |
|---|:--:|---|---|
| **VPS-044 / V32** — repli | 3 | ✅ `avant-20260919-1537-1d1521b2` : api `84cc3fe36db2` (17/09 14:45:58), web `99811d3a61a2` (14:44:48) = les images en service du 17/09 15:37 au 19/09 16:36 ; `avant-20260916-0519` **partie** (18/09 00:40, comme prédit) ; `until=24h` intact ; `deploy.sh` étiquette toujours `:latest` (relu, l. 278) → juste **parce que** `latest` n'était pas pré-construit ; l'étiquette du jour meurt **21/09 00:40** (32 h) | 2ᵉ ✅ de suite « où une étiquette existe » — mais **par la même chance** : la fiche reste ouverte, (b) est le correctif |
| **VPS-041 / V29** — deux rotateurs | 2 | `logrotate` **00:00:03 → 00:03:43**, 7,2 s de CPU (21,9 le 17/09 ; 3 min 40 de mur à 10 % de CPU) ; **7 troués** : les 6 `-dev-` (45–97 %) **+ `tracky-postgres` 45 % sur 8,1 Mo** (revenu) ; **9** à 0 octet | 8ᵉ nuit, +1 troué de production |
| **VPS-042 / V30** — dépôts | 2 | `dispocar` : `dev` **48 813 o** (44 064 le 17/09 — vivante), `prd` **1 172 o ×7** (14 → 20/09) ; `conductor` `prd` 29 754 o 01:04 ; session `vaultbk` **253 h** sans commande ; comptes 4 = 4 | `prd` vide 7ᵉ nuit |
| **VPS-038** — cohorte | 2 | **34** émetteurs (HM-787-GA) ; 34 / 34 / 34 ; jeudi 34, vendredi 34 complets ; 0 silencieux 24 h sur `wire_logs` ∖ `positions` | seuil tenu, +1 |
| **VPS-033 / V14** — `apt` | 2 | **NON MESURABLE** (cache 00:57, 10 h ; `apt-daily` a sonné 00:56 — 1ʳᵉ sonnerie du jour UTC, conforme à la loi du 14/09) ; indicatif 70 dont 0 ; `unattended-upgrades` a installé libperl/libaom/libc-bin le 18/09 06:21 (canal vivant, VPS-M74) | 5 valides / 21 |
| **VPS-043 / V31** — copieur | 2 | 4 tentatives, 4 OK (`pairesCopiees` 2 · 1 · 1 · 1), 15 manifestes, 0 orphelin ; `throw` intact | mécanisme non exercé |
| **VPS-030 / V17** — hors rétention | 3 | `/root` 1,7 Go, `avant-chantier-20260915-1720.dump` 161 Mo, 3 fichiers racine (177 Ko) — les mêmes | inchangé |
| **VPS-M56 / V5** — budget | 2 | **647 s** (7,2×), 15,82 → **81,67** ; `/opt` 51 s pour 12 / 21 ; `gzip -dc` 172 Mo ; **audit 22,6 % de la machine, `user`+`sys` 9,4 %** — sur une VM servie à 12 % | 28ᵉ dépassement, **cause nouvelle : l'hôte** → VPS-M107 |
| **VPS-M92** — périmètre | 2 | comptes 4 = 4, dossiers 12 = 12 ; conteneurs **NON MESURÉ** (disjoncteur ; 38 `running` lus dans `config.v2.json` en marge E, tous `health=starting` ou `aucune` — *l'état de santé n'est plus tenu par `dockerd` depuis 01:04 : « Health check … timed out starting »* ×96 lignes à 01 h) ; recréés : api/web 19/09 16:36 ✅ journal, lp 18:26 ✅ | conteneurs : mesure non faite |
| **VPS-M93** — série `MOVING` | 2 | dimanche : part `MOVING` **10,6 %**, rétention **20,4 %** (23,7 / 30,1 mercredi) — cycle hebdo, ne se compare qu'au dimanche 13/09 (non mesuré : passages manqués) | point de dimanche |
| **VPS-005 / V7** — limites | 2 | **NON MESURÉ** (disjoncteur) ; 31 / 38 au 17/09 | — |
| **VPS-010 / V4** — noyau | 2 | -136 actif, -139 installé, uptime **46,6 j**, `libc6` sous 6 services ; débloquée par la mort des clients, **re-bloquée par VPS-045** | bloquée |
| **VPS-012** — clés CI | 2 | `github-actions-vizyo-auth` 0 / 7 j, sans option ; `vizyo-vps-hostinger` **3 820** | 13ᵉ passage |
| **VPS-032** — sessions SSH | 3 | **3 859 / 7 j** ; 891 · 506 · 607 · 557 · **935 · 132 · 121 · 110** — ⚠️ la chute du 18/09 (935 → 132) coïncide avec la **pause des agents locaux du poste** (17/09 08:50, T75) : *ce sont eux qui font ~600 sessions par jour* — première ventilation de VPS-032 par déduction, à confirmer à la reprise | cause probable nommée |
| **VPS-036 / VPS-027** — hyperviseur | 2 | 348 / 8 663 ; 52 / j × 6 ; 0 inattendue ; sonde `usage-telemetry.py` ×320 | inchangé |
| **VPS-026** — `alpine` | 3 | tiré 17/09 20:37, présent 11:31 ; le §11 dit « ABSENT » sous disjoncteur — **NON MESURÉ** par le collecteur, mesuré en marge : 23ᵉ prédiction tenue | conforme |
| **VPS-011** — healthchecks | 3 | **NON MESURÉ** (disjoncteur ; le bloc rendait « -1 SANS SONDE », VPS-M106 corrigé) ; `dockerd` : *« Health check … timed out starting »* ×96 lignes à 01 h, ×213 à 05 h — les sondes elles-mêmes n'obtiennent plus le CPU pour démarrer | aggravé par VPS-045 |
| **VPS-040** — démo | 3 | base **581 Mo** (+40) ; import hebdo **ÉCHEC** → **VPS-046** | ↑ |
| **VPS-M102** — marge | 3 | ✅ tenu : 11 scripts de marge (A–K), tous `nice -n 19 ionice -c3`, tous les clients docker sous `timeout` (45–180 s), aucun `find /` | règle appliquée |

### Le disque, ventilé

| Poste | 09-17 | **09-20** | Écart |
|---|---:|---:|---:|
| `df /` | 53 Go (55 %) | **52 Go (54 %)** | −1 |
| images (`docker images -q`) | 29 | **31** | +2 (`avant-0919` ×3, `latest` ×3 ; −`avant-0916` ×2, −… ) |
| cache de build | 9,854 Go | **NON MESURÉ** (disjoncteur) ; `cache.db` réécrit 19/09 18:26:39 (build LP) | — |
| `/var/lib/docker/containers` | 303 Mo | **318 Mo** | +15 |
| `/var/lib/docker/volumes` | — | **3,0 Go** | — |
| `/var/backups` | 5,4 Go | **5,0 Go** | −0,4 |
| inodes | 1 759 426 | **1 747 496** | −11 930 |

---

## 8. Plan d'action — par ordre de RENDEMENT

> Par **ce que ça rapporte pour ce que ça coûte**. Un bloc = une décision. **Rien de ce qui suit n'a été exécuté.**

### 1️⃣ Demander à l'hôte — **V35 (1)** *(5 min côté poste, 0 s côté machine ; la seule réponse qui dit si le CPU revient)*

Ticket Hostinger pour `srv1201617` avec l'extrait `sar` du §3 : *limitation appliquée depuis le 19/09 22:10 UTC ?
règle ? levée ?* — en précisant que la cause interne est éteinte depuis 05:25. **Gain** : trancher entre « ça se lève
seul » et « ça ne se lèvera pas ». **Risque** : nul.

### 2️⃣ Rendre à la production ce que l'hôte laisse — **V35 (2)** *(20 s, réversible, avant 18:00 UTC)*

```bash
docker ps --format '{{.Names}}' | grep -E 'dev-|demo-|dronely|maquettes|dg-epaviste' | sort   # relire AVANT : 18 conteneurs hors production attendus
```

Puis, pile par pile, `docker compose stop` (jamais `down`) sur `maalem-dev`, `maestroo-dev`, `tracky-demo`, `dronely`,
`foodsqan-maquettes`, `dg-epaviste`. **Gain** : leur part des 0,4 vCPU et de 72 sondes / min ; **mesure** : `sar`
demain à heure égale. **Contrepartie** : dev et démo indisponibles jusqu'au `start`. ⚠️ `foodsqan-traefik` reste :
il tient 80/443 de toute la production (VPS-021).

### 3️⃣ Recréer la démo sur les bonnes images — **V36** *(1 min, quand VPS-045 est levé — sinon après le 2️⃣, la démo étant arrêtée de toute façon)*

```bash
cd /opt/vizyo-tracky/deploy/vps && docker compose --env-file .env.demo -f docker-compose.demo.yml up -d
```

**Gain** : démo à jour, import du 27/09 réussi. **Contrepartie** : ~1 min de démo. Puis la ligne dans `deploy.sh`.

### 4️⃣ La règle là où les commandes se tapent — **V34** *(1 ligne dans `CLAUDE.md`, hors périmètre de cet audit)*

*« Sur le VPS : `docker logs`/`exec`/`inspect`/`images` = `timeout 20`, `--tail ≤ 2000`, jamais depuis un `bash -c` qui
peut se fermer avant — 7 occurrences de VPS-016 sur 7. »* **Gain** : la seule chose qui empêche la 8ᵉ.

### 5️⃣ Réveiller le poste — VPS-037 / VPS-M73 *(30 s côté poste — 4 nuits de chance, 3 passages pour 1 rapport)*

```powershell
Set-ScheduledTask -TaskName 'Vizyo-Verify-Copie-HorsSite' -Settings (New-ScheduledTaskSettingsSet -WakeToRun -StartWhenAvailable -ExecutionTimeLimit (New-TimeSpan -Hours 2))
```

Et une tâche Windows vide *« réveil 02:15 »* pour l'audit. **Preuve** : demain, collecte à 02:2x, seule.

### 6️⃣ Donner 3 jours au repli — **V32 (a)** *(10 s ; l'étiquette juste du 19/09 meurt demain 00:40)*

```bash
sed -i 's/until=24h/until=72h/' /etc/cron.d/docker-image-prune   # relire cat /etc/cron.d/docker-image-prune AVANT
```

### 7️⃣ Un seul rotateur — **V29** *(10 s, avant minuit ; 8ᵉ nuit, `tracky-postgres` troué de nouveau)*

```bash
mv /etc/logrotate.d/docker-containers /root/logrotate.d-docker-containers.retire-2026-09-20
```

⚠️ `rotate 14` → `max-file: 14` sur le seul `tracky-postgres` (TRK-035).

### 8️⃣ Trancher `prd` Dispocar — **V30** *(décision, 0 s ; 7ᵉ nuit à 1 172 o)* · **V14**, **V17**, **V4** *(après V35 seulement)* — inchangés.

---

## 9. Améliorer l'agent

### a. Ce qui a été corrigé dans `collecte.sh` pendant ce passage

| Défaut | Ce qu'il produisait | Correctif, et sa vérification |
|---|---|---|
| **VPS-M104** *(session du matin)* — **31 clients `docker` sans `timeout`** dans le collecteur écrit pour dénoncer VPS-016 | À 10:40, `docker system df` n'a pas rendu la main en 3 min ; la collecte entière s'est arrêtée derrière lui, sans une ligne ; tuée proprement à 10:49:44 | Fonction `docker()` : `timeout -k 5 45` + **disjoncteur** (fichier témoin) : au premier dépassement, tous les appels suivants rendent 124 immédiatement et l'écrivent dans `$ERRBUF` ; sections 4 et BUDGET impriment « DISJONCTEUR ARMÉ » |
| 🆕 **VPS-M105** — **le correctif M104 était faux** : `timeout … command docker` — `command` est un builtin, `timeout` ne peut pas le lancer | **rc 127 sur chaque appel docker**, avalé par les `2>/dev/null` : 31 vides silencieux fabriqués par le correctif qui devait les nommer. Jamais exécuté (collecte produite avant lui — VPS-M35 à l'identique) | `DOCKER_BIN=$(type -P docker)` ; **banc sur la machine** : (A) l'ancienne forme rend **127** *« failed to run command 'command' »* ✓ ; (B) `docker version` 2,7 s rc 0 ✓ ; (C) disjoncteur sur `/bin/sleep` : 1ᵉʳ appel 124 en 1 099 ms, 2ᵉ en 97 ms, témoin posé puis retiré ✓ ; (D) `docker ps -q` 38 en 18,2 s ✓ ; témoin `/tmp` nettoyé par le trap EXIT |
| 🆕 **VPS-M106** — **sous disjoncteur, quatre blocs jugeaient un vide** | *« 🟠 DISPARU(S) : <les 38 conteneurs> »*, *« 🔴 PERSONNE ne publie 80/443 — la production est injoignable »* (elle répondait 200), *« 38 certificats, dont 38 sans service vivant »*, *« 1 conteneurs toutes les  / −1 SANS AUCUNE SONDE »*, et `[: : integer expression expected` | Gardes : liste courante vide → « comparaison NON FAITE, ne PAS lire 38 disparus » ; `INSPECT_JSON` vide → « routage / 80-443 / certificats NON MESURÉS » ; cadences vides → « healthchecksParMin NON MESURÉ ». **Passe 2** (script entier, disjoncteur attendu) : voir annexe |
| 🆕 **VPS-M107** — **l'audit sur une VM sans CPU en prenait la majorité** | 647 s, charge 15,8 → **81,7**, *« audit 22,6 % de la machine »* pour `user`+`sys` 9,4 % ; le poste le plus lourd : `gzip -dc` de l'archive Tracky (172 Mo) | **Mode allégé** : steal mesuré sur 2 s au départ ; ≥ 50 % → relecture des archives > 50 Mo, parcours de `/opt` et balayage par contenu **NON FAITS, et dits** (`mode_allege_dit`) ; BUDGET imprime le steal de départ dans les deux cas. Coût : 2 s |

### ⚠️ Ce que ce passage a réfuté dans le travail d'avant

1. **La règle *« un `%steal` élevé ne prouve pas une contention d'hôte »*** (08-10) était juste *pendant* la boucle ;
   elle ne couvre pas le cas d'aujourd'hui — `dockerd` à 1 %, de l'idle, et 80 % de steal. *Une règle écrite sur un
   cas encode ce cas (VPS-M49, encore).*
2. Le correctif du matin (M104) : validé par son auteur sur la lecture du code, jamais exécuté — **127 partout**.
3. Le compte « 7ᵉ et 8ᵉ occurrences » de la mémoire du poste : ce sont les **nº 6 et nº 7** du référentiel.

### b. Angles morts restants

1. **Rien ne surveille le CPU *servi* entre deux passages** — 13 h entre le premier palier (22:10) et ce rapport ;
   `/admin/system` montre la charge, pas le steal. *Piste, à coût nul* : lire `/proc/stat` (steal) dans la sonde de
   charge de Tracky qui existe déjà, et une sentinelle *« steal > 50 % pendant 20 min »* au centre d'alerte.
   **36ᵉ report** de l'angle « rien entre deux passages », sous une forme nouvelle.
2. **Le rattrapage du planificateur du poste bloque le passage suivant** : le run du 19/09 (14:12) a « tourné » 20 h et
   celui du 20/09 02:20 n'a pas été lancé. *Piste* : `ExecutionTimeLimit` / une garde « si un passage du même audit a
   plus de 6 h, l'abandonner ». **1ᵉʳ report.**
3. **La liste des conteneurs, les limites, les healthchecks, le cache de build dépendent tous du même `docker inspect`
   / `system df`** : un disjoncteur les vide tous ensemble. *Piste* : lire `config.v2.json` et `hostconfig.json`
   (sans client — la marge E l'a fait pour 38 conteneurs en 15 s) comme **source de repli** quand le disjoncteur est
   armé. **1ᵉʳ report.**
4. **VPS-032 n'a jamais été ventilé par origine** ; la chute 935 → 132 sessions/jour au moment de la pause des agents
   locaux est la première indication de *qui* — par déduction, pas par mesure. *Piste* : compter les sessions par
   `SSH_CONNECTION` port source ↔ processus parent sur le poste. **10ᵉ report** de « qui parle à cette machine ».
5. **Les fiches `APPLIQUE` ne sont re-mesurées par rien.** **13ᵉ report.**
6. **La comparaison des certificats ne couvre que 2 domaines sur 28.** **10ᵉ report.**
7. **Le collecteur ne voit pas le journal du copieur** (`copie.log`, poste) — cette fois lu à la main. **4ᵉ report.**
8. 🆕 **Aucune série n'est alignée sur l'heure du jour** (VPS-M103) — tenu à la main ce passage (`trip_automation_runs`
   01h–03h : 3 · 1 · 5 → 11 · 12 · 18 min). *Piste inchangée.* **2ᵉ report.**

### c. Une question ouverte pour le prochain passage

**Quand l'hôte reprend 80 % du CPU d'une VM de production, qui le voit, et combien de temps après ?** Ce matin la
réponse est : *le propriétaire, réveillé par « Tracky est down », 7 h après le premier palier ; puis cet audit, 13 h
après.* Le centre d'alerte a vu l'effet (un 503 de 57 s), le coupe-circuit a payé deux SMS, et **aucun des deux ne
sait qu'il tourne sur 0,4 vCPU**. La question n'est pas technique — elle est : *le steal est-il une grandeur de la
machine (audit VPS) ou une grandeur du produit (centre d'alerte) ?* Tant qu'elle n'a pas de propriétaire, elle n'a pas
de seuil, et sans seuil elle n'a pas d'alerte.

---

## Post-scriptum — l'après-midi du 20/09 : ce que le propriétaire a fait (12:48 → 13:12 UTC)

> *Ajouté après la publication du matin, pour que le passage du 21/09 lise la passation ici et pas dans sa mémoire.*

| Heure UTC | Geste | Preuve mesurée | Ce qu'il reste |
|---|---|---|---|
| **12:48** | **V35 (2)** — `docker stop -t 20` de **14** conteneurs hors production (`maalem-dev` ×6, `maestroo-dev` ×4, `tracky-demo` ×4) ; **gardés** `dg-epaviste-website` (site public d'un client), `dronely` ×2, `foodsqan-maquettes` (statiques, sans sonde) — donc 14 et non 18, à dessein | 38 → **24 running** (`config.v2.json`), 110 s ; 9 URL de production 200/301/302/307 ; charge **18 → 4–8** ; `sar` 13:00 / 13:10 : `steal` **52 %**, `idle` **41 %** ; `docker ps -q` **4 s** (20 avant). ⚠️ échantillon 10 s à 13:11 : steal 89 % — **le plafond de l'hôte tient**, la VM demande moins | **V35 (1) le ticket** ; mémo de rallumage `/root/conteneurs-arretes-2026-09-20.txt` |
| **13:08** | **V34** — garde-fou **`docker-orphelins.timer`** (5 min) : tue tout client `docker logs|stats|events|attach` **sans terminal et > 10 min**, parent d'abord. Source `deploy/vps/docker-orphelins/` | banc : faux client tué · chaîne `bash -c` → *« parent tué d abord »* (journal) · client avec pty → `vus=1 tues=0` · **vrai** `docker logs -f` orphelin tué · 1ᵉʳ passage réel `Result=success`, 0,6 s CPU | règle écrite dans `CLAUDE.md` (à committer) ; **le collecteur doit lire `journalctl -t docker-orphelins` et `/run/docker-orphelins/dernier`** au prochain passage (VPS-M06) |
| **13:12** | **V32 (a)** `until=72h` (sauvegarde `/root/docker-image-prune.avant-v32a-2026-09-20`) | `grep` = `until=72h` | preuve demain : `avant-20260919-1537` survit à 00:40 |
| **13:12** | **V14** — `apt-daily.timer` : drop-in `OnCalendar=01:30`, `RandomizedDelaySec=15m` (⚠️ la fiche croyait à `RandomizedDelaySec=12h` seul : le timer était à **06,18:00** + 12 h aléatoires) | `NextElapse 21/09 01:41:32` | « MESURE VALIDE » à 02:22 deux jours de suite |
| **13:12** | **V17** — 4 archives → `/var/backups/instantanes-chantier-20260915/` (`mv -n`, `700`) | racine de `/var/backups` : **0 fichier** | le dossier sera **NEUF** dans `dossiersSauvegardeListe` demain : voulu |

| **13:35–13:36** | **V35 (1)** — ticket envoyé par le chat hPanel ; **Hostinger confirme et lève la limitation** : *« a Hostinger CPU limitation was active and it has now been successfully removed; your sustained high usage from 09-19 18:40 to 09-20 05:25 UTC is consistent with the trigger »* — seuil et fenêtre non exposés, *« may be reapplied after sustained usage »* | 13:37 : steal **1 / 1 / 1 %**, idle 89–96 %, charge **1,24**, `docker ps -q` **0,14 s**, `/api/health` **29–55 ms** | **VPS-045 → `SURVEILLANCE` gravité 2** ; seuil de réescalade écrit |
| **13:38** | **Les 14 conteneurs rallumés** (bases d'abord) | 38 / 38 running, 14 / 14 `healthy`/`starting`, dev et démo à 200, steal 4 % pendant le redémarrage | mémo renommé `…rallumes-1339.txt` |
| **13:40–13:47** | **V36 (a)** — `up -d` de la pile démo, puis `systemctl start tracky-demo-refresh` | api démo sur `6b15f68b1061` (= `latest`), **5 migrations jouées**, `managedByManagerAt` présente, **« Import réussi » 13:47:15** (309 s, 572 006 positions), démo 200 en 70 ms | **(b)** `deploy.sh` à coder |

| **~14:30** | **V32 (b) + V36 (b)** codées dans `deploy.sh` : repère de repli sur **l'image du conteneur en service** ; la **démo suit par défaut** après la santé de la production (`--sans-demo`) ; `collecte.sh` lit `demo=` au journal | `pnpm verif:deploiement` : **131 contrôles, tous verts** (95 avant) | **en ligne au prochain `git pull` du VPS** (à faire AVANT le prochain déploiement) ; récit complet : [doc 36](../../fiabilite-coupe-circuit-2026-09/36-INCIDENT-VPS-BRIDE-2026-09-20-DEUX-DOCKER-LOGS-ET-90-POURCENT-DE-CPU-RETIRES.md) |

**Non faits, et pourquoi** : **V29** — retirer la
stanza demande `max-file: 14` sur `tracky-postgres`, donc une recréation du conteneur de la base de production : créneau à
choisir ; **V4** — la machine est à pleine puissance, le redémarrage redevient possible (hors 05:30–09:00 Paris, hors
HH:42–HH:46).

⚠️ **Pour le passage du 21/09** : (1) **38 conteneurs** (les 14 sont rallumés) — mais `tracky-demo-api` et `tracky-demo-web`
sont **recréés** (13:40), c'est voulu ; (2) un timer de plus dans la section 7 (`docker-orphelins.timer`, ~288/j) — il est au
catalogue `ordonnancement` ; (3) `apt` frais à 02:22 pour la première fois par construction ; (4) le dossier
`instantanes-chantier-20260915` est neuf ; (5) `until=72h` : l'étiquette du 19/09 doit survivre ; (6) **steal attendu < 10 %**
sur toute la journée du 20/09 après 13:36 — sinon VPS-045 remonte.

## Annexe — comment reproduire ce rapport

```bash
ssh root@72.62.26.240 'bash -s' < docs/vps-audit/collecte.sh > /tmp/collecte-vps.txt
```

| Passe | Fenêtre UTC | Durée | Rôle |
|---|---|---:|---|
| 0 | 10:36:57 → 10:49:44 | **767 s, tuée** | session du 19/09 en rattrapage : sections 1–3 puis blocage dans `docker system df` (VPS-M104) ; marges A–G ; archivée en **tête** de `collectes/2026-09-20.txt` |
| 1 | 11:18:20 → 11:29:08 | **647 s** | **la mesure du jour** (`DOCKER_TIMEOUT=90`, choix consigné : à 45 s le disjoncteur aurait sauté sur le premier appel) ; disjoncteur armé à 11:22:23 (`docker system df` 90,4 s) → sections 4–5 NON FAITES ; archivée à la suite dans le même fichier |
| 2 | 11:41:21 → 11:48:14 | **413 s** | **script entier rejoué** avec les valeurs par défaut (VPS-M35) : mode allégé ✓ (steal 89 %), disjoncteur à 45 s ✓, vides nommés ✓ — mais charge 8,9 → 68 (VPS-M107, 2ᵉ jet à faire) |
| 3 | 14:08:53 → 14:10:59 | **125 s** | **après la levée de la limitation** (13:36) : steal 0 %, charge 0,19 → 1,26, `docker system df` 8,8 s, bloc garde-fou lu, champ `demo=` affiché — **la machine saine, mesurée avec le collecteur corrigé : la référence du 21/09** |

Vérifications ciblées en marge, **toutes en lecture seule, toutes sous `nice -n 19 ionice -c3`, tous les clients
docker sous `timeout`** (VPS-M102 tenu) :

- **état 11:14** : `pgrep -x docker`, `vmstat 1 4`, `sar -u`, `dockerd` sur 3 s, `ss` sessions, `curl` santé, `qemu-ga`, `journalctl -u docker` par heure ;
- **banc** : la fonction `docker()` isolée (A–E, §9 a) ;
- **H** : colonnes utiles (1 `docker exec`), `journalctl -u tracky-demo-refresh`, `fail2ban` / `dpkg.log` du 18/09, `docker images` (21 s), `sar` 05:00–06:10, part servie par heure ;
- **I, J** : `positions` / `wire_logs` / décisions / `trip_automation_runs` / `engine_control_commands` (2 `docker exec` de 75 s et 7 s), taille des bases ;
- **K** : `/var/log/tracky-demo-refresh.log`, `logrotate` (`CPUUsageNSec`), `tracky-backup` (durées) ;
- **côté poste** : `list_scheduled_tasks` / `list_task_runs`, transcription du run du 18/09, `Get-WinEvent` (1 / 42 / 107), `Get-ScheduledTask` (`WakeToRun`), `copie.log`, diff de `CLAUDE.md`.
