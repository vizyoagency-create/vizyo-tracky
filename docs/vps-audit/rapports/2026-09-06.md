# Audit VPS — 2026-09-06

> ## Verdict
>
> **Le test écrit d'avance le 2026-09-05 est RÉPONDU, et il est TENU.** Il exigeait que
> `vizyo-auth-backup` se soit déclenché **seul**, avec **deux** copies et une cadence d'environ
> 20 h. La machine rend **trois** copies et une cadence de **24 h** : le timer a tourné sans
> personne le **09-05 à 04 h 01 min 41** *et* le **09-06 à 04 h 04 min 10**, journalisés par
> `systemd[1]: Starting…`. **VPS-015 — un script posé, une unité déclarée, et rien qui se
> déclenche — ne se rejoue pas ici.**
>
> **✅ ET VPS-038 CONNAÎT SON PREMIER PASSAGE SANS PERTE NOUVELLE DEPUIS LE 08-31.** Les trois
> chemins de code indépendants donnent **30 émetteurs contre 30** ; le registre reste à **14
> silencieux sur 44**, et sa composition s'explique **entièrement par le vieillissement** d'un
> boîtier qui a changé de bande. **Le constat reste en gravité 1** — le seuil écrit exige un
> retour à **32**, pas un arrêt de la chute.
>
> **~~🔴 MAIS UNE GRANDEUR TOMBE DEPUIS TROIS JOURS…~~ → VPS-039 A ÉTÉ RÉFUTÉ LE JOUR MÊME.** La
> part des décisions d'échantillonnage qui devient une position passe bien de **40,8 % → 34,7 % →
> 29,8 %**, mais elle **suit la part de véhicules en mouvement** (28,0 → 25,3 → 18,6 %) : la flotte
> a roulé de moins en moins du jeudi au dimanche, et l'échantillonneur a bridé les trames des
> véhicules à l'arrêt — c'est sa fonction. **Il n'y a pas de dérive, il y a un week-end.**
> Voir l'**addendum** ci-dessous et 🆕 **VPS-M85**.
>
> **🔴 DEUX BLOCS DE LA MÊME SECTION RENDAIENT DES VERDICTS OPPOSÉS SUR LES MÊMES FICHIERS.** À
> **47 h**, `vizyo-manager` et `vizyo-texto` étaient **« ✅ à jour (1 j) »** dans la table de
> couverture et **« ⚠️ PÉRIMÉE (> 30 h) »** vingt lignes plus bas — un âge tronqué en jours contre
> un âge en heures, **une bande aveugle de dix-huit heures**, et c'est la ligne verte qui nomme les
> conteneurs, donc celle qu'on lit. 🆕 **VPS-M84, corrigé ce passage.**
>
> **✅ ANGLE MORT N° 9 TRAITÉ APRÈS 22 REPORTS — et il retourne une ligne verte d'hier.** Le
> certificat **servi** est bien celui du **volume** (empreintes identiques sur les deux domaines).
> Mais la mesure révèle autre chose : *« `acme.json` réécrit le 09-04 21 h 33 »* datait le
> renouvellement d'**`auth.vizyoagency.com`**, pas de Tracky — dont les certificats ont **24
> jours**. **C'était la question ouverte d'hier, et elle avait raison.**

---

## 0. ADDENDUM du 2026-09-06, quelques heures après publication — VPS-039 est RÉFUTÉ

> **Ce rapport a été publié le matin avec un constat de gravité 2 et un test daté au 2026-09-09.
> Le test n'était pas nécessaire : la réponse existait déjà en base, dans une colonne que je
> n'avais pas ouverte. Le constat est réfuté, et il est bénin.**

`position_sampling_decisions` porte deux colonnes ignorées par la collecte — `decision` (le motif
de chaque rejet) et `state` (l'état du véhicule) :

| jour | `MOVING` | `STOPPED` | `SKIPPED_THROTTLE` | taux retenu |
|---|---:|---:|---:|---:|
| jeu 09-03 | 28,0 % | 70,1 % | 60,4 % | **40,8 %** |
| ven 09-04 | 25,3 % | 72,9 % | 62,3 % | **34,7 %** |
| sam 09-05 | 18,6 % | 79,9 % | 68,6 % | **29,8 %** |
| dim 09-06 | 1,6 % | 98,1 % | 85,8 % | **14,1 %** *(partiel)* |

**Le taux de rétention suit la part de véhicules en mouvement.** L'échantillonneur bride les
trames des véhicules à l'arrêt : c'est sa fonction, pas un défaut. Aucune action.

⚠️ **Réserve de mesure, écrite plutôt que tue** : les 40,8 % du §3 viennent d'un rapport
`positions` / `decisions` **entre deux tables** ; la table des décisions rend **36,4 %**
d'`INSERTED` pour le même jour (+0,8 % de `RECOVERED_BUFFER`). Les niveaux absolus diffèrent —
bornes de journée, et des positions arrivent par des chemins qui ne journalisent pas de décision.
**La pente, elle, est identique**, et c'est elle qui portait le constat.

### 🔑 Ce que cet addendum coûte, et pourquoi il vaut mieux que le constat qu'il retire

**Ma réfutation du matin portait sur la mauvaise grandeur.** Le §3 teste *« le samedi est-il un
jour creux ? »* avec les **positions par émetteur** (610 le samedi 08-29 contre 619 le vendredi) et
conclut que non. Or les positions par émetteur dépendent **à la fois** du roulage **et** de
l'échantillonnage : elles ne pouvaient pas trancher une hypothèse qui ne parle que du roulage. La
grandeur qui répondait — la part de `MOVING` — était dans la table d'à côté, depuis toujours.

*C'est **VPS-M85**, écrit ce passage : une réfutation est une mesure comme une autre, elle doit
porter sur la grandeur dont parle l'hypothèse. Et le geste, lui, était juste — s'abstenir de
réfuter aurait été **VPS-M83**, qui est pire.*

⚠️ **Conséquence produit, elle bien réelle** : le seuil « alerter si ≤ 32 % » écrit au §3 aurait
crié **13,7 % le jour de sa mise en service**, un dimanche, parce que les camions sont au parking.
*Un instrument calibré sur ce constat serait né faux.* **Les paragraphes 3 et 9.3 ci-dessous sont
conservés tels qu'ils ont été publiés** — on ne réécrit pas une mesure après coup, on la date et on
la corrige à côté.

---

## 1. Tableau par domaine

| Domaine | État | En une phrase |
|---|:--:|---|
| Production | 🟢 | **99 · 102 · 47 · 118 ms**, 4 URL sur 4. 33/33 conteneurs, 0 arrêté, 33/33 en `unless-stopped`. |
| Capacité | 🟢 | **89,59 %** d'inactivité la veille, à **+0,08 pt** de la journée la plus ancienne. Écriture disque retombée à 667 puis 137 blocs/s après les builds du 09-03. |
| Docker | 🟢 | **VPS-016 éteint, 20ᵉ jour** : **+0,6 h de CPU en 24 h**, 2,3 % à l'instant. 0 client bloqué, **0 connexion** sur la socket. |
| Mémoire | 🟢 | **35 %** utilisés (2,7 Gi / 7,8), **0 OOM en 30 j**, PSI `full` **0,00** sur processeur et mémoire. Swap 1 147 Mo. |
| Disque | 🟢 | **53 % (50 Go), 46 Go libres — +1 Go d'utilisé rendu**, malgré 1,94 Go d'images neuves. Cache de build **10,1 Go**, ramasse-miettes daté du dernier build. |
| Données | 🟠 | ~~🆕 VPS-039~~ **RÉFUTÉ le jour même** (§0) : le taux suit la part de `MOVING`, la flotte roulait moins. **VPS-038 reste en gravité 1**, mais **sans perte nouvelle** : 30 contre 30 sur trois chemins. |
| Sauvegardes | 🔴 | ✅ **`vizyo-auth` s'est déclenché SEUL, deux fois.** 🔴 **VPS-013 intact, 29ᵉ passage** — et le correctif VPS-M84 le rend enfin visible : `vizyo-manager` et `texto` passent de « ✅ à jour » à **« 🟠 NUIT MANQUÉE (47 h) »**. |
| Ordonnancement | 🟢 | **Aucune entrée neuve.** Les 2 fichiers modifiés sont ceux du 09-04, déjà au catalogue. **VPS-036** : aucun ordre d'écriture neuf, cadence **52/jour** stable sur 6 journées complètes. |
| Sécurité | 🟠 | ✅ **Certificat servi = certificat du volume.** **VPS-012** : la clé de CI reste à **16** connexions (0 → 12 → 16 → 16). **23 échecs sur 7 j, 0 sur `root`**, 0 empreinte non déclarée. |
| Méthode | 🔴 | **🆕 VPS-M84** (deux seuils contradictoires dans la même section) et **🆕 VPS-M85** (une réfutation menée avec la mauvaise grandeur — §0). **Angles morts n° 3 et n° 9 FERMÉS.** **VPS-M56 : 20ᵉ dépassement, 116 s pour 90.** |

---

## 2. Ce qui va bien — avec les chiffres qui le prouvent

- **La production répond sur les 4 URL** : `app-tracky` 99 ms, `/api/health` 102 ms,
  `tracky.vizyoagency.com` 47 ms, `app-verify` 118 ms (302 attendu).
- **33/33 conteneurs décrits, 33/33 en `unless-stopped`, 0 arrêté** — contrôle « attendu vs
  obtenu » (VPS-M08), **28ᵉ passage**. Les deux seuls compteurs de redémarrage > 0 datent toujours
  du **08-04**.
- **La mémoire tient** : 2,7 Gi utilisés (**35 %**), **0 OOM en 30 jours**, PSI `full` à **0,00**
  sur le processeur **et** la mémoire.
- **`dockerd` reste éteint, 20ᵉ jour** : cumul **237,2 → 237,8 h** en 24 h, soit **+0,6 h** (2,3 %
  d'un cœur à l'instant). **0 client Docker bloqué, 0 connexion établie sur `/run/docker.sock`.**
- **Le disque rend un giga-octet d'occupation** — 51 → **50 Go utilisés**, 46 Go libres — malgré
  **1,94 Go d'images construites** le 09-05 à 18 h 12. Le ramasse-miettes de BuildKit a tourné :
  `cache.db` réécrit **à la seconde** du dernier build (écart 0 s).
- **0 service en échec**, et **les 6 unités planifiées pointent toutes un script exécutable** — le
  contrôle né du 203/EXEC de VPS-015.
- **Aucun ordre d'écriture neuf de l'hyperviseur** : la seule commande signalée reste celle du
  **09-01** (`systemctl mask multipathd`), déjà au dossier. Cadence **52/jour** sur les 6 journées
  complètes (333 exécutions pour **1 841 sondages** — le ratio de 17 pour 1 reste écarté), **un
  seul nom de sonde** (`usage-telemetry.py`, 304 fois).
- **Aucune empreinte SSH non déclarée** : les 2 empreintes vues sur la fenêtre sont toutes deux
  dans `authorized_keys`. **23 échecs sur 7 jours, 0 sur `root`**, cibles `admin`, `test`,
  `system`, `pi`, `ubnt` — du balayage.
- **Les 4 unités de sauvegarde rendent succès**, les **6** archives relues sont **complètes**, les
  **30 archives de `vizyo-verify` sont chiffrées, 0 en clair**, dossier en `700`.
- **La copie hors-site tient son seuil** : 23 h, contenu copié 0 h.

### ✅ Et le test écrit d'avance hier est répondu — c'est le fait du jour

Le rapport du 09-05 avait écrit, mot pour mot :

> *« Test écrit d'avance : au passage du 2026-09-06, `/var/backups/vizyo-auth` doit porter **deux**
> copies, et la ligne de cadence doit afficher **~20 h**, pas « une seule salve ». »*

La machine répond mieux que le test ne demandait :

```
-rw------- 1 root root 1412828 2026-09-04 07:34 vizyo_auth_20260904-073414.sql.gz   ← le lancement MANUEL
-rw------- 1 root root 1439531 2026-09-05 04:01 vizyo_auth_20260905-040141.sql.gz   ← 1re echeance AUTOMATIQUE
-rw------- 1 root root 1447686 2026-09-06 04:04 vizyo_auth_20260906-040410.sql.gz   ← 2e echeance AUTOMATIQUE
```

```
2026-09-06T04:04:10  systemd[1]: Starting vizyo-auth-backup.service - Vizyo Auth - Sauvegarde quotidienne PostgreSQL...
2026-09-06T04:04:11  vizyo-auth-backup.sh[1733048]: OK : 1447686 octets.
2026-09-06T04:04:11  vizyo-auth-backup.sh[1733048]: Retention 30 j : 0 archive(s) supprimee(s).
2026-09-06T04:04:11  vizyo-auth-backup.sh[1733048]: 3 sauvegarde(s) en place dans /var/backups/vizyo-auth.
```

**Trois copies, deux déclenchements autonomes, rétention active, cadence mesurée à 24 h.** C'est
`systemd[1]` qui démarre l'unité, pas une session : la réserve écrite hier est levée.

⚠️ **Et il faut dire pourquoi elle avait été écrite** : la collecte du 09-05 tournait à **02 h 22**,
soit **1 h 40 avant** la première échéance. *Le rapport d'hier ne pouvait pas voir cette exécution ;
il a eu raison de ne pas la supposer.* La première échéance a bien eu lieu ce jour-là, à 04 h 01.

### ✅ Et les « 20 à 26 correctifs de sécurité » d'hier ont été INSTALLÉS

Hier, sur un cache `apt` de 9 h — donc **hors mesure** — deux sources indicatives passaient de
« 0-1 correctif de sécurité » à **« 20-26 »**, et le rapport avait refusé de les reporter tout en
écrivant que *« c'est une raison de refaire la mesure »*. La mesure est refaite, et elle est
**valide pour la première fois depuis onze passages** :

| source | 09-05 (cache 9 h, **non mesurable**) | 09-06 (cache **1 h**, ✅ VALIDE) |
|---|---:|---:|
| cache `apt` | 95, dont **20** sécurité | **75**, dont **0** |
| `update-notifier` | 101, dont **26** | **75**, dont **1** |

Entre les deux, `apt-daily-upgrade` a tourné le **09-05 à 06 h 41 min 30**. **Le canal a fait son
travail** — et la seule raison pour laquelle on peut l'affirmer est que le rapport d'hier s'était
interdit d'appeler « mesure » ce qui n'en était pas une.

⚠️ **Ce que ce vert ne dit PAS** : les **75** paquets restants ne sont pas encore un retard. Le
bloc de VPS-M74 le positionne dans son cycle : le cache a été rafraîchi **20 h après** le dernier
passage de l'installateur, donc ces paquets ont été **découverts depuis** et l'installateur ne les
a pas vus. **Le vrai test est écrit** : si au passage suivant l'installation a eu lieu (prévue
aujourd'hui **06 h 31**) **et** que le compte n'a pas baissé, la panne est établie.

---

## 3. ~~🔴 🆕 VPS-039~~ → **RÉFUTÉ quelques heures plus tard, voir §0.** *Le chapitre est conservé tel qu'il a été publié : c'est la mesure du matin, et la réfutation se lit mieux à côté de ce qu'elle corrige.*

### La mesure

Trois chemins de code, par jour, sur toute la fenêtre disponible :

```
   jour      sem  emetteurs  positions  decisions  pct_retenu  par_emetteur
 2026-08-28  Fri      39       24136        —          —            619
 2026-08-29  Sat      39       23797        —          —            610
 2026-08-30  Sun      38       15024        —          —            395
 2026-08-31  Mon      38       22211        —          —            585
 2026-09-01  Tue      32       23982        —          —            749
 2026-09-02  Wed      32       21129        —          —            660
 2026-09-03  Thu      32       21930      53810      40.8 %         685
 2026-09-04  Fri      30       21464      61832      34.7 %         715
 2026-09-05  Sat      30       17399      58423      29.8 %         580
 2026-09-06  Sun      30        1477      10505      14.1 %          49   ← PARTIEL, ne se compare pas
```

**Le dénominateur ne bouge pas** : 53 810 → 61 832 → 58 423 décisions, sur **30 émetteurs les trois
jours**. **C'est le numérateur qui décroche** : 40,8 % → 34,7 % → **29,8 %**, soit **−11 points en
trois jours**, régulièrement.

Et c'est exactement ce qui sépare les trois compteurs de la section 5 :

| table | rapport à la veille | ce qu'elle mesure |
|---|---:|---|
| `wire_logs` | **×0,95** | les trames **reçues** — la chaîne d'ingestion |
| `position_sampling_decisions` | **×0,94** | les décisions **prises** — l'échantillonneur est appelé |
| `positions` | **×0,81** | les positions **écrites** — ce qui reste après la décision |

*L'ingestion va bien, l'échantillonneur tourne : c'est sa sortie qui rétrécit.*

### Ce que l'audit ne peut PAS dire, et ne dira donc pas

**Ni la cause, ni s'il s'agit d'une dérive.** Deux limites, toutes deux mesurées :

1. **La fenêtre ne fait que trois jours.** `position_sampling_decisions` a une rétention de
   **3,03 jours** : il n'existe aucun point avant le 09-03. **Trois points ne distinguent pas une
   pente d'un cycle hebdomadaire** — c'est l'angle mort n° 5 de ce référentiel, au 6ᵉ report, qui
   se paie ici pour la première fois.
2. **Le 09-05 est un samedi.** Mais l'explication « c'est le week-end » est **faible, et c'est
   mesuré** : sur `positions` par émetteur, le samedi 08-29 rendait **610** contre **619** le
   vendredi (−1 %), quand le dimanche 08-30 tombait à **395** (−35 %). *Le samedi n'est pas un jour
   creux sur cette flotte ; le dimanche l'est.* Un samedi n'explique donc pas 11 points.

⚠️ **Et je m'interdis d'aller plus loin.** Nommer une cause — une consigne d'échantillonnage
modifiée, un seuil d'immobilité, une flotte qui roule moins — serait exactement **VPS-M83**, écrit
il y a vingt-quatre heures : *une explication écrite pour désamorcer un chiffre doit porter le test
qui la réfuterait, ou ne pas être écrite.*

### 🔑 Le test de réfutation, écrit d'avance

> **Au passage du 2026-09-09** — le premier qui mesurera une **journée ouvrée pleine** (mardi
> 09-08) —, le taux de rétention doit être revenu **à 38 % ou plus**. S'il l'est, la variation
> était un cycle hebdomadaire et ce constat se referme en `SURVEILLANCE`. **S'il reste à 32 % ou
> moins, l'hypothèse du cycle est réfutée** et il s'agit d'une dérive : le constat passe en
> gravité 2 et l'enquête bascule côté produit.
>
> ⚠️ **Ne PAS trancher sur le passage du 09-07** : il mesurera un **dimanche**, le seul jour dont
> on sait déjà qu'il est creux sur cette flotte (395 contre 610). *Un test qui tombe le jour où la
> mesure ne veut rien dire est VPS-M41 — et il a déjà été payé une fois.*

### `aNePasFaire`

⚠️ **Ne pas lire ce constat comme une aggravation de VPS-038.** Ce sont deux grandeurs
différentes : VPS-038 compte des **émetteurs** (30, stable), celui-ci mesure un **taux de
rétention** à effectif constant. *Les confondre ferait croire que la flotte continue de tomber
alors qu'elle a arrêté.*
⚠️ **Ne pas se réjouir de la baisse de volume de `positions`.** La projection à fenêtre pleine
descend de 324 Mo, et c'est un gain de disque **réel** — mais un gain de disque obtenu en écrivant
moins de positions n'est un gain que si l'on a décidé d'en écrire moins.

---

## 4. ✅ VPS-038 — premier passage sans perte nouvelle, et il reste en gravité 1

### Ce que la machine répond

```
h_silence    j    imei              statut    flotte     vehicule
     57.3   2.4   864035053277662   OFFLINE   7cc3c2f7   rattache
     74.3   3.1   864035054756649   OFFLINE   88627f81   rattache
    134.4   5.6   864035054756714   OFFLINE   2ad69ac1   rattache   ┐
    134.4   5.6   864035054489431   OFFLINE   2ad69ac1   rattache   │
    135.0   5.6   864035054756102   OFFLINE   2ad69ac1   rattache   │ la cohorte
    135.4   5.6   864035054756763   OFFLINE   2ad69ac1   rattache   │ du 08-31
    135.7   5.7   864035054756755   OFFLINE   2ad69ac1   rattache   │
    136.3   5.7   864035054757027   OFFLINE   2ad69ac1   rattache   ┘
    175.0   7.3   864035054755856   OFFLINE      —       SANS VEHICULE
    375.5  15.6   864035054756177   OFFLINE   2ad69ac1   rattache
    424.6  17.7   864035054756730   OFFLINE   2ad69ac1   rattache
    554.1  23.1   864035053277480   OFFLINE   88627f81   rattache
   1603.9  66.8   864035054756292   OFFLINE      —       SANS VEHICULE
   2224.5  92.7   863378070030776   OFFLINE      —       SANS VEHICULE
```

**Aucun nom neuf.** Les quatorze silencieux d'hier sont les quatorze d'aujourd'hui, chacun
vieilli de vingt-quatre heures — **le premier passage depuis le 2026-08-31 où la liste ne
s'allonge pas.**

Et la ventilation par bande le confirme **sans passer par le total** (VPS-M78) :

| bande | 09-05 | 09-06 | ce qui a bougé |
|---|---:|---:|---|
| 6-24 h | 0 | **0** | — |
| 1-3 j | 2 | **1** | un boîtier a franchi les 3 jours |
| 3-7 j | 6 | **7** | il est arrivé ici |
| > 7 j | 6 | **6** | — |
| **total** | **14** | **14** | *identique, et pour la bonne raison cette fois* |

**Le déplacement d'un boîtier d'une bande à la suivante explique la totalité de l'écart.** Aucune
entrée, aucune sortie.

### Les trois chemins de code disent la même chose

| table | émetteurs 24 h | veille |
|---|---:|---:|
| `wire_logs` | **30** | 30 |
| `positions` | **30** | 30 |
| `position_sampling_decisions` | **30** | 30 |

**La trajectoire s'arrête** : 39 · 39 · 38 · 38 · 32 · 32 · 32 · 30 · **30**.

### Le seuil, et pourquoi le constat NE redescend PAS

Le seuil écrit le 09-05 est explicite : *« redescend en gravité 2 dès que le compte d'émetteurs de
`positions` repasse à **32 sur une journée complète**, et en `SURVEILLANCE` à **38**. Reste en
gravité 1 tant que ce compte ne remonte pas, **quel que soit** l'état des autres compteurs. »*

**Il est à 30. Le constat reste donc en gravité 1**, et c'est le seuil qui le dit, pas moi.

⚠️ **Ne pas lire « plus de perte » comme « rétablissement ».** Neuf boîtiers manquent toujours à
l'appel, dont six d'un seul lot ; ce qui a cessé, c'est l'**hémorragie**, pas la **perte**. *Un
compteur qui arrête de descendre n'est pas un compteur qui remonte.*

### Et VPS-M83 est confirmé un second jour

Le rapport du 09-04 avait rangé un boîtier hors cohorte en le disant *« garé pour la nuit »*. Il
est aujourd'hui à **57,3 h**, le second à **74,3 h** ; **aucun des deux n'a réémis**, et le second
vient de franchir la barre des trois jours. *La règle écrite hier tient : une explication offerte
pour désamorcer un chiffre doit porter le test qui la réfuterait.*

---

## 5. 🔴 🆕 VPS-M84 — deux blocs de la même section, deux verdicts opposés sur les mêmes fichiers

### La mesure, ce passage, avant correctif

```
── Couverture : chaque base EN SERVICE a-t-elle une sauvegarde ? ──
  capcom6-mysql              ✅ a jour (1 j)          capcom6
  texto-postgres             ✅ a jour (1 j)          vizyo-texto
  vizyo-manager-postgres     ✅ a jour (1 j)          vizyo-manager

── Age de la derniere sauvegarde, par dossier de /var/backups ──   ← VINGT LIGNES PLUS BAS
  vizyo-manager               47 h  ⚠️ PERIMEE (> 30 h)
  vizyo-texto                 47 h  ⚠️ PERIMEE (> 30 h)
```

**Les mêmes fichiers. La même section. Quarante-sept heures des deux côtés. Deux verdicts
contraires.**

### La cause — deux unités et deux seuils, dans le même bloc de code

| | table de couverture | bloc par dossier |
|---|---|---|
| calcul | `(MAINTENANT - recent) / 86400` — **jours entiers, tronqués** | `age_h` en **heures** |
| seuil d'alerte | `agemax >= 2`, soit **48 h** | `age_h > 30` |

47 h donnait donc `agemax = 1` → **vert**, et `47 > 30` → **orange**. **Entre 30 h et 48 h s'ouvre
une bande aveugle de dix-huit heures** où la table qui répond à *« cette base est-elle
sauvegardée ? »* dit oui pendant que sa voisine dit qu'une nuit a été manquée.

### `pourquoiInvisible` — et c'est la ligne verte qui gagne

**La table de couverture nomme les CONTENEURS ; le bloc par dossier nomme des RÉPERTOIRES.** Pour
rapprocher les deux, il faut déjà savoir que `vizyo-texto` est la sauvegarde de `texto-postgres`.
Un lecteur s'arrête à la première — c'est elle qui pose la question, c'est elle qui porte les ✅ —
et il n'a aucune raison de descendre vérifier qu'on lui dit l'inverse plus bas.

*C'est la troisième forme de la même famille : **VPS-M15** lisait la trace et jamais l'unité,
**VPS-M81** confondait l'âge et le mécanisme, celui-ci **mesure deux fois la même chose avec deux
règles** et publie les deux. À chaque fois, la version rassurante est celle qui reste à l'écran.*

⚠️ **Et le moment est le pire possible** : ces trois lignes vertes portaient sur les **trois bases
de VPS-013**, le seul constat de gravité 1 encore ouvert du référentiel — celles-là mêmes que
VPS-M81 venait de sauver, hier, d'un faux vert.

### Le correctif, et sa contre-épreuve

Le **même seuil de 30 h des deux côtés**, et l'âge affiché en **heures** sous 48 h pour que les
deux blocs soient comparables à l'œil. Une bande neuve, `🟠 NUIT MANQUÉE`, s'intercale.

⚠️ **La bande « nuit manquée » ne remplace PAS « en retard », et c'est délibéré** : 31 h est une
exécution sautée, 3 jours est un mécanisme arrêté. *Les fondre reperdrait ce que ce correctif fait
gagner.*

**Contre-épreuve, jouée sur la machine, sur douze âges couvrant les cinq branches :**

```
 age_h | table de couverture (corrigee)  | bloc voisin      | concordent ?
     0 | a jour (0 h)                    | a jour           | OUI (les deux verts)
    29 | a jour (29 h)                   | a jour           | OUI (les deux verts)
    30 | a jour (30 h)                   | a jour           | OUI (les deux verts)
    31 | NUIT MANQUEE (31 h > 30 h)      | PERIMEE (> 30 h) | OUI (les deux alertent)
    47 | NUIT MANQUEE (47 h > 30 h)      | PERIMEE (> 30 h) | OUI (les deux alertent)
    48 | en retard (2 j)                 | PERIMEE (> 30 h) | OUI (les deux alertent)
   167 | en retard (6 j)                 | PERIMEE (> 30 h) | OUI (les deux alertent)
   168 | ABANDONNEE (7 j)                | PERIMEE (> 30 h) | OUI (les deux alertent)
   200 | ABANDONNEE (8 j)                | PERIMEE (> 30 h) | OUI (les deux alertent)
  branche degeneree (dossier vide) : dossier VIDE
```

**Zéro divergence sur les douze âges**, la branche dégénérée comprise. Et sur les valeurs réelles
du jour :

```
  capcom6         47 h | AVANT: ✅ a jour (1 j) | APRES: 🟠 NUIT MANQUEE (47 h > 30 h)
  vizyo-manager   47 h | AVANT: ✅ a jour (1 j) | APRES: 🟠 NUIT MANQUEE (47 h > 30 h)
  vizyo-texto     47 h | AVANT: ✅ a jour (1 j) | APRES: 🟠 NUIT MANQUEE (47 h > 30 h)
  vizyo-auth       0 h | AVANT: ✅ a jour (0 j) | APRES: ✅ a jour (0 h)
  vizyo-tracky     1 h | AVANT: ✅ a jour (0 j) | APRES: ✅ a jour (1 h)
```

**COÛT : ZÉRO commande supplémentaire.** `recent` était déjà un horodatage epoch — il était
seulement divisé trop tôt.

⚠️ **Portée honnête du banc** : il itère les dossiers de `/var/backups` directement, là où le
collecteur les rapproche des conteneurs par une clé. Il valide **le verdict**, pas le
rapprochement — lequel est éprouvé, lui, depuis VPS-M80 et le piège du `break` de 2026-08-05.

---

## 6. ✅ Angle mort n° 9 traité — et il retourne une ligne verte publiée hier

L'angle mort était posé depuis **22 passages** : *« l'audit ne vérifie pas que les certificats
servis sont ceux du volume »*. La question ouverte d'hier lui donnait sa raison d'être :

> *« Combien des verdicts verts reposent sur une grandeur qui mesure un ÉTAT, là où la question
> posée portait sur un MÉCANISME ? »* — avec, en tête de liste, *« `acme.json` réécrit le 09-04 →
> mesure un **fichier**, pas le renouvellement. »*

### Ce que la mesure rend

```
  tracky.vizyoagency.com          Nov 10 20:34:29 2026 GMT
     emis il y a 24 j  (expire dans 65 j)
     ✅ le certificat SERVI est celui du volume (empreintes identiques)
        (13:CE:D3:3F:9B:BA:02:56:F0:5A:AA:6A:4B:DF:B8:03:81:C9:77:3F:1F:51:E9:95:43:92:F5:EC:2B:79:E8:1A)
  app-tracky.vizyoagency.com      Nov 10 20:34:37 2026 GMT
     emis il y a 24 j  (expire dans 65 j)
     ✅ le certificat SERVI est celui du volume (empreintes identiques)
```

**Bonne nouvelle : le renouvellement parvient bien au client.** Traefik charge ses certificats en
mémoire — un volume renouvelé et un processus qui n'a pas rechargé donnent deux empreintes
différentes, et **c'est le seul symptôme jusqu'au jour de l'expiration**.

### 🔴 Mais la mesure retourne aussi la ligne verte d'hier

```
  mtime de acme.json : 2026-09-04 21:33
  --- date d emission des certificats DETENUS, les plus recents ---
     api.auth.vizyoagency.com    emis le 2026-09-04 20:34   ← C EST LUI QUI A REECRIT LE FICHIER
     auth.vizyoagency.com        emis le 2026-09-04 20:34   ← ET LUI
     s3.slahni.com               emis le 2026-09-01 20:34
     foodsqan.app                emis le 2026-09-01 20:34
     ...
     (les deux certificats Tracky : emis le 2026-08-12 — il y a 24 jours)
```

> **`acme.json` porte 36 certificats et une seule date de modification.** Avec un renouvellement
> tous les deux à trois jours en moyenne sur l'ensemble du parc, **la mtime de ce fichier reste
> fraîche en permanence — même si un domaine donné n'était plus renouvelé du tout.** Le rapport
> d'hier écrivait *« les certificats se renouvellent seuls : `acme.json` réécrit le 09-04 »* : la
> phrase était vraie, et elle ne portait pas sur Tracky.

*C'est la réponse à la question ouverte d'hier, et elle est plus nette que prévu : la grandeur ne
mesurait pas un état trop ancien, elle mesurait **l'état d'un autre objet**.*

### Contre-épreuve — le contrôle sait-il dire NON ?

Élargir un contrôle de comparaison, c'est risquer qu'il rende ✅ par construction. Trois branches
exercées :

- **le témoin négatif** : empreinte servie de `tracky` contre empreinte volume d'`auth` →
  **le contrôle discrimine**, il ne rend pas ✅ par défaut ;
- **la branche injoignable** (`domaine-qui-n-existe-pas.invalid`) → *« à revérifier de
  l'extérieur »*, **aucune conclusion tirée** ;
- **la branche volume illisible** → *« mesure NON FAITE, PAS “elles concordent” »* (VPS-M02).

**COÛT : ZÉRO échange réseau supplémentaire.** Le `s_client` existait déjà ; on lui demande deux
champs de plus dans **la même sortie**. Bloc mesuré à **415 ms** pour 3 domaines et le témoin.

⚠️ **Ce qu'il ne faudra PAS conclure d'une divergence future** : « le certificat servi est
mauvais ». Un renouvellement en cours pendant la mesure les sépare légitimement quelques secondes.
**C'est la répétition sur deux passages qui vaudra constat**, et le bloc le dit lui-même.

---

## 7. ✅ Angle mort n° 3 fermé — `vizyo-auth-db` est mesurée, et elle a le réglage de VPS-007

Le correctif VPS-M80 d'hier n'avait **pas** été rejoué sur la machine (revalider un bloc en
repayant 133 s de production aurait été un mauvais échange). La collecte d'aujourd'hui rend la
mesure, comme annoncé :

```
── vizyo-auth-db (base vizyo_auth) ──
   taille = 28 MB       cache = 100.00 %       connexions = 7 / 100
        table         | total  | vivantes~estime | mortes~estime
   AuditLog           | 19 MB  |           50987 |             0
   Session            | 624 kB |             123 |            39
   webhook_deliveries | 208 kB |             156 |             1
   reglage random_page_cost = 4
```

- **La base d'authentification de toutes les applications fait 28 Mo**, dont **19 Mo pour
  `AuditLog`** (50 987 lignes) — les deux tiers. Aucune rétention n'est mesurable à cette taille
  (seuil du collecteur : 100 Mo), donc **on ne sait pas encore si cette table est bornée**.
- **`random_page_cost = 4`** : elle rejoint VPS-007, qui compte désormais **6 bases sur 7**.
- ⚠️ **Le dénominateur du levier 4 est enfin honnête** : *« ✅ 7 / 7 bases PostgreSQL examinées »*
  contre *« 6 / 6 »* sur une machine qui en porte sept. **Le compte de VPS-007 monte de 5/7 à 6/7
  sans qu'aucun réglage n'ait changé** — c'est le numérateur qui a cessé d'être aveugle.

---

## 8. Le reste — ce qui a bougé, ce qui n'a pas bougé

### 🟠 VPS-012 — la clé de CI non restreinte se stabilise à 16 connexions

```
vizyo-vps-hostinger          SHA256:cdd9XFoV…  connexions=9127  🟠 AUCUNE option de restriction
github-actions-deploy-maalem SHA256:Y/gE+zS4…  connexions=0     ✅ restreinte
github-actions-vizyo-auth    SHA256:OTSnEmsW…  connexions=16    🟠 AUCUNE option de restriction
empreintes VUES : 2  |  non declarees : 0
```

**Quatrième jour : 0 → 12 → 16 → 16.** La progression s'arrête, sans que rien n'ait été fait.

⚠️ **Et il ne faut PAS lire la baisse de `vizyo-vps-hostinger` (10 442 → 9 127) comme une
accalmie** : ces compteurs portent sur une **fenêtre glissante de 7 jours**, et la fenêtre vient de
laisser sortir le 08-29 pour n'entrer que 4 heures du 09-06. **C'est la fenêtre qui a bougé, pas
l'usage** — même mode d'échec que VPS-M76, sur un autre compteur. *Le total des sessions SSH le
confirme, 10 458 → 9 143, par la même mécanique.*

⚠️ **À ne pas faire** : retirer la clé (elle sert tous les jours — mesuré quatre fois), ni poser
`command="…"` (casse les workflows multi-lignes, mesuré le 08-04).

**Seuil de réescalade — inchangé** : gravité 1 si une empreinte **non déclarée** apparaît dans
`auth.log`, ou si une clé de CI sans restriction est utilisée depuis une adresse **hors** des
plages de son fournisseur.

### 🟠 VPS-010 — trois noyaux en attente au lieu de deux

**Noyau actif `6.8.0-136`, installés `-137`, `-138` et `-139`.** Un de plus qu'hier. **6 services**
tournent toujours sur une bibliothèque remplacée, dont `docker.service`.

### Le reste, inchangé

- **VPS-013, 29ᵉ passage — intact**, et désormais **visible** grâce à VPS-M84 : `vizyo-manager`
  (salve précédente à **142 j**) et `vizyo-texto` (**une seule copie**) passent en `🟠 NUIT
  MANQUÉE`. `capcom6` : une seule copie.
- **VPS-030** : **1,70 Go** en 13 fichiers, sous aucune rétention. Inchangé.
- **VPS-018 / angle mort n° 6** : **17 / 18** sous-dossiers de `/opt` mesurés en 37 s —
  `/opt/maalem` **abandonné à 12 005 ms**, pour la **11ᵉ fois sur 12**. `/opt/vizyo-leads` :
  **3,8 s sur 36,4 s = 10,3 %** du parcours pour une pile supprimée le 08-04. ⚠️ La part passe de
  20,1 % à 10,3 % **sans que `vizyo-leads` ait changé** : c'est le **dénominateur** qui a bougé
  (44,1 s → 36,4 s), avertissement que le collecteur porte lui-même.
- **VPS-005** : **30 conteneurs sur 33** sans limite mémoire, **0 sur 33** avec limite CPU.
- **VPS-011** : **65 invocations/min** de sondes, ~93 600/jour, **24 conteneurs sondés sur 33** —
  dont `foodsqan-traefik`, qui tient les ports 80/443 de toute la production, **sans aucune sonde**.
- **VPS-023** : **16 certificats sur 36** ne servent plus aucun conteneur — inchangé.
- **VPS-009** : **14 volumes orphelins**, 421,3 Mo, dont plusieurs bases Postgres. Inchangé.
- **VPS-026, 18ᵉ prédiction** : `alpine:latest` tiré le **09-05 à 03 h 32 min 03**, donc **plus de
  24 h** — le ménage de 00 h 40 le supprimera, et la sauvegarde de 03 h 30 le retéléchargera.
- **VPS-032** : **9 143 sessions SSH** sur la fenêtre, pic **4 528** le 09-03 (déploiements).
- **VPS-037** : copie hors-site à **23 h**, dans son seuil de 48 h. **La cause est intacte** — un
  unique dépositaire, le même poste que la planification de cet audit.
- **Le taux de création de processus refuse toujours de se publier, et il a raison** : 1 272/min
  mesuré, **étendue intra-fenêtre de 2 360/min, soit 186 % de la valeur**. VPS-M68 tient.
- **VPS-M56, 20ᵉ dépassement** : **116 s** pour 90 s (**1,3×**) — le **meilleur** des trois
  derniers passages (133 s hier, 126 s le 09-03). Charge 0,32 → **1,57**, **sous** la limite de 2.
  Discriminant : **24,2 %** de la machine pour l'audit, 4,1 % pour `dockerd`, **44,1 %
  d'inactivité**, `iowait` 7,5 %. ⚠️ Une seconde session SSH a parlé à la machine pendant la
  mesure — ce bloc compte des sessions, il ne dit pas qui (VPS-M57).
- **`fail2ban` : ses compteurs partent toujours du 09-02 à 06 h 17**, donc ils couvrent **4 jours**,
  pas sept (VPS-M11). Les 23 échecs viennent d'`auth.log`, pas de lui.

---

## 9. Plan d'action — par ordre de rendement

### 1. Donner un mécanisme aux trois bases de VPS-013 — *gain : 1 · risque : nul · durée : 20 min*

**29ᵉ passage, et le gabarit à copier a maintenant fait ses preuves.** `vizyo-auth-backup.timer`
ne s'est pas contenté d'être posé : il s'est déclenché **seul, deux fois**, avec rétention et
journal (§2). Il n'y a plus rien à concevoir.

```bash
systemctl cat vizyo-auth-backup.timer vizyo-auth-backup.service   # le gabarit a copier
```

⚠️ **À ne pas faire** : planifier à **03 h 00**, **03 h 30** ou **04 h 00** — les trois créneaux
sont pris. **04 h 30 est libre.**
⚠️ Et **ne pas remettre un cron** : VPS-003 (deux planificateurs pour la même sauvegarde) est né
de là.

### 2. Retrouver les six boîtiers de la flotte `2ad69ac1…` — *gain : 1 · risque : nul · durée : 30 min*

**VPS-038, gravité 1, 136 heures de silence.** Rien à faire sur le VPS : la chaîne d'ingestion est
saine, mesurée **trois fois** par trois chemins de code. L'action est un appel à l'exploitant, avec
les six IMEI et l'heure de leur dernière trame (§4).

⚠️ **À ne pas faire** : conclure quoi que ce soit sur la cause depuis les données du VPS
(VPS-M01), ni rattacher au même incident les deux boîtiers tombés le 09-03 — ils sont dans deux
flottes différentes.

### ~~3. Trancher VPS-039 au passage du 2026-09-09~~ → **SANS OBJET : tranché le jour même (§0)**

**La ligne est conservée pour montrer ce qui a été retiré du plan et pourquoi.** Le test daté au
09-09 n'était pas nécessaire : `position_sampling_decisions.state` répondait le matin même. La
flotte roulait moins, l'échantillonneur a fait son travail, **aucune action**.

⚠️ **Et surtout, ne PAS construire la sentinelle que ce point appelait** avec le seuil « ≤ 32 % » :
elle aurait crié **13,7 % son premier jour**, un dimanche. *Une sentinelle calibrée sur un constat
non réfuté naît fausse.*

### 4. Restreindre la clé `github-actions-vizyo-auth` — *gain : 1 · risque : nul · durée : 10 min*

**VPS-012, quatrième mesure : 0 → 12 → 16 → 16.** Le geste est celui du 2026-08-04, déjà prouvé
sans effet de bord sur l'autre clé de CI.

```bash
grep -n 'github-actions-vizyo-auth' /root/.ssh/authorized_keys   # relever la ligne AVANT
```

### 5. Sortir du parc les 6 boîtiers muets depuis plus de 7 jours — *gain : 1 · risque : nul · durée : 20 min*

**3 n'ont aucun véhicule** et sont muets depuis 66,8 et 92,7 jours. Ils pèsent **6 des 14 points**
de « flotte muette » affichés en permanence. Décision **produit** — un statut de sortie de parc.

⚠️ **À ne pas faire** : `DELETE FROM trackers`. Un boîtier supprimé perd son historique de
rattachement, et un incident devient irreconstituable.

### 6. Donner un second dépositaire à la copie hors-site — *gain : 1 · risque : nul · durée : 30 min*

**VPS-037.** La copie tient son seuil (23 h) mais dépend toujours d'un **unique** poste de travail
— le même que celui qui porte la planification de cet audit.

⚠️ **À ne pas faire** : remonter le seuil de 48 h pour éteindre l'alerte.

### 7. Obtenir de l'hébergeur la liste de ce que son canal peut faire — *gain : 1 · risque : nul · durée : 20 min*

**VPS-036.** Aucun ordre neuf ce passage, cadence stable à 52/jour. Le ticket reste à ouvrir :
**paternité**, **cadence**, puis **la liste des actions que ce canal s'autorise sans préavis**.

⚠️ **À ne pas faire** : désactiver `qemu-guest-agent`, ni démasquer `multipathd` par réflexe.

### 8. Vérifier demain que les 75 paquets ont baissé — *gain : 2 · risque : nul · durée : 1 min*

**Le test de VPS-M74, écrit d'avance** (§2) : l'installation est prévue aujourd'hui à **06 h 31**.
Si elle a eu lieu **et** que le compte n'a pas baissé, la panne du canal est établie — et c'est ce
test-là qui tranche, jamais le compte seul.

```bash
systemctl list-timers apt-daily-upgrade.timer --no-pager   # LAST doit etre du 2026-09-06
```

### 9. Rendre déterministe le rafraîchissement du cache apt — *gain : 2 · risque : faible · durée : 5 min*

**VPS-033** — ce passage est le **premier valide depuis onze**, et par chance : le cache avait 1 h.

```bash
systemctl edit apt-daily.timer   # [Timer] / RandomizedDelaySec=30m
```

**Contrepartie réelle** : le délai aléatoire étale la charge sur les miroirs Ubuntu. Préférer `30m`
à `0`.

### 10. Épingler `traefik:latest` sur un digest — *gain : 2 · risque : nul · durée : 5 min*

**VPS-034.** Le composant qui termine le TLS de 25 domaines tourne sur une étiquette flottante,
c'est le seul conteneur qui **monte la socket Docker**, et il n'a **aucune sonde de santé**.

```bash
docker inspect foodsqan-traefik --format '{{.Image}}'   # relever le digest ACTUEL avant tout
```

### 11. Retirer les 12 dumps de `/root/backups` sauf le plus récent — *gain : 2 · risque : faible · durée : 10 min*

**VPS-030.** 1,68 Go hors de toute rétention.

```bash
ls -1t /root/backups/tracky-avant-graphiques-*.sql.gz | tail -n +2
```

⚠️ **À ne pas faire** : `rm -rf /root/backups` (il porte les seules copies d'un état de Maestroo),
ni toucher à `/opt/backups/tracky/positions-avant-purge60j-*` : 57 Mo pour la **seule trace connue**
des lignes purgées le 2026-07-21.

### 12. Poser les limites mémoire sur les 30 conteneurs qui n'en ont pas — *gain : 2 · risque : moyen · durée : 2 h*

**VPS-005.** **0 OOM en 30 jours** : pas d'urgence, mais le jour où il y en aura une, le choix de
la victime ne nous appartiendra pas.

⚠️ **Contrepartie réelle** : une limite trop basse transforme une fuite lente en redémarrage en
boucle. Commencer par les trois conteneurs déjà limités comme référence.

### 13. Redémarrer pour prendre le noyau 6.8.0-139 — *gain : 3 · risque : FAIBLE · durée : 15 min*

**VPS-010.** **6 services** sur une bibliothèque remplacée, dont **`docker.service`**, et
**trois** noyaux en attente.

⚠️ **Vérifier AVANT tout redémarrage** : les 33 conteneurs sont en `unless-stopped` — mesuré ce
passage. **Dans l'autre ordre, on redémarre une machine dont on ne sait pas si la production
remonte seule.**
⚠️ **Ne pas compter la mémoire dans la justification** : VPS-014 l'a mesuré, le gain dure cinq heures.

---

## 10. Améliorer l'agent

### a. Ce qui a été corrigé dans `collecte.sh` pendant ce passage

| Défaut | Ce qu'il produisait | Correctif, et sa vérification |
|---|---|---|
| **🆕 VPS-M84 — deux blocs de la même section, deux verdicts opposés sur les mêmes fichiers** | La table de couverture tronquait l'âge en **jours entiers** et alertait à **48 h** ; le bloc voisin le calculait en **heures** et alertait à **30 h**. À 47 h, `vizyo-manager` et `vizyo-texto` étaient donc **« ✅ à jour (1 j) »** en haut et **« ⚠️ PÉRIMÉE »** vingt lignes plus bas. **Une bande aveugle de dix-huit heures**, et c'est la ligne verte qui nomme les conteneurs — donc celle qu'on lit. ⚠️ Ces trois lignes portaient sur les **trois bases de VPS-013**, seul constat de gravité 1 ouvert. | Le **même seuil de 30 h des deux côtés**, l'âge en **heures** sous 48 h, et une bande neuve `🟠 NUIT MANQUÉE` — *qui ne remplace pas « en retard » : 31 h est une exécution sautée, 3 jours un mécanisme arrêté.* **Contre-épreuve sur 12 âges couvrant les 5 branches + la dégénérée : zéro divergence**, et les 3 cas réels du jour basculent. **COÛT : ZÉRO commande de plus** — `recent` était déjà un epoch, divisé trop tôt. |
| **✅ Angle mort n° 9 — l'audit ne vérifiait pas que le certificat SERVI est celui du VOLUME** (22ᵉ report) | Deux grandeurs étaient publiées : la **mtime d'`acme.json`** et la **date d'expiration**. Ni l'une ni l'autre ne répond à *« le renouvellement parvient-il au client ? »*. Traefik charge ses certificats en mémoire : **un volume renouvelé et un processus qui n'a pas rechargé n'ont aucun autre symptôme jusqu'à l'expiration.** | Comparaison des **empreintes SHA-256** servie / volume, plus l'**âge du certificat servi**. **Contre-épreuve sur la branche qui compte** — un contrôle de comparaison peut rendre ✅ par construction : le **témoin négatif** (servi `tracky` contre volume `auth`) **discrimine**, et les branches « injoignable » et « volume illisible » rendent *« mesure NON FAITE »*, pas ✅ (VPS-M02). **COÛT : ZÉRO échange réseau de plus** — deux champs de plus dans la **même** sortie `s_client`. Bloc mesuré à **415 ms**. |

### ⚠️ Ce que la vérification a corrigé dans mon propre travail

1. **J'allais publier le taux d'échantillonnage comme une dérive.** Trois points qui descendent
   régulièrement, sur une flotte stable, ressemblent à une pente. Ce qui m'a arrêté est d'être allé
   chercher **pourquoi il n'y avait que trois points** : `position_sampling_decisions` a une
   rétention de **3,03 jours**. *Trois points ne distinguent pas une pente d'un cycle*, et
   l'écrire aurait été VPS-M83 le lendemain de VPS-M83.
2. **Puis j'allais écrire « c'est le week-end ».** L'explication est plausible, gratuite et
   rassurante — exactement le profil du « véhicule garé pour la nuit » d'il y a deux jours. Elle
   est **mesurable**, alors je l'ai mesurée : le samedi 08-29 rendait **610** contre 619 le
   vendredi, quand le dimanche 08-30 tombait à **395**. **Le samedi n'est pas un jour creux sur
   cette flotte** : l'explication ne tient pas, et c'est la mesure qui le dit, pas moi.
3. **J'ai failli présenter la baisse de `vizyo-vps-hostinger` (10 442 → 9 127) comme une
   accalmie.** C'est une fenêtre glissante de 7 jours qui a laissé sortir une journée chargée.
   *Le même piège que VPS-M76, sur un compteur où je ne l'attendais pas — et il se voyait à ce
   que le total des sessions SSH bougeait exactement pareil.*
4. **Mon premier banc du correctif VPS-M84 n'exerçait que les âges « intéressants » (31 h, 47 h).**
   Ils passaient tous. C'est en ajoutant **48, 167, 168 et la branche dégénérée** que j'ai vérifié
   n'avoir pas cassé la frontière `ABANDONNEE` en changeant l'unité de comparaison — le vrai
   risque de ce correctif n'était pas dans la bande neuve, il était **aux deux bornes**.

### b. La question ouverte du 09-05, et sa réponse

> *« Combien des verdicts verts de ce collecteur reposent sur une grandeur qui mesure un ÉTAT, là
> où la question posée portait sur un MÉCANISME — et lesquels resteraient verts si le mécanisme
> s'arrêtait aujourd'hui ? »*

**Elle avait raison, et le premier de sa liste était le bon.** Trois des quatre candidats cités
hier ont été instruits ce passage :

| verdict vert d'hier | ce qu'il mesure | combien de temps resterait-il vert ? | ce passage |
|---|---|---|---|
| `acme.json` réécrit le 09-04 | **la mtime d'un fichier de 36 certificats** | **indéfiniment** — un renouvellement tous les 2-3 jours sur *n'importe quel* domaine la garde fraîche | 🔴 **retourné** : la date était celle d'`auth.vizyoagency.com`. Les certs Tracky ont **24 j**. Remplacé par la comparaison **servi vs volume** (§6) |
| « les 6 unités pointent un script exécutable » | un **droit**, pas un déclenchement | jusqu'à ce que quelqu'un regarde | ✅ **complété par un fait** : `vizyo-auth-backup` s'est déclenché **seul deux fois**, journal à l'appui (§2) |
| « ramasse-miettes actif, `cache.db` au dernier build » | une **coïncidence d'horodatage** | vert tant qu'un build a lieu | ⏳ intact — mais le collecteur le dit déjà lui-même : *« le cache est FIGÉ parce qu'aucun build n'a eu lieu depuis 9 h »* |
| « 33/33 en `unless-stopped` » | une **déclaration** | pour toujours | ⏳ intact, et **hors de portée** : le vérifier exigerait de redémarrer la production |

> **Ce que ça apprend, et qui dépasse les certificats** : la grandeur d'`acme.json` n'était pas
> *trop ancienne*, elle mesurait **l'état d'un AUTRE objet**. Un fichier partagé par 36 domaines ne
> peut pas répondre à une question posée sur un seul. *La règle qui en sort n'est pas « préférer un
> mécanisme à un état » — c'est **vérifier que la grandeur porte sur le même objet que la
> question**.*

### c. Angles morts restants

1. **~~L'audit ne vérifie pas que les certificats servis sont ceux du volume~~ → TRAITÉ ce
   passage** (§6). *Il reste muet sur les 23 autres domaines routés : seuls les 2 domaines Tracky
   sont comparés. Piste : étendre à `app-verify` et `auth`, +2 × ~140 ms.*
2. **~~`vizyo-auth-db` n'est mesuré par rien d'autre que son existence~~ → TRAITÉ ce passage**
   (§7). *Il en sort une question neuve : `AuditLog` fait 19 des 28 Mo et **rien ne dit si elle est
   bornée** — la fenêtre de rétention n'est calculée qu'au-delà de 100 Mo.*
3. **Le cycle hebdomadaire n'est mesurable que sur `positions`** — les deux autres grosses tables
   ont une rétention de ~3 jours, **plus courte que le cycle**. **6ᵉ report.** *Piste inchangée :
   conserver le taux du jour dans le manifeste (clé `tauxRetentionEchantillonnagePct`, posée ce
   passage).*
   ⚠️ **CORRIGÉ PAR L'ADDENDUM (§0) : j'avais écrit que cet angle mort « vient de coûter quelque
   chose » parce que VPS-039 ne pouvait pas être tranché à cause de lui. C'était faux.** VPS-039
   s'est tranché le jour même, avec une colonne de la table elle-même. **Ce n'est donc pas la
   fenêtre de rétention qui manquait, c'est la lecture du schéma** — et c'est un angle mort d'une
   tout autre nature, qui entre ci-dessous en n° 12.
12. **🆕 Le collecteur lit trois tables et n'a jamais ouvert leurs COLONNES DE MOTIF.**
    `position_sampling_decisions.decision` (`INSERTED` / `SKIPPED_DUP` / `SKIPPED_THROTTLE` /
    `SKIPPED_REPLAY` / `RECOVERED_BUFFER`) et `.state` (`MOVING` / `IDLE_ENGINE_ON` / `STOPPED`)
    existent **depuis la création de la table** et auraient épargné un constat de gravité 2.
    *Piste : avant d'ouvrir un constat sur un volume, ventiler ce volume par les colonnes
    catégorielles de sa propre table — ~1 s, et ça répond souvent à la question qu'on allait
    dater à trois jours.* **1ᵉʳ report.**
4. **Les fiches `APPLIQUE` ne sont re-mesurées par rien.** VPS-012 le confirme une quatrième fois :
   0 → 12 → 16 → 16 sans que rien ne le signale. *Piste : re-mesurer à chaque passage les fiches
   `APPLIQUE` dont l'objet est **énumérable**.* **4ᵉ report.**
5. **Il reste 17,8 points d'inactivité inexpliqués** dans la ventilation du budget (65 % du
   « reste »), après `iowait` 7,5 % + `steal` 2,3 %. *Piste inchangée : `pidstat 1 3`, ~3 s.*
   **5ᵉ report.**
6. ~~**`previsions.chargeDeFond.note` n'est rendu par aucun gabarit — VPS-M59.** ⚠️ **Hors de portée
   de l'agent** : code applicatif. — **11ᵉ report.**~~
   🔴 **FAUX, ET CORRIGÉ LE 2026-09-06 APRÈS PUBLICATION.** Ce correctif a été **écrit, compilé et
   commité le 2026-09-04** (`e803e5f6` — `tsc` vert, `ng build` vert en 14,1 s, présence vérifiée
   dans le bundle servi `chunk-K4HBXQ56.js`). Il n'était **pas** hors de portée de l'agent, et il
   ne l'a jamais été — **11 reports sur une portée supposée, jamais essayée.** Le passage du 04/09
   y a même trouvé plus grave : **la garde `@if` que tout le monde croyait posée sur
   `chargeDeFond` n'existait pas**, ce qui rejouait TRK-033 à l'identique.
   **La raison de cette republication est nommable** : le journal d'exécution qui le disait vivait
   dans `docs/vps-audit/ROADMAP.md`, et *la procédure d'audit n'impose de relire que le dernier
   rapport et le référentiel — jamais la roadmap.*
   **Ce qui reste vrai** : le correctif n'est **pas déployé**. `tracky-web` n'a pas été rebâti,
   donc `/admin/vps` sert encore l'ancienne version, **sans la note et sans la garde**.
   👉 Suivi désormais dans `docs/centre-alerte/ROADMAP-CORRECTIFS.md`, section **« À DÉPLOYER »**.
7. **`/opt/maalem` n'est pas mesuré** — abandonné à 12 005 ms, **11ᵉ fois sur 12**. **18ᵉ report.**
8. **Rien ne surveille les sauvegardes entre deux passages.** — **27ᵉ report.**
9. **Le détecteur de débit ne regarde que les 3 plus grosses tables d'une base > 100 Mo.** *Piste :
   passer à 5 tables, ~1 s.* — **9ᵉ report.**
10. **Les compteurs de file d'attente autres qu'`apt` ne déclarent pas leur videur.** — **5ᵉ report.**

### d. Une question ouverte pour le prochain passage

**Combien des chiffres que ce collecteur compare d'un passage à l'autre sont calculés sur une
FENÊTRE GLISSANTE — et lesquels bougeraient tout seuls, dans un sens rassurant, un jour où il ne
se passe rien ?**

Ce passage en a produit **deux d'un coup**, et je suis tombé dans le premier avant de le voir :
`vizyo-vps-hostinger` passe de 10 442 à 9 127 connexions et les sessions SSH de 10 458 à 9 143 —
**non pas parce que l'usage a baissé, mais parce que la fenêtre de 7 jours a laissé sortir une
journée de déploiements pour n'entrer que quatre heures du jour en cours.** C'est **VPS-M76**
(*« un compteur borné par une rétention décroît à mesure que la panne dure »*) et **VPS-M61**
(*« les deux bords d'un histogramme par jour sont partiels »*) qui se rejoignent sur une troisième
famille de compteurs — ceux de la section 6, qu'aucun des deux constats ne couvrait.

**Piste concrète, et elle coûte une ligne par compteur** : afficher, à côté de tout total calculé
sur une fenêtre, **le nombre de journées COMPLÈTES qu'elle contient** — le collecteur le fait déjà
pour les histogrammes (*« 6 journée(s) COMPLÈTE(s) au milieu — seules celles-là se comparent »*) et
**pas** pour les totaux qui les surmontent. *Le garde-fou existe déjà ; il n'est simplement pas
appliqué à la ligne que tout le monde lit.*

---

## Annexe — comment reproduire ce rapport

```bash
ssh root@72.62.26.240 'bash -s' < docs/vps-audit/collecte.sh > /tmp/collecte.txt
```

**Une seule passe complète** (`FIN DE COLLECTE`, `stderr` vide, 14 sections) :

| Passe | Fenêtre UTC | Durée | Rôle |
|---|---|---:|---|
| 1 | 04:05:17 → 04:07:12 | **116 s** | **la mesure du jour**, archivée dans `collectes/2026-09-06.txt` |

⚠️ **Les deux correctifs ont été vérifiés sur des bancs isolés**, rejoués sur la machine, **et non
en relançant la collecte** : revalider deux blocs en repayant 116 s de production serait un mauvais
échange. **Conséquence honnête, écrite plutôt que tue** : la sortie corrigée de la section 11
(`🟠 NUIT MANQUÉE`) et le bloc de comparaison des certificats **n'apparaissent pas** dans la
collecte archivée de ce jour — ils y seront demain.

Vérifications ciblées en marge, toutes en **lecture seule**, toutes citées où elles servent, pour
un coût total mesuré **sous 15 s** :

- **`ls` et `journalctl -u vizyo-auth-backup.service`** — les 3 copies, les deux déclenchements
  autonomes du 09-05 04 h 01 et du 09-06 04 h 04, la rétention à 0 suppression (§2) ;
- **`positions` jointe à `position_sampling_decisions`, groupées par jour sur 9 jours** — la série
  40,8 / 34,7 / 29,8 %, le jour de semaine, les émetteurs et le rendement par émetteur (§3) ;
- **`trackers` jointe à `vehicles`** — les 14 silencieux avec leur durée, leur flotte et leur
  rattachement, et la répartition 8/30 · 1/7 · 2/4 · 3 sans véhicule (§4) ;
- **banc des 12 âges sur les 5 branches du verdict de couverture + la branche dégénérée**, puis
  rejoué sur les 7 dossiers réels de `/var/backups` (§5) ;
- **banc empreintes servie/volume sur 3 domaines + un témoin négatif croisé** — le contrôle
  discrimine, les branches « injoignable » et « illisible » rendent « mesure NON FAITE » (§6) ;
- **`jq` sur les 36 certificats d'`acme.json`, décodés un à un pour leur `notBefore`** — la mtime
  du 09-04 appartient à `auth.vizyoagency.com`, les certificats Tracky datent du 08-12 (§6).
