# Audit VPS — 2026-09-17

> ## Verdict
>
> **🔴 `dockerd` BRÛLE DE NOUVEAU UN CŒUR : 6ᵉ OCCURRENCE DE VPS-016, OUVERTE 19 H 21 APRÈS LA
> CLÔTURE DE LA 5ᵉ, ET C'EST ENCORE UN `docker logs` SANS `timeout` LANCÉ DEPUIS LE POSTE.**
> Le 16/09 à **12:41:08 UTC** (14:41 Paris), une session SSH du poste (`82.67.153.51:26499`,
> `sshd[3366619]`) a lancé un `bash -c` de diagnostic — *« ── dernières lignes vizyo-auth-api »* —
> dont la première commande, **`docker logs --tail 15 vizyo-auth-api`** (pid 3366708), ne s'est
> jamais terminée. La session est morte ; le `bash -c` (pid 3366707) vit, rattaché à `init`, comme
> le 20/08 et le 13/09. `sar` date la boucle à la minute : **85,2 % d'inactivité à 12:40, 42,4 % sur
> l'intervalle 12:40–12:50** (≈ 9 % de l'intervalle à deux cœurs = **la boucle commence vers 12:41**,
> avec le client, *pas* avec la recréation du conteneur 4 min plus tard), puis **37–40 % sans une
> seule exception jusqu'à ce matin**. `dockerd` : **98,3 % maintenant**, cumul **304,6 → 319,5 h
> (+14,9 h)** = 12:41 → 03:21 à 10 min près, **1 290 867 `read()`/s pour 0 octet**. La journée du
> 09-16 finit à **63,33 %** (12,7 h à deux cœurs, 11,3 h à un) ; le 09-17 partiel est à **38,65 %**.
> *Le seuil de réescalade écrit hier est franchi mot pour mot* → **VPS-016 repasse `A_TRAITER`,
> gravité 1**. Le remède est le même que V28, **il n'a pas été exécuté** : `kill 3366707` (le parent
> d'abord) puis `3366708` — 2 s, aucune coupure. 🆕 **V33.** ⚠️ Cette occurrence ne visait **pas** un
> journal troué : le conteneur ciblé a été **recréé à 12:45:34** (l'ancien, 284 h, *« manually
> stopped »*), son journal est parti avec lui — VPS-041 n'est ni confirmé ni infirmé par elle.
> **6 occurrences sur 6 = un `docker logs` depuis une session du poste qui se ferme avant lui.**
>
> **🔴 LE POSTE A DORMI DE 23:17 À 03:15 UTC : L'AUDIT EST PARTI À 03:18 (+56 min), LES DEUX AUDITS
> À LA MÊME SECONDE (VPS-M57, 3ᵉ collision), ET LA PAIRE DU 16/09 N'A PAS ÉTÉ COPIÉE (VPS-037).**
> Journal Windows : veille **2026-09-16 23:16:59 Z**, réveil **03:15:38 Z** (source *« Unknown »*) ;
> le planificateur a lancé `audit-centre-alerte-tracky` et `audit-vps-tracky` à **03:16:25,135 et
> 03:16:25,356** — 221 ms d'écart — et l'autre audit a ouvert **27 sessions SSH pendant mes sections
> 6 à 12** (03:21:30 → 03:23:10). `WakeToRun` est **toujours `False`** ; le JSON de copie date du
> **15/09 04:30** ; **2 paires produites depuis n'existent qu'ici** (16/09 03:31 et 17/09 03:31) —
> *et le collecteur écrivait « 46 h, à jour »* (corrigé : il compte désormais les paires). La 3ᵉ
> paire sans copie sur 7 jours — le seuil de gravité 1 — se joue **ce matin à 04:30 UTC**, selon que
> le poste reste éveillé.
>
> **🔑 ET UNE LECTURE D'HIER ÉTAIT FAUSSE : LES PASSAGES D'AUTOMATISATION NE SE SONT PAS RACCOURCIS
> GRÂCE AU CŒUR RENDU — C'EST LA NUIT QUI LES RACCOURCIT (🆕 VPS-M103).** `trip_automation_runs` sur
> trois journées : **09-14 (un cœur) 08h–15h = 56 · 52 · 55 · 59 · 50 · 30 · 55 · 53 min** ; **09-15
> (un cœur) 08h–15h = 42 · 38 · 56 · 54 · 51 · 57 · 48 · 34** ; **09-16 (DEUX cœurs de 07h à 12:41)
> 07h–12h = 54 · 55 · 55 · 53 · 38 · 44**, puis 47 · 56 · 42 à un cœur, puis 19 · 8 · 4 · 8 · 1 le soir.
> Le rapport du 16/09 comparait 15h–17h à 18h–23h et y lisait *« le coût du cœur rendu se lit dans
> l'application »* — c'était le cycle diurne (VPS-M69/M71, une 3ᵉ forme). **Le seul effet mesuré de
> V28 reste `deploy.sh` : 226 s et 292 s à deux cœurs contre 303–585 et 1 916 s.** Et les passages
> > 45 min *de jour* — qui débordent sur le suivant — sont un fait **quotidien**, cœurs ou pas.
>
> **✅ CE QUI TIENT, ET CE QUI AVANCE.** Périmètre **38 = 38**, comptes **4 = 4** et dossiers **12 = 12**
> (première vraie différence des deux listes posées hier) ; **7 sauvegardes sur 7, 9 archives sur 9** ;
> mercredi complet **33 émetteurs** (6ᵉ jour ≥ 32), **33 / 33 / 33** ; ✅ **le test VPS-M74 est passé :
> les 3 correctifs de sécurité (polkitd ×3) ont été installés le 16/09 à 06:52:26**, `update-notifier`
> 64 → 61 — le canal `unattended-upgrades` marche ; ✅ **VPS-044, première moitié de la preuve** :
> `avant-20260916-0519-0c9672c9` = **`cc1fec4a6ebb` / `c8d99972e2eb`**, exactement les images des
> conteneurs au passage d'hier — mais `until=24h` est intact et cette étiquette **meurt demain 00:40**
> (43 h) : *le seuil « deux passages de suite » n'est décidable qu'avec V32 (a)* ; `deploy.sh`
> **87ae29c9 à 05:24:17 en 292 s** (2ᵉ durée à deux cœurs) ; `alpine:latest` **absent à 03:19,
> re-tiré 03:30:59** (22ᵉ prédiction) ; disque **53 Go / 96 (55 %)**, 29 images (32 − 4 `avant-17xx`
> − alpine + 2). 🔵 **Dispocar : `dev` rend 44 064 o ce matin** (première copie non vide), **`prd`
> reste à 1 172 o** (4ᵉ nuit) — la question de V30 se précise : c'est `prd` qui vise une base vide.

---

## 1. Tableau par domaine

| Domaine | État | En une phrase |
|---|:--:|---|
| **Charge & capacité** | 🔴 | `dockerd` **98,3 %**, cumul **319,5 h** (+14,9 h), **1,29 M `read()`/s pour 0 octet** ; 09-16 **63,33 %** (12,7 h libres, 11,3 h à un cœur), 09-17 partiel **38,65 %** ; charge 1,78 → **4,51** pendant la collecte ; production **81–92 ms** (elle répond, la marge non). |
| **Docker** | 🔴 | 🔴 **VPS-016, 6ᵉ occurrence** : `docker logs --tail 15 vizyo-auth-api` pid 3366708, **52 743 s**, parent `bash -c` 3366707 vivant (PPID 1), session `82.67.153.51:26499` morte ; `vizyo-auth-api` **recréé 12:45:34** (🆕 le collecteur liste désormais les recréations) ; `tracky-api` / `tracky-web` ✅ sur `latest` (87ae29c9, déployé 05:24 en **292 s**) ; ✅ `avant-20260916-0519` = l'image d'hier ; 29 images. |
| **Sauvegardes** | 🔴 | 7 / 7, 9 / 9 relues — **mais 2 paires Verify sans copie hors-site** (16/09, 17/09), JSON du 15/09 04:30, poste endormi à 04:30 hier, `WakeToRun=False` ; le rendez-vous de **ce matin 04:30** décide de la gravité 1 (3ᵉ paire / 7 j). |
| Sécurité | 🟠 | 7 échecs / 7 j, **0 sur root** ; 4 empreintes vues / 6 déclarées, 0 inconnue ; session `vaultbk` **172 h** ; `apt` **NON MESURABLE** (25 h — la 2ᵉ sonnerie de 20:34 n'a rien fait, comme prévu) mais ✅ **3 sécurité → 0 installés à 06:52** ; 6 services sous `libc6` remplacée, noyau -136 / -139. |
| Données | 🟢 | `positions` **33** / `wire_logs` **33** / décisions **33** ; mercredi complet **33** (6ᵉ jour ≥ 32) ; registre **11 / 11 / 11** ; `…6714` a mesuré **jusqu'au 09-16 15:28:34** puis 1 612 trames seules — 4ᵉ épisode. |
| Disque | 🟢 | **53 Go / 96 (55 %)**, 44 Go libres, = hier ; cache de build **9,854 Go**, `cache.db` = dernier build (05:23:57) ✅ ; `/opt` 17 / 20 (`maalem`, `vault`, `verify` en délai — ne se compare pas) ; VPS-030 inchangé (1,70 Go + 161 Mo + 3 fichiers racine, V17 non faite). |
| Mémoire | 🟢 | 3,2 Gi / 7,8, swap **1,2 Gi** (−0,26), PSI mémoire 0,00, **0 OOM 30 j**. |
| Planification | 🟠 | 25 unités : 22 succès, 0 échec ; hyperviseur **52 / j × 6**, 0 inattendue ; **`logrotate` 21,9 s de CPU** (00:00:00 → 00:01:17 ; 1,63 s hier) — 5ᵉ nuit de V29 non faite, **6 troués** (les mêmes `-dev-`), 13 à 0 octet ; `until=24h` intact (V32 a non faite). |
| Mises à jour | 🟠 | ✅ **VPS-M74 tranché : le canal installe** (polkitd ×3, 06:52:26) ; mesure `apt` invalide ce matin (4 valides / 20) ; **V14** reste le seul geste ; uptime **43,2 j**, redémarrage requis (V4, débloquée puis re-bloquée par V33 : tuer le client AVANT de redémarrer). |
| **L'agent lui-même** | 🔴 | **+56 min** (poste endormi 23:17 → 03:15 Z) ; **228 s** (2,5×) — `dockerd` **43,2 %**, audit **13,4 %**, machine à **7,1 % d'inactivité** pendant la mesure ; **3ᵉ collision** avec l'audit du centre d'alerte (même seconde de départ, 27 sessions SSH pendant ma fenêtre) ; **3 correctifs posés**, tous bancs ; **1 lecture d'hier réfutée** (VPS-M103). |

---

## 2. Ce qui va bien — avec les chiffres qui le prouvent

| Ce qui va bien | La mesure qui le prouve | Source |
|---|---|---|
| **Le canal de sécurité `unattended-upgrades` installe** — test VPS-M74 passé | `unattended-upgrades.log` 06:52:24 *« Packages that will be upgraded: libpolkit-agent-1-0 libpolkit-gobject-1-0 polkitd »*, 06:52:39 *« All upgrades installed »* ; `dpkg.log` 06:52:26 ×3 (124-2ubuntu1.24.04.3 → .4) ; `update-notifier` 64 (01:26) → **61** (06:52:38) ; le cache `apt` périmé rend 73 → 70 dont **3 → 0** | vérif. D |
| **L'étiquette de repli est vraie cette fois** (VPS-044, ½ preuve) | `tracky-api:avant-20260916-0519-0c9672c9` = **`cc1fec4a6ebb`**, `tracky-web:avant-…` = **`c8d99972e2eb`** — les images que portaient les conteneurs au passage du 16/09 (rapport §2 d'hier) ; `latest` n'avait pas été pré-construit | vérif. B |
| Le second point de `deploy.sh` à deux cœurs | journal T33 : `87ae29c9` 05:24:17, **292 s**, `attente=true` — contre 226 s (fa9ff1d1) et 303–585 s / 1 916 s à un cœur | §4 |
| Le périmètre n'a pas bougé, sur trois listes | conteneurs **38 = 38**, comptes **4 = 4** (`conductorbk`, `dispocarbk`, `ubuntu`, `vaultbk`), dossiers **12 = 12** — première différence effective des deux clés posées hier | §4 |
| Toutes les sauvegardes de la nuit ont réussi | `dpkg` 00:00, `tracky` **03:04:56** (37 copies, 5,3 Go), puis `verify` 03:31 (`DERNIER-ETAT.json` OK, base 12 K + 13 fichiers 5,3 M) — les autres (`auth` 04:00, `manager` 04:33, `texto` 04:44, `capcom6` 04:50) après ma collecte, hier toutes ✅ | §11, vérif. G |
| Les archives se relisent | **9 sur 9** décompressées **et** marqueur de fin présent | §11 |
| Les trois compteurs s'accordent | `positions` 33 · `wire_logs` 33 · `position_sampling_decisions` 33 — 3ᵉ égalité parfaite | §5 |
| La flotte tient son seuil | par jour calendaire : 30 · 30 · 33 · 32 · 32 · 33 · 33 · **33** (mercredi complet) · 32 partiel — 6ᵉ jour ≥ 32 | vérif. F |
| Le ramasse-miettes de build tient | cache **9,854 Go** (9,812 hier), `cache.db` réécrit **0 s** après le build de 05:23:57 — 11ᵉ passage sous la borne | §4 |
| `alpine:latest` s'est comporté comme prédit, 22ᵉ fois | **ABSENT** à 03:19 (ménage 00:40, 45 h), `LastTagTime` **03:30:59** = re-tiré par la sauvegarde Verify | §11, vérif. B |
| Rien n'a été tué faute de mémoire | 0 OOM 30 j, PSI `full` 0,00, 4,6 Gi disponibles, swap en baisse (1,46 → 1,23 Gi) | §2 |
| L'hyperviseur n'a rien envoyé d'inattendu | 355 exécutions / 3 765 sondages ; **52 / jour** × 6 ; INATTENDUES **0** | §7 |
| Toutes les clés ayant servi sont déclarées | 4 vues / 6 déclarées, 0 inconnue ; Conductor a déposé à **03:06** (29,7 Ko `prd`), Dispocar à 01:34 / 01:38 | §6, vérif. B |
| **Dispocar `dev` n'est plus vide** | `dev-dispocar-20260917-033853.sql.gz.age` = **44 064 o** (1 172 o les 3 nuits d'avant) — *`prd` reste à 1 172 o* | vérif. B |
| Les certificats servis sont les bons | empreintes identiques, **54 j** avant expiration ; `acme.json` touché 21:33 | §6 |
| La production répond | `/` 81 ms, `/api/health` 92 ms, LP 391 ms, Verify 302 en 185 ms — **à un cœur** | §6 |

---

## 3. 🔴 VPS-016 — 6ᵉ occurrence : 19 h 21 de deux cœurs, puis un `docker logs` depuis le poste, et le remède écrit hier n'a jamais eu le temps de servir

**Gravité 2 → 1 · `SURVEILLANCE` → `A_TRAITER` · 🆕 V33 (🟡 PRÉPARÉ, 2 s) · V29 reste due**

### La mesure — à la minute, par trois horloges qui ne se connaissent pas

| Heure UTC (16/09) | Événement | Source |
|---|---|---|
| **12:41:08** | `sshd[3366619]` : *Accepted publickey for root from 82.67.153.51 port 26499* | `auth.log` |
| **12:41:08** | `bash -c echo "── dernières lignes vizyo-auth-api"; docker logs --tail 15 vizyo-auth-api 2>&1 \| cut -c1-220; … docker logs --since "2026-09-16T10:55:00Z" --until "…12:05:00Z" vizyo-auth-api …` (pid 3366707, `SSH_CONNECTION=82.67.153.51 26499`) → premier enfant **`docker logs --tail 15 vizyo-auth-api`** (pid 3366708, `futex_wait_queue`, 1 s de CPU) | `ps`, `/proc/3366707/environ` |
| 12:40 → 12:50 | `sar` : **85,22 % → 42,41 %** d'inactivité ; `sys` **4,5 → 30,3 %** — puis 37–40 % et `sys` 32–34 % sur les 75 relevés suivants, jusqu'à 03:20 | `sa16`, `sa17` |
| 12:45:35 | `dockerd` : *« ShouldRestart failed … container=7e9819… execDuration=284h49m … exitStatus 143 … hasBeenManuallyStopped=true »* → **`vizyo-auth-api` recréé 12:45:34** (`ba679c3d…`), rejoint ses réseaux 12:45:38 | `journalctl -u docker` |
| 03:18 (17/09) | client **52 743 s** ; `dockerd` **98,3 %**, cumul **319,5 h** ; **2 connexions établies** sur la socket pour 1 processus `docker` | §1, §4 |

**L'arithmétique du cumul** : 304,6 h hier à 04:36 → 319,5 h ce matin à 03:21 = **+14,9 h**, dont ~0,15 h de
régime de fond (0,7 %) ; reste **14,7 h** de boucle → début ≈ **12:39**, à deux minutes de l'horodatage du client.
**Et `sar` tranche entre le client et la recréation** : sur l'intervalle 12:40–12:50, 42,41 % = 86 %·x + 38 %·(1−x)
→ x ≈ 9 %, soit **≈ 55 s à deux cœurs** — la boucle commence **vers 12:41**, *avec* le `docker logs`, **4 min avant**
que le conteneur ne soit recréé. La recréation n'est pas la cause ; elle a seulement emporté le journal qui aurait dit
s'il était troué.

### QUOI — la classe, sixième exemplaire, et ce que cet exemplaire change

- **6 sur 6 : un `docker logs` sans `timeout`, depuis une session du poste, qui se ferme avant lui.** Ici un
  `bash -c` de diagnostic — le motif *« echo "── …" ; docker logs … | cut »* est celui d'un agent (Claude ou Codex)
  enquêtant sur `vizyo-auth-api` entre 10:55 Z et 12:05 Z, puis redéployant le service à 12:45. L'outil n'est **pas
  nommé** (VPS-M01) ; la classe l'est, pour la 3ᵉ fois depuis le 13/09.
- ⚠️ **Ce qu'elle ne confirme pas** : que les octets NUL de la stanza `logrotate` (VPS-041) soient nécessaires au
  blocage. `vizyo-auth-api` n'était **pas** dans la liste des troués (les 6 sont des `-dev-`), son journal courant
  neuf porte **0 NUL** — et l'ancien fichier est parti avec le conteneur. *Mesure NON FAITE, pas « pas de NUL ».*
  V29 reste utile pour `docker logs` exact ; **elle ne suffit pas à fermer cette classe**, et ce passage le prouve.
- **19 h 21** : V28 faite le 15/09 à 17:20, sixième occurrence le 16/09 à 12:41. *Le remède tue le symptôme en
  2 s ; la cause vit sur le poste, dans les habitudes de qui tape des `docker logs` sur cette machine.*

### `pourquoiInvisible`

Rien ne l'a dit avant ce passage — **14 h 40** : le collecteur ne tourne qu'une fois par jour (angle mort n° 3,
35ᵉ report), et l'autre audit (centre d'alerte) ne regarde pas la machine. La sonde de charge de `/admin/system`
montre l'instant, personne ne la lit la nuit.

### QUOI FAIRE — V33, dans cet ordre, et rien n'a été exécuté

```bash
ps -o pid,ppid,lstart,etime,stat,cmd -p 3366707,3366708   # relire AVANT : le parent est 3366707 (bash -c, PPID 1)
```

```bash
kill 3366707 && sleep 1 && kill 3366708 2>/dev/null ; pgrep -a -x docker   # parent d'abord (cas du 08-20) ; doit rendre vide
```

**Gain** : le cœur, dans la minute (`sar` à 86 % au relevé suivant, comme le 15/09 à 18:40). **Risque** : nul — aucun
conteneur touché. ⚠️ **Ne PAS redémarrer Docker ni la machine (V4) avant** : ~50 s de coupure et l'état qui prouve la
cause disparaît. **Preuve attendue** : `pgrep -x docker` vide, `dockerd` < 10 % au relevé suivant, cumul figé.

**Et le geste qui vise la cause, côté poste, 1 ligne** : dans `CLAUDE.md` (hors périmètre d'écriture de cet audit —
décision du propriétaire) : *« `docker logs` sur le VPS : TOUJOURS `timeout 20 docker logs …`, jamais depuis un
`bash -c` qui peut se fermer avant lui — 6 occurrences de VPS-016 sur 6. »*

### `aNePasFaire`

- ❌ Ne pas lire « 63,33 % sur le 09-16 » comme une demi-marge : c'est 12,7 h à 87 % et 11,3 h à 39 %.
- ❌ Ne pas conclure de cette occurrence que V29 ne sert à rien : elle sert à `docker logs` exact ; elle ne ferme pas
  la classe, et ne l'a jamais promis.
- **Seuil** : ce constat redescend en `SURVEILLANCE` gravité 2 quand `pgrep -x docker` est vide **et** `dockerd` < 10 %
  sur deux passages ; **gravité 1 reste** tant que le client vit.

---

## 4. 🔴 VPS-037 / VPS-M57 / VPS-M73 — le poste dort, et tout ce qu'il porte dort avec lui

**VPS-037 gravité 2 (seuil de gravité 1 à 04:30 ce matin) · V6 + geste `WakeToRun` (30 s, côté poste)**

| Heure UTC | Événement | Source |
|---|---|---|
| 16/09 **23:16:59** | le poste **entre en veille** (événement 42) | journal Windows |
| 17/09 **02:22** | heure de cet audit — **le poste dort** | — |
| 17/09 **03:15:38** | réveil (événement 1, source *« Unknown »*) | journal Windows |
| 17/09 **03:16:25,135 / 03:16:25,356** | le planificateur lance **les deux audits**, 221 ms d'écart | `list_scheduled_tasks` |
| 03:18:28 → 03:22:15 | ma collecte, **228 s** ; l'autre audit ouvre **27 sessions SSH** de 03:21:30 à 03:23:10 (mes sections 6 → 12) | `auth.log`, vérif. E |
| 03:31 | `verify_20260917-033056` produit — **2ᵉ paire depuis la dernière copie réussie** (15/09 04:30) | vérif. B |
| **04:30** | rendez-vous du copieur — `WakeToRun=False`, `NextRunTime 17/09 06:30 local` : **si le poste se rendort avant, 3ᵉ paire sans copie sur 7 jours → gravité 1** | `Get-ScheduledTask` |

**Ce que ça change par rapport à hier** : rien dans le mécanisme, tout dans le compte. Hier : *une* paire de nuit
sans copie par sommeil. Ce matin : **deux** (16/09, 17/09), et le collecteur écrivait **`OK 46 h à jour`** — le seuil
de 48 h ne parle qu'au **2ᵉ** rendez-vous manqué, et « à jour » se lit vert. **Corrigé** : il compte désormais les
manifestes plus récents que le JSON (*« 🔴 2 paire(s) produite(s) DEPUIS la dernière copie réussie — elles n'existent
qu'ICI »*) et rend « 🟠 RENDEZ-VOUS MANQUÉ » dès 30 h.

**VPS-M57, 3ᵉ collision — et c'est structurel, pas un hasard** : après un sommeil, le planificateur rattrape *toutes*
les tâches en retard **à la même seconde** (13/09 : quota ; 17/09 : veille). Deux audits sur une machine à 2 vCPU
dont un cœur est déjà confisqué : ma collecte à **228 s** (audit 13,4 %, `dockerd` 43,2 %, machine à 7,1 %
d'inactivité). ⚠️ **Le collecteur a produit une phrase fausse** : *« la copie hors-site a probablement été manquée
AUSSI »* — à 03:18 elle n'était pas encore due. Corrigé (conditionnel sur l'heure).

### QUOI FAIRE

1. **Côté poste, 30 s, inchangé depuis hier** — et c'est le geste qui décide de la gravité 1 de ce matin :

```powershell
Set-ScheduledTask -TaskName 'Vizyo-Verify-Copie-HorsSite' -Settings (New-ScheduledTaskSettingsSet -WakeToRun -StartWhenAvailable -ExecutionTimeLimit (New-TimeSpan -Hours 2))
```

   Idem pour `audit-vps-tracky` (la tâche Claude n'expose pas `WakeToRun` ; le réveil vient alors d'une tâche
   Windows *« réveil 04:15 »* vide, à créer).

2. **V6** — un second dépositaire qui n'est pas ce poste (la machine reçoit déjà Conductor et Dispocar par
   `recevoir-dump` : le mécanisme inverse existe). **V31** reste due.

### `aNePasFaire`

- ❌ Ne pas lancer le copieur à la main « pour rattraper » (geste, VPS-M81 ; masque la mesure de 04:30).
- ❌ Ne pas déplacer l'audit du centre d'alerte « pour éviter la collision » : la collision ne vient pas des heures
  déclarées (01:09 / 02:22, 73 min d'écart), elle vient du **rattrapage groupé**. Le remède est le réveil, pas l'horaire.

---

## 5. 🆕 VPS-M103 — j'ai lu l'effet de V28 dans une série qui a un cycle diurne

**Gravité 3 · méthode · `A_TRAITER` (règle écrite, à appliquer)**

### La mesure — `trip_automation_runs`, durée par passage (min)

| Jour | Cœurs | 05h | 06h | 07h | 08h | 09h | 10h | 11h | 12h | 13h | 14h | 15h | 16h | 17h | 18h | 19h | 20h | 21h | 22h | 23h |
|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|
| 09-14 | 1 | 13 | 17 | 27 | **56** | **52** | **55** | **59** | **50** | 30 | **55** | **53** | 23 | 16 | 26 | 23 | 7 | 13 | 4 | 10 |
| 09-15 | 1 → 2 à 17:20 | 14 | 11 | 13 | 42 | 38 | **56** | **54** | **51** | **57** | **48** | 34 | 13 | 29 | 15 | 16 | 10 | 5 | 6 | 10 |
| 09-16 | **2** → 1 à 12:41 | 23 | 13 | **54** | **55** | **55** | **53** | 38 | 44 | **47** | **56** | 42 | 19 | 8 | 4 | 8 | 1 | 10 | 1 | 8 |

**Le 09-16 de 07h à 12h, à DEUX cœurs, rend 54 · 55 · 55 · 53 — le 09-14 de 08h à 11h, à UN cœur, 56 · 52 · 55 · 59.**
Le nombre de cœurs ne se lit nulle part dans cette table ; l'heure de la journée, partout. Le rapport du 16/09
(§2, §3) écrivait *« le coût du cœur rendu se lit dans l'application : 34 · 13 · 29 min (15h–17h) → 15 · 16 · 10 · 5 …
(18h–03h) »* : c'était **l'après-midi contre la nuit**. La roadmap du centre d'alerte porte la même lecture (*« 7
passages > 45 min le 14/09 … tant qu'il dure »*).

**QUOI** : comparer une valeur *avant* et *après* un geste sur une grandeur qui a un cycle, sans aligner les heures.
VPS-M69 (débit lu à une seule heure) et VPS-M71 (jour ouvré contre dimanche) sont les deux formes précédentes ;
celle-ci est la troisième : **avant/après un geste = jour/nuit**.

**Ce que ça change** : (1) **V28 n'a un effet mesuré que sur `deploy.sh`** (226 / 292 s contre 303–1 916 s) — et
c'est déjà beaucoup ; (2) **les passages > 45 min qui débordent sur le suivant sont un fait de chaque jour ouvré,
08h–15h, quels que soient les cœurs** — c'est un sujet de volume (trajets clos de jour × analyses IA), donc
applicatif, et la fenêtre HH:42–HH:46 de `deploy.sh` (TRK-077) est **toujours** prise en journée.

**QUOI FAIRE** : avant d'écrire *« avant → après »* sur une série horaire, **comparer la même heure du jour
précédent** (le collecteur le fait déjà pour `positions` : J-7 même jour de semaine). *Règle : un geste se mesure
à heure égale, ou pas.*

**`aNePasFaire`** : ❌ ne pas retirer de VPS-016 le journal T33 — `deploy.sh` n'a pas de cycle diurne, son
avant/après tient.

---

## 6. VPS-044 — première moitié de la preuve, et un seuil qui n'est décidable qu'avec V32 (a)

**Gravité 3 · `A_TRAITER` · V32 (a) 🟡 10 s non faite, (b) 🔧**

```
tracky-api:avant-20260916-0519-0c9672c9   cc1fec4a6ebb   créée 2026-09-15 17:45:25   ← = image du conteneur au passage du 16/09 ✅
tracky-web:avant-20260916-0519-0c9672c9   c8d99972e2eb   créée 2026-09-15 17:44:31   ← idem ✅
tracky-api:latest  80d613258b7a  2026-09-16 05:23:57  = conteneur (584fdeff…, journal T33 87ae29c9 05:24:17, 292 s)
/etc/cron.d/docker-image-prune : 40 0 * * * … --filter "until=24h"        ← INCHANGÉ
```

L'étiquette est **juste** parce que `latest` n'avait pas été pré-construit : `deploy.sh` a étiqueté ce qui tournait.
Le seuil d'`APPLIQUE` (*« deux passages de suite »*) **ne peut pas être atteint demain** : étiquetée à 05:19 le 16/09,
elle aura **43 h au ménage du 18/09 00:40** → supprimée, sauf V32 (a) ou un nouveau déploiement d'ici là. *Un seuil
dont la source disparaît avant l'échéance est un test indécidable (VPS-M94)* : le seuil est réécrit *« deux passages
de suite **où une étiquette existe** »*, et V32 (a) devient la condition de la mesure, pas seulement du confort.
🆕 Le collecteur porte désormais `imagesProdListe` et vérifie l'arrière à chaque passage (première pose ce matin).

Images : **32 → 29** — parties : `avant-…-1731` ×2 **et** `avant-…-1741` ×2 (toutes étiquetées 17:31–17:41 → 31 h ;
hier je n'avais prédit que les 1731) et `alpine` ; arrivées : `latest` api + web. Prédiction d'hier (« 30 ou 32 ») :
fausse de 1 sur les `avant-1741`, juste sur la règle.

---

## 7. Les autres constats, re-mesurés

| Fiche | Grav. | Mesure du 17/09 | Écart / 16-09 |
|---|:--:|---|---|
| **VPS-041 / V29** — deux rotateurs | 2 | `logrotate.service` 00:00:00 → **00:01:17**, **21,92 s de CPU** (1,63 hier, 13,2 avant-hier : suit la taille des fichiers de production, recréés la veille) ; **6 troués** = les mêmes `-dev-` (`maalem-dev-api` 80 %, `maestroo-dev-api` 93 %, `maestroo-dev-web` 96 %, `maalem-dev-web` 98 %, `maalem-dev-admin` 98 %, `maestroo-dev-lp` 94 %) ; **13 à 0 octet** (à 03:2x, ≈ comparable à 02:23) ; stanza présente | 5ᵉ nuit, ×13 en CPU |
| **VPS-042 / V30** — dépôts | 2 | `dispocar-distant` **8 dumps** : **`dev` 09-17 = 44 064 o** (première non vide) ; `prd` 09-14 → 09-17 = **1 172 o ×4** ; `vizyo-conductor-distant` **20** fichiers, `prd` 29,7 Ko à 03:06 (l'heure de dépôt a glissé de 01:0x à 03:06) ; session `vaultbk` **172 h**, aucune commande ; comptes **4 = 4** | la question de V30 se précise sur `prd` |
| **VPS-038** — cohorte | 2 | mercredi 09-16 complet **33** ; 33 / 33 / 33 ; registre 11 / 11 / 11 ; **`…6714`** : dernière position **09-16 15:28:34**, **1 612 trames / 24 h**, `lastSeenAt` 03:30 — 4ᵉ épisode « trames sans position » ; 10 silencieux > 6 h sur `positions`, 0 sur `wire_logs` = garés | seuil tenu |
| **VPS-033 / V14** — `apt` | 2 | **NON MESURABLE** (cache 01:26 du 16/09, 25 h ; `apt-daily` a sonné 20:34 = 2ᵉ sonnerie, 1 s, rien — la loi du 14/09 tient, 4 valides / 20) ; ✅ **VPS-M74 clos par la mesure** : 3 sécurité → 0, installés 06:52:26 | canal prouvé, mesure aléatoire |
| **VPS-043 / V31** — copieur | 2 | pas de tentative depuis le 15/09 04:30 (poste), mécanisme intact ; `verify` 03:31 : 16 manifestes | inchangé |
| **VPS-030 / V17** — hors rétention | 3 | `/root/backups` 1 679 Mo / 12, `/opt/backups` 57 Mo, racine `/var/backups` 3 fichiers (177 Ko), `avant-chantier-…1720.dump` 161 Mo — les mêmes | inchangé |
| **VPS-M56 / V5** — budget | 2 | **228 s** (2,5×), 1,78 → 4,51 ; audit **13,4 %**, `dockerd` **43,2 %**, inexpliqué 20,8 % (l'autre audit y est) ; `/opt` **48 s** (17/20) | 27ᵉ dépassement, cause nommée |
| **VPS-M73** — passages | 3 | **+56 min** (03:18), poste endormi 23:17 → 03:15 Z ; 🆕 phrase fausse du bloc corrigée | en retard, dit |
| **VPS-M92** — périmètre | 2 | 38 / 4 / 12 identiques — 🆕 **mais 3 conteneurs recréés en 24 h** (`tracky-web` 05:24:04, `tracky-api` 05:24:05 — journal T33 ✅ ; **`vizyo-auth-api` 12:45:34 — hors journal**, geste D1 hors script pour une autre pile) : angle mort traité | angle mort traité |
| **VPS-M93** — série `MOVING` | 2 | lun 18,0 · mar 20,1 · **mer 09-16 23,7 %** (complets) ; INSERTED 28,2 → 28,3 → **30,1** | cohérente, monte |
| **VPS-005 / V7** — limites | 2 | 31 / 38 sans limite, 0 CPU | inchangé |
| **VPS-010 / V4** — noyau | 2 | -136 actif, -139 installé, uptime **43,2 j**, `libc6` sous 6 services ; ⚠️ **re-bloquée** : V33 avant V4 | bloquée par V33 |
| **VPS-012** — clés CI | 2 | `github-actions-vizyo-auth` 0 / 7 j, sans option ; `vizyo-vps-hostinger` **3 105** | 12ᵉ passage sans geste |
| **VPS-032** — sessions SSH | 3 | **3 138 / 7 j** ; 177 · 111 · 1 055 · 506 · 607 · **557** | cohérent |
| **VPS-036** — hyperviseur | 2 | 0 inattendue, 52 / j × 6 ; 3 dernières = `fstrim` + télémétrie 03:14 | inchangé |
| **VPS-026** — `alpine` | 3 | absent 03:19 → re-tiré **03:30:59** ; partira le 18/09 00:40 (45 h) — 23ᵉ prédiction | conforme |
| **VPS-011** — healthchecks | 3 | 72 / min ≈ 103 680 / j ; 28 / 38 | inchangé |
| **VPS-018** — `/opt/vizyo-leads` | 4 | 823 Mo, 7,4 s / 47,6 s (15,5 %) | — |
| **VPS-040** — démo | 3 | base **541 Mo** (+8) ; `trips` 256 / 24 h (×1,51 J-7 : rejeu) | — |
| **VPS-M102** — marge | 3 | ✅ **tenu ce passage** : 5 vérifications en marge, toutes sous `nice -n 19 ionice -c3 timeout 20`, aucun `find` au-dessus d'un dossier nommé (le seul : `/var/backups/vizyo-verify -maxdepth 1`) | règle appliquée |

### Le disque, ventilé

| Poste | 09-16 | **09-17** | Écart |
|---|---:|---:|---:|
| `df /` | 53 Go (55 %) | **53 Go (55 %)** | 0 |
| `docker system df` — Images | 26,5 Go (32) | **26,51 Go (29)** | −3 images, même poids (couches partagées) |
| cache de build | 9,812 Go | **9,854 Go** | +42 Mo (build 05:23) |
| `/var/lib/docker/containers` | 282 Mo | **303 Mo** | +21 |
| `/var/backups` | 5,5 Go | **5,4 Go** | −0,1 (rétention `tracky`) |
| inodes | 1 756 999 | **1 759 426** | +2 427 |

---

## 8. Plan d'action — par ordre de RENDEMENT

> Par **ce que ça rapporte pour ce que ça coûte**. Un bloc = une décision. **Rien de ce qui suit n'a été exécuté.**

### 1️⃣ Rendre le cœur — **V33** *(2 s, risque nul, un cœur sur deux en jeu depuis 14 h 40)*

```bash
ps -o pid,ppid,lstart,etime,stat,cmd -p 3366707,3366708   # relire AVANT
```

```bash
kill 3366707 && sleep 1 && kill 3366708 2>/dev/null ; pgrep -a -x docker
```

**Gain** : `sar` à 86 % au relevé suivant, `deploy.sh` ×2–6 plus rapide, collecte 228 → ~140 s. **Contrepartie** :
aucune. ⚠️ **Avant V4**, jamais après. Puis **une ligne dans `CLAUDE.md`** : `docker logs` sur le VPS = `timeout 20`,
jamais depuis un `bash -c` qui peut mourir avant — c'est la cause des six.

### 2️⃣ Réveiller le poste — VPS-037 *(30 s, la 3ᵉ paire sans copie se joue à 04:30 ce matin)*

```powershell
Set-ScheduledTask -TaskName 'Vizyo-Verify-Copie-HorsSite' -Settings (New-ScheduledTaskSettingsSet -WakeToRun -StartWhenAvailable -ExecutionTimeLimit (New-TimeSpan -Hours 2))
```

**Preuve** : demain, `DERNIERE-COPIE-LOCALE.json` du **17/09 04:30** avec `pairesCopiees 2` — ou le collecteur qui rend
*« 3 paires produites depuis la dernière copie réussie »* et VPS-037 en gravité 1. Puis **V6**.

### 3️⃣ Donner 3 jours au repli — **V32 (a)** *(10 s ; sans lui, la preuve de VPS-044 est indécidable)*

```bash
cat /etc/cron.d/docker-image-prune   # relire AVANT
```

```bash
sed -i 's/until=24h/until=72h/' /etc/cron.d/docker-image-prune
```

**Gain** : `avant-20260916-0519` survit au 18/09 00:40 et le collecteur peut rendre le 2ᵉ ✅. **Contrepartie** :
~4 Go d'images 2 jours de plus, sur 44 Go libres.

### 4️⃣ Un seul rotateur — **V29** *(10 s, avant minuit ; 21,9 s de CPU cette nuit, 6 troués)*

```bash
mv /etc/logrotate.d/docker-containers /root/logrotate.d-docker-containers.retire-2026-09-17
```

⚠️ `rotate 14` y est posé pour TRK-035 → `max-file: 14` sur le seul `tracky-postgres` (compose). **Ce que ça ne fait
pas** : fermer VPS-016 — la 6ᵉ occurrence ne visait pas un troué.

### 5️⃣ Trancher `prd` Dispocar — **V30** *(décision, 0 s)*

`dev` rend 44 Ko depuis ce matin, `prd` 1 172 o pour la 4ᵉ nuit : le script côté Dispocar vise probablement une base
`prd` vide ou inexistante — *se vérifie côté Dispocar, pas ici*. Et : à qui sont `179.198.198.199`, les 3 comptes, la
session `vaultbk` de 172 h ?

### 6️⃣ Planifier le redémarrage — **V4** *(après V33 seulement)*

Uptime 43,2 j, noyau -136 / -139, `libc6` sous 6 services. Créneau **23:30**, hors HH:42–HH:46, hors 02:20–02:30,
hors 03:00–04:55. 38 / 38 `unless-stopped`.

### 7️⃣ Fixer l'heure du rafraîchissement `apt` — **V14** *(60 s ; 4 valides / 20 — le canal installe, la mesure reste au hasard)*

```bash
systemctl edit apt-daily.timer   # [Timer]  OnCalendar=  OnCalendar=*-*-* 01:30:00  RandomizedDelaySec=15m
```

### 8️⃣ Ranger les archives de la fenêtre — **V17** *(30 s, 161 Mo + 177 Ko)* — inchangé depuis hier.

---

## 9. Améliorer l'agent

### a. Ce qui a été corrigé dans `collecte.sh` pendant ce passage

| Défaut | Ce qu'il produisait | Correctif, et sa vérification |
|---|---|---|
| **La copie hors-site rendait « à jour » à 46 h sur une paire sans copie** (VPS-037) | Le seuil de 48 h ne parle qu'au 2ᵉ rendez-vous manqué ; « OK 46 h à jour » se lit vert. Hier j'avais même prédit *« PÉRIMÉE (46 h) »* — faux sur le seuil. | Trois tiers (≤ 30 h à jour · 30–48 h 🟠 RENDEZ-VOUS MANQUÉ · > 48 h PÉRIMÉE) **et** le compte des manifestes plus récents que le JSON : *« 🔴 2 paire(s) produite(s) DEPUIS la dernière copie réussie — elles n'existent qu'ICI »*. **Banc** : 47 h, 2 paires, 1 rendez-vous écoulé. Coût : un `find -maxdepth 1 -newermt`, 0 s. |
| **Angle mort n° 1 du 16/09 — l'arrière du déploiement n'était pas vérifié** *(1ᵉʳ report, traité)* | L'étiquette `avant-*` pouvait pointer une image jamais déployée (VPS-044) sans qu'aucune ligne ne le dise. | Clé `imagesProdListe` (`tracky-api=<id>,tracky-web=<id>`) posée au manifeste ; le collecteur compare la dernière `avant-*` (tri **lexical du nom**, pas date de l'image — `avant-1731` pointait une image plus vieille que `avant-1741`) à l'image du conteneur au passage précédent : ✅ repli RÉEL / 🔴 le repli MENT / (pas de déploiement). **Banc** : première pose → « NON VÉRIFIÉ ». Coût : 2 `docker image inspect`. |
| 🆕 **Les conteneurs RECRÉÉS étaient invisibles** (VPS-M92 compare des noms) | `vizyo-auth-api` recréé le 16/09 12:45 — hors journal T33, geste D1 hors script pour une autre pile — et aucune ligne ne le disait. | Bloc « Conteneurs RECRÉÉS depuis 24 h » (`docker ps --format CreatedAt`). **Banc** : 3 lignes (`tracky-web` 05:24:04, `tracky-api` 05:24:05, `vizyo-auth-api` 12:45:34). Coût : un `docker ps`. |
| 🆕 **Le bloc budget affirmait une copie manquée qui n'était pas encore due** | *« la copie hors-site (04:30) a probablement été manquée AUSSI »* — à 03:18, elle était dans 72 min. | Conditionnel sur l'heure de départ : avant 04:30 → *« n'est PAS encore due : elle aura lieu seulement si le poste reste éveillé »*. |

### ⚠️ Ce que ce passage a réfuté dans mon propre travail d'hier

1. **VPS-M103** — *« le coût du cœur rendu se lit dans l'application »* (§2 et §3 du 16/09) comparait l'après-midi
   à la nuit. La table du §5 le montre à trois cœurs près : 54 · 55 · 55 min **à deux cœurs** le matin du 16/09.
2. La prédiction *« 30 ou 32 images »* oubliait les `avant-1741` (même règle, même heure d'étiquetage) : **29**.
3. La prédiction *« PÉRIMÉE (46 h) »* se trompait de seuil (48 h) — et ce seuil était lui-même le défaut.

### b. Angles morts restants

1. **Rien ne surveille `dockerd` entre deux passages** — 14 h 40 de cœur brûlé avant qu'une ligne le dise ; la 5ᵉ
   avait duré 60 h 46 pour la même raison. *Piste* : `/admin/system` porte déjà la charge ; une sentinelle Tracky
   *« charge 1 min > 1,5 pendant 30 min sans déploiement »* (côté centre d'alerte, T-quelque chose) — ou, à coût nul,
   lire `sar` **au passage du centre d'alerte** aussi (il tourne à 01:09). **35ᵉ report** (fusionné avec « rien ne
   surveille les sauvegardes entre deux passages » : c'est le même angle).
2. **Le rattrapage groupé du planificateur n'est pas prévu par le catalogue** (VPS-M57 ×3). *Piste* : le collecteur
   sait déjà compter les sessions pendant sa fenêtre ; ajouter *« si > 10 sessions d'une autre origine en 2 min →
   🟠 rattrapage groupé probable, lire `list_task_runs` »*. **1ᵉʳ report.**
3. **Le collecteur ne voit pas le journal du copieur** (`copie.log`, poste). **3ᵉ report.** *Atténué* : le compte de
   paires côté VPS dit désormais l'essentiel.
4. **Les fiches `APPLIQUE` ne sont re-mesurées par rien.** **12ᵉ report.**
5. **20,8 points d'inactivité inexpliqués** pendant la collecte — cette fois l'autre audit y est, non ventilé.
   **13ᵉ report.**
6. **La comparaison des certificats ne couvre que 2 domaines sur 28.** **9ᵉ report.**
7. **Le collecteur ne sait pas qu'il tourne en même temps qu'un autre audit** (VPS-M57). **6ᵉ report** — et
   l'angle n° 2 en est la version mesurable.
8. 🆕 **Aucune série du collecteur n'est alignée sur l'heure du jour** (VPS-M103) : `trip_automation_runs` n'y est
   pas ; `positions` a J-7 ; les autres n'ont que la veille. *Piste* : pour toute série horaire, imprimer *« même
   heure, J-1 »* à côté de *« relevé précédent »*.

### c. Une question ouverte pour le prochain passage

**Six `docker logs` sur six viennent du poste — combien de commandes tapées sur cette machine chaque jour n'ont
aucune garde, et par qui ?** `auth.log` compte **557 sessions le 09-16** et 3 105 connexions de la clé
`vizyo-vps-hostinger` en 7 jours ; le collecteur en fait ~150/jour, `deploy.sh` quelques dizaines. Le reste — plusieurs
centaines par jour — ce sont des sessions d'agents (Claude, Codex) qui lisent, diagnostiquent, redéploient, et dont
**aucune n'est journalisée autrement que par `sshd`**. La 6ᵉ occurrence a été identifiée par `SSH_CONNECTION` dans
`/proc/<pid>/environ` d'un processus **encore vivant** ; morte, elle n'aurait laissé qu'un port source. *La question
n'est pas « faut-il un `timeout` sur `docker logs` » (c'est une ligne) — c'est : quel outil du poste parle à cette
machine, sous quelle règle, et où ça s'écrit.* La roadmap le nomme déjà en creux (T66 : *« le garde-fou marche ; son
silence, non »*) ; VPS-032 le compte depuis le 22/08 sans jamais dire *qui*.

---

## Annexe — comment reproduire ce rapport

```bash
ssh root@72.62.26.240 'bash -s' < docs/vps-audit/collecte.sh > /tmp/collecte-vps.txt
```

**Une seule passe complète** (`FIN DE COLLECTE`, `stderr` **vide**, 14 sections) :

| Passe | Fenêtre UTC | Durée | Rôle |
|---|---|---:|---|
| 1 | 03:18:28 → 03:22:15 | **228 s** | **la mesure du jour**, archivée dans `collectes/2026-09-17.txt` — ⚠️ produite **avant** les trois correctifs ; ⚠️ **+56 min** (poste endormi) ; ⚠️ **en collision** avec l'audit du centre d'alerte (même seconde de départ, 27 sessions SSH de 03:21:30 à 03:23:10) ; ⚠️ `dockerd` à 43 % de la machine |

⚠️ **Les trois correctifs ont été vérifiés sur un banc isolé, sur la machine, en lecture seule** (1 s + 0 s), sous
`nice -n 19 ionice -c3`, pas en relançant 228 s de collecte.

Vérifications ciblées en marge, **toutes en lecture seule, toutes sous `nice -n 19 ionice -c3 timeout 20`**
(VPS-M102 tenu) :

- **A.** `ps -o … -p 3366708,3366707`, remontée des ancêtres par `/proc/<pid>/stat`, `/proc/3366707/environ`
  (`SSH_CONNECTION`), `/proc/913/stat` (cumul 319,5 h) ; `sar -u -f sa16` (12:00 → 23:50) et `sa17` ; `journalctl -u
  docker` (12:30–12:45, et depuis le 15/09) ; `auth.log` port 26499 — le §3 ;
- **B.** `docker images --format` (étiquettes `avant-*`, `latest`, `alpine`), `docker inspect .Image/.Created` sur
  4 conteneurs, `docker image inspect alpine:latest` (`LastTagTime`), `ls -la` de `dispocar-distant` et
  `vizyo-conductor-distant`, `find /var/backups/vizyo-verify -maxdepth 1 -newermt` — les §2, 4, 6, 7 ;
- **C.** `tail -3 /opt/tracky-deploiements/journal.jsonl` — le §6 ;
- **D.** `unattended-upgrades.log` et `dpkg.log` du 16/09 06:52 — le §2 ;
- **E.** `auth.log` 03:18–03:23 (sessions pendant la collecte) — le §4 ;
- **F.** `positions` : émetteurs distincts par jour calendaire (9 j) ; `…6714` ; `trip_automation_runs` du 14 au
  17/09 (durée par passage) — les §2, 5, 7 ;
- **G.** `journalctl -u logrotate`, `systemctl show logrotate.service -p CPUUsageNSec`, `stat` des `*-json.log`
  courants, `tr -cd '\000' | wc -c` sur le journal de `vizyo-auth-api`, `DERNIERE-COPIE-LOCALE.json`,
  `/etc/cron.d/docker-image-prune`, `ss -xp` sur `docker.sock` — les §3, 6, 7 ;
- **côté poste** : `Get-ScheduledTask` / `Get-ScheduledTaskInfo` (`WakeToRun`), `copie.log`, `Get-WinEvent`
  (événements 1 / 42 / 107), `list_scheduled_tasks` (heures de lancement des deux audits) — le §4.
